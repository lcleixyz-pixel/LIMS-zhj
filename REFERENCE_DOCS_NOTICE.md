# 参考资料：版权拆分说明

> 2026-07-11 建立。本文件告诉团队：`参考/` 目录去哪了、为什么、影响什么。
> 用户要求"写清说明"——本文件就是那份说明。

## 一句话

`参考/` 目录（含 CNAS 质量手册包、CL01 准则、GB/T 27025 标准等 **214 个版权原件**）已从主仓移出，放到本地 `LIMS-zhj-参考-private/`，**永不入 git、永不 push、永不开源**。这是项目总体规划第七条红线"标准原文永不入分发包/开源仓"的硬性要求。

## 为什么必须移走

主仓要做团队审计、未来可能开源。`参考/` 里 247 个 git 跟踪文件中，**214 个是版权敏感的体系文档原件**：

| 类型 | 数量 | 代表内容 |
|---|---|---|
| .doc | 126 | 2020/2025 版 CMA/CNAS 质量手册与程序文件包 |
| .pdf | 62 | CNAS-CL01:2018 准则、CL01-G001:2024 应用要求、CL01-A015:2018 珠宝领域说明、GB/T 27025-2019 |
| .docx | 25 | 2023 版检验检测机构资质认定评审准则、记录表格包 |
| .txt | 1 | GB/T 27025-2019 标准原文 txt |

这些是**他人版权材料**。push 到公开仓或打包分发，即构成大规模版权泄露。

## 移到哪

**`01-项目代码/LIMS-zhj-参考-private/`**（与主仓同级，纯本地目录）

- 该目录**不建 git 仓库**（连本地仓都不建，杜绝任何 push 风险）。
- 247 个文件完整在里面，含所有版权原件 + 20 个自写 .md 分析。
- 要查标准原文、对照 CNAS 包、看 2025 版质量手册范例，**直接去那里翻**。

## 主仓里保留了什么

`参考/` 里 20 个 .md 是自己写的分析笔记（非版权），已复制到 **`docs/参考分析/`** 保留在主仓。包括 Kimi_Agent 珠宝检测方案分析、TB 内审管审笔记等。

## 对代码的影响（重要，开发者必读）

**只有一个服务受影响**：`jewelry-qms/app/service/QmsPlanningImportService.php`

- 该服务是**建设期工具**，用于从官方标准文件解析清单、导入规划数据。**日常记录填报、打印、PDF 不调用它，系统日常运行零影响。**
- `officialSourceManifest()` 原本列 5 个标准 PDF/DOCX 路径指向 `参考/`。**已加容错**：文件缺失时标记 `file_present=false`、`status=missing_local_reference`，**不报错、不阻断**。
- `extractReferenceManualSupplements()` 原本已容错（文件缺失返回空数组）。
- **想重新跑规划导入时**：需在持有标准原件的环境运行——把 `LIMS-zhj-参考-private/` 内容放回 `参考/`，或做软链。

## 开源/分发前的复查命令

```bash
# 参考/ 必须不在主仓（应返回 0）
git ls-files 参考/ | wc -l

# 全仓不得含标准原件（应无输出）
git ls-files | grep -iE "CNAS|GB.*T.*27025|CL01|评审准则"
```

## 与拆仓的关系

本操作与 2026-07-09 的"参考系统拆仓"（移走 flinkiso 等 3 套代码到 `LIMS-zhj-reference/`）同类，都是为让主仓可审计/可开源。区别：

| | 拆仓（0709） | 版权拆分（0711） |
|---|---|---|
| 移走 | flinkiso 等开源参考代码 | 标准/体系原件（版权） |
| 去向 | `LIMS-zhj-reference/`（独立 git 仓，可分享） | `LIMS-zhj-参考-private/`（纯本地，永不外传） |
| 性质 | 整洁性 | 版权合规红线 |

## 相关文件

- 拆仓 spec：`docs/superpowers/specs/2026-07-09-repo-split-archive-design.md`
- 变更台账：`docs/变更台账.md`
- 规划第七条红线：`整合/项目总体规划-v1.1.md`
- 受影响服务：`jewelry-qms/app/service/QmsPlanningImportService.php`
