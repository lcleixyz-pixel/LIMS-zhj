# LIMS-zhj QMS 修订治理进度

更新时间：2026-07-07

## 当前阶段

已完成“修订草案和治理准备”的三项用户拍板，并已形成第五版候选修订包、LIMS 预导入包、3 个代表性记录模板模拟试填包、26 个记录模板全量模拟试填包、记录模板字段字典包、受控发布治理演练包、发布执行记录模板包、机构人员学习实施包、LIMS 人工评审包、人工评审工作台、人工评审决策回填预览、人审通过模拟包、LIMS 第一阶段写库行级预览包、LIMS 第二阶段结构化导入行级预览包、第二阶段结构化导入人工复核工作台、第二阶段结构化复核意见回填预览包、质量手册修订/换版路径包、治理就绪总览包、治理关闭工作台、治理闭环执行包、治理关闭最小试点包、治理关闭试点回填预览包、治理关闭试点源工作台回填补丁预演包、治理关闭试点人工执行工作簿、治理关闭试点真实执行交回包、治理关闭试点人工执行模拟完成包、治理关闭意见回填预览包、治理就绪刷新预览包、`jewelry-qms` 命令层 dry-run 入口、字段字典命令层 dry-run、受控发布演练命令层 dry-run、发布执行记录模板命令层 dry-run、质量手册修订路径命令层 dry-run、机构人员学习实施包命令层 dry-run、第二阶段人工复核工作台命令层 dry-run、第二阶段复核意见预览包命令层 dry-run、治理就绪总览命令层 dry-run、治理关闭工作台命令层 dry-run、治理闭环执行包命令层 dry-run、治理关闭最小试点包命令层 dry-run、治理关闭试点回填预览命令层 dry-run、治理关闭试点源工作台补丁预演命令层 dry-run、治理关闭试点人工执行工作簿命令层 dry-run、治理关闭试点真实执行交回包命令层 dry-run、治理关闭试点人工执行模拟完成包命令层 dry-run、治理关闭意见预览命令层 dry-run、治理就绪刷新预览命令层 dry-run、`apply-rehearsal` 非写库演练、`--write-preview-dir` 第一阶段行级预览、`--stage2-preview-dir` 第二阶段行级预览、`--stage2-review-dir` 第二阶段复核闸门、`--stage2-review-preview-dir` 第二阶段复核预览闸门、`--governance-readiness-dir` 治理总闸门、`--governance-closure-dir` 治理关闭证据闸门、`--governance-closure-execution-dir` 治理闭环执行闸门、`--governance-closure-pilot-dir` 治理关闭最小试点闸门、`--governance-closure-pilot-return-dir` 治理关闭试点回填预览闸门、`--governance-closure-pilot-source-update-dir` 治理关闭试点源工作台补丁预演闸门、`--governance-closure-pilot-operator-workbook-dir` 治理关闭试点人工执行工作簿闸门、`--governance-closure-pilot-operator-handback-dir` 治理关闭试点真实执行交回闸门、`--governance-closure-pilot-operator-completion-simulation-dir` 治理关闭试点人工执行模拟完成包闸门、`--governance-closure-preview-dir` 治理关闭意见预览闸门、`--governance-readiness-refresh-dir` 治理就绪刷新预览闸门和第二阶段结构化导入预检。下一步可进入“人工评审候选手册 + 核对试填结果 + 确认 05-02 编号附件/表单归属 + 确认质量手册修订/换版路径 + 确认受控发布治理演练 + 确认机构人员学习实施包 + 确认发布执行记录模板 + 复核第二阶段块级链接并回填预览 + 先用治理关闭试点人工执行工作簿补齐 5 个低阻断批次 + 用真实执行交回包收回真实证据、执行人、复核人、日期和交接结果 + 仅把模拟完成包作为命令链路参考 + 用试点回填预览确认真实结果能安全回到源工作台 + 用源工作台补丁预演确认逐字段回填不会改错源表/源字段 + 按治理总览、治理关闭工作台、治理闭环执行包、治理关闭意见预览和治理就绪刷新预览关闭阻断项”。

当前不改现用《质量手册（第四版）》、不写 LIMS 生产库、不发布受控文件。第五版候选稿、程序目录、记录模板、实施计划、验证矩阵、预导入 CSV/JSON、代表性与全量模拟试填样表、字段字典、受控发布演练包、发布执行记录模板包、机构人员学习实施包、人工评审清单、apply-rehearsal 演练材料、第一阶段写库行级预览包、第二阶段结构化导入行级预览包、第二阶段结构化导入人工复核工作台、第二阶段结构化复核意见回填预览包、质量手册修订/换版路径包、治理就绪总览包、治理关闭工作台、治理闭环执行包、治理关闭最小试点包、治理关闭试点回填预览包、治理关闭试点源工作台回填补丁预演包、治理关闭试点人工执行工作簿、治理关闭试点真实执行交回包、治理关闭试点人工执行模拟完成包、治理关闭意见回填预览包和治理就绪刷新预览包已经放入交接箱。模拟完成包只用于验证命令链路，不代表真实执行；后续应先人工评审并确认质量手册修订/换版路径，再先用试点人工执行工作簿补齐少量人工闭环，用真实执行交回包收回真实证据、执行人、复核人、日期和交接结果，并用试点回填预览确认真实结果能安全回到源工作台，再用源工作台补丁预演确认逐字段回填不会改错源表/源字段，然后按治理总览、治理关闭工作台、治理闭环执行包、治理关闭意见预览和治理刷新预览关闭阻断项，最后才讨论是否进入 LIMS 受控流程。

## 已完成

- 已实现桥接治理检查脚本：`jewelry-qms/scripts/qms_current_manual_bridge_check.py`。
- 已生成桥接检查报告：`.team/交接箱/2026-07-07-qms工程包到LIMS桥接治理检查.md`。
- 已生成第五版候选修订准备包：`.team/交接箱/2026-07-07-第五版候选修订准备/`。
- 已生成第五版候选手册正文：`.team/交接箱/2026-07-07-第五版候选修订准备/10-质量手册第五版候选稿.md`。
- 已生成支持性程序目录、记录模板、实施计划、验证矩阵和依据现行性复核记录。
- 已新增只读验证脚本：`jewelry-qms/scripts/qms_draft_package_check.py`。
- 已新增 LIMS 预导入生成与 dry-run 脚本：`jewelry-qms/scripts/qms_lims_preimport_build.py`、`jewelry-qms/scripts/qms_lims_preimport_check.py`。
- 已通过候选包门禁：条款覆盖 29/29，LIMS 2022 编号 38/38，发现项 0。
- 已生成 LIMS 预导入包：`.team/交接箱/2026-07-07-第五版候选修订准备/lims_preimport_package/`。
- 已通过预导入包 dry-run：文件/结构化文件 65 条、记录模板 26 条、追溯矩阵 29 条、手册块级索引 29 条、外来依据 4 条，发现项 0。
- 已新增记录模板模拟试填生成与 dry-run 脚本：`jewelry-qms/scripts/qms_record_template_trial_build.py`、`jewelry-qms/scripts/qms_record_template_trial_check.py`。
- 已生成记录模板模拟试填包：`.team/交接箱/2026-07-07-第五版候选修订准备/record_template_trial_pack/`。
- 已通过记录模板试填 dry-run：公正性风险、合同评审、内审整改 3 个实例，Markdown 试填表 3 份，发现项 0。
- 已扩展记录模板试填脚本支持 `--all-templates` 全量模式。
- 已生成记录模板全量模拟试填包：`.team/交接箱/2026-07-07-第五版候选修订准备/record_template_full_trial_pack/`。
- 已通过全量试填 dry-run：26 个记录模板、26 个 JSON 实例、26 份 Markdown 试填表，发现项 0。
- 已生成记录模板字段字典包：`.team/交接箱/2026-07-07-第五版候选修订准备/record_template_field_catalog/`，含 26 份逐模板字段字典、437 条字段级明细、通用字段覆盖矩阵和字段索引；dry-run 验证发现项 0。字段字典用于人工评审和 LIMS 字段配置准备，不写库，不代表受控发布。
- 已将字段字典接入 LIMS 预导入命令层：`qms:preimport-package --field-catalog-dir` 可读取 `record_template_field_catalog/`，并对照预导入记录模板 schema 校验 26 个模板、437 条字段明细、26 份逐模板字段字典；命令层 dry-run 发现项 0，仍不写库、不代表受控发布。已验证该参数与 `--stage2-check` 可组合运行；带 `--apply --ack-human-reviewed --review-dir --field-catalog-dir` 时仍因 67 个 pending 人工决策返回 blocked，复跑 dry-run 显示未发生记录模板写库。
- 已新增 LIMS 预导入命令：`php think qms:preimport-package`，默认 dry-run，`--apply` 需配 `--ack-human-reviewed`。
- 已通过命令层 dry-run：识别文件候选 65 条、记录模板候选 26 条、追溯矩阵 29 条、外来依据 4 条；37 个现行程序文件全部匹配，`XZTC/CX-05-02-2022` 已按编号附件/表单分流，`missing_reference_current_documents=0`。
- 已验证 apply 闸门：未提供人工确认时返回 blocked；复跑 dry-run 显示候选记录模板仍为 0 个已匹配，未发生写库。
- 已新增 LIMS 人工评审包生成与 dry-run 脚本：`jewelry-qms/scripts/qms_human_review_pack_build.py`、`jewelry-qms/scripts/qms_human_review_pack_check.py`。
- 已生成 LIMS 人工评审包：`.team/交接箱/2026-07-07-第五版候选修订准备/human_review_pack/`。
- 已通过人工评审包 dry-run：条款评审 29 条、记录模板评审 26 条、05-02 归属判定 1 条、apply 前闸门 11 条，发现项 0；所有人工决策仍为 pending。
- 已将人工评审包接入 `qms:preimport-package` 命令层：新增 `--review-dir`，apply 需要 `--apply --ack-human-reviewed --review-dir` 且评审包全部通过。
- 已通过 review-pack 命令层 dry-run：`review_pack_status=pending`、`review_pack_pending_decisions=67`。
- 已验证带 ack 但人工评审包 pending 的 apply 闸门：返回 blocked，未进入写库事务，复跑 dry-run 显示候选记录模板仍为 0 个已匹配。
- 已新增 `--stage2-check` 第二阶段结构化导入预检：LIMS 目标表 `qms_structured_documents`、`qms_document_blocks`、`qms_document_block_links` 均可用；65 条结构化文件、29 个手册块、29 条追溯矩阵可形成导入计划；手册块与追溯矩阵条款错配为 0；计划程序块链接 93 个、附件/表单块链接 2 个、记录模板块链接 30 个；当前仍因人工评审包 67 个 pending 决策而处于 `blocked_by_human_review`。
- 已新增 LIMS 人工评审工作台：`human_review_workbench/` 按总览、角色、条款、记录模板、05-02 归属和 apply 闸门组织 67 个 pending 决策项，并提供 `decision_update_template.csv` 供线下评审意见整理；dry-run 验证发现项 0，工作台不修改 `human_review_pack/`，不写库，不代表批准。
- 已新增 LIMS 人工评审决策回填预览：`human_review_decision_preview/` 从 `human_review_workbench/decision_update_template.csv` 生成回填预览，检查非法决策、缺少说明、05-02 处置语义与 LIMS 通过语义差异；当前回填就绪度为 `no_proposed_decisions`，67 个决策项仍全部阻断，dry-run 验证发现项 0；预览包不修改 `human_review_pack/`，不能作为 `--review-dir`。
- 已新增受控发布治理演练包：`controlled_release_rehearsal/` 覆盖 65 个发布对象、28 个审批签核演练项、29 个培训宣贯演练项、28 个旧版处置演练项、5 个口径闸门和 4 个实施有效性检查项；dry-run 验证发现项 0。该包不写库，不代表受控批准或发布。
- 已将受控发布演练包接入 `qms:preimport-package` 命令层：新增 `--release-plan-dir`，命令层 dry-run 显示 `release_plan_status=passed`、`release_plan_release_allowed_now=0`。带字段字典、人工评审包和 `--stage2-check` 的组合 dry-run 仍只因 67 个 pending 人审项处于 `blocked_by_human_review`；带 `--apply --ack-human-reviewed --review-dir --release-plan-dir` 的闸门验证返回 `blocked`，未进入写库事务。
- 已新增发布执行记录模板包：`release_execution_template_pack/` 覆盖 6 个发布执行记录候选模板、120 条字段明细、6 条模拟试填，来源对应 65 个发布对象、28 个审批项、29 个培训项、28 个旧版处置项和 4 个有效性检查项；dry-run 验证发现项 0。该包不写库，不代表受控发布或真实运行记录形成。
- 已将发布执行记录模板包接入 `qms:preimport-package` 命令层：新增 `--release-execution-dir`，命令层 dry-run 显示 `release_execution_status=passed`。带发布执行模板、受控发布演练、字段字典、人工评审包和 `--stage2-check` 的组合 dry-run 仍只因 67 个 pending 人审项处于 `blocked_by_human_review`；带 `--apply --ack-human-reviewed --review-dir --release-execution-dir` 的闸门验证返回 `blocked`，未进入写库事务，也不形成真实运行记录。
- 已新增人审通过模拟包：`human_review_simulation_pack/` 将 67 个人工决策项设为模拟通过并逐行写入 `SIMULATED_APPROVAL_NOT_REAL_REVIEW`；dry-run 验证发现项 0。该包只用于 `--apply-rehearsal`，不代表真实人工评审。
- 已新增 `qms:preimport-package --apply-rehearsal` 非写库演练模式：使用模拟人审包、字段字典、受控发布演练包、发布执行记录模板包和 `--stage2-check` 时返回 `rehearsal_ready`，`stage2.status=ready_after_phase1_apply`，`database_write_performed=0`。同一模拟包用于真实 `--apply` 时返回 `blocked`，阻断项为 `simulated_human_review_pack_not_allowed_for_apply`。
- 已新增 LIMS 第一阶段写库行级预览能力：`qms:preimport-package --write-preview-dir` 可在 dry-run 或 `--apply-rehearsal` 中输出 `lims_write_preview_package/`，预览 `documents`、`record_form_templates`、`qms_sources` 的行级动作，不写数据库；正式 `--apply` 搭配 `--write-preview-dir` 会被拒绝。
- 已通过写库行级预览包 dry-run：65 条 `documents` 预览中 27 条可新增 draft、1 条 `XZTC/SC` 需走既有文件修订/换版路径、37 条 2022 现行程序只做 published 既有文件引用；26 条记录模板均为 draft 预览，4 条外来依据为 upsert 预览，发现项 0。
- 已复核真实待审人工评审包：dry-run 仍显示 `review_pack_pending=67`、`stage2.status=blocked_by_human_review`；真实 `--apply --ack-human-reviewed` 仍返回 `blocked`，阻断项为人工评审包尚未全部通过，未进入写库事务。
- 已新增质量手册修订/换版路径包：`manual_revision_path_pack/` 针对 `XZTC/SC` 生成既有记录核对、9 个修订闸门、5 个 LIMS 动作预览和 5 个人工决策项；dry-run 验证发现项 0。该包把第五版候选手册固定到既有文件修订/换版路径，不允许按同编号新增草稿直接写入。
- 已将质量手册修订/换版路径包接入 `qms:preimport-package` 命令层：新增 `--manual-revision-dir`，命令层 dry-run 显示 `manual_revision_status=passed`、`manual_revision_existing_route=existing_document_revision_required`、`manual_revision_pending_human_decisions=5`。带人工评审包、字段字典、受控发布演练、发布执行模板和 `--stage2-check` 的组合 dry-run 通过；真实 `--apply --ack-human-reviewed` 和真实待审包下的 `--apply-rehearsal --ack-human-reviewed` 均返回 `blocked`，阻断项为 67 项人审 pending 和 5 项手册修订决策 pending，未写数据库。
- 已新增机构人员学习实施包：`staff_training_implementation_pack/` 将受控发布演练中的 29 个培训宣贯源条目拆成 88 个岗位学习任务、12 个学习材料入口、12 道理解确认题、8 行问题反馈模板和 10 份岗位一页卡；dry-run 验证发现项 0。该包用于组织人员学习、理解确认和问题反馈准备，不写库，不代表真实培训完成、人工评审通过或受控发布。
- 已将机构人员学习实施包接入 `qms:preimport-package` 命令层：新增 `--staff-training-dir`，命令层 dry-run 显示 `staff_training_status=passed`，并校验 29 个培训源项、88 个岗位学习任务、12 道理解确认题、8 行反馈模板和 10 份岗位卡。带人工评审包、字段字典、受控发布演练、发布执行模板、质量手册修订路径和 `--stage2-check` 的组合 dry-run 通过；真实 `--apply --ack-human-reviewed` 返回 `blocked`，阻断项为 67 项人审 pending、5 项手册修订决策 pending、88 项学习任务 pending、12 项理解确认 pending 和 8 项反馈回填空白，未写数据库。
- 已新增 LIMS 第二阶段结构化导入行级预览能力：`qms:preimport-package --stage2-preview-dir` 可在 dry-run 或 `--apply-rehearsal` 中输出 `lims_stage2_write_preview_package/`，预览 `qms_structured_documents`、`qms_document_blocks`、`qms_document_block_links` 的行级动作，不写数据库；正式 `--apply` 搭配 `--stage2-preview-dir` 会被拒绝。
- 已通过第二阶段结构化导入行级预览包 dry-run：65 条结构化文件预览、29 条手册块预览、125 条块级链接预览；其中程序文件链接 93 条、附件/表单链接 2 条、记录模板链接 30 条，未解析目标 0，发现项 0。该包把第二阶段影响面提前摊开，不代表第二阶段已导入或允许写库。
- 已新增第二阶段结构化导入人工复核工作台：`stage2_structured_review_workbench/` 将第二阶段预览包整理成 29 条手册块复核矩阵、125 条块级链接复核矩阵、29 条按条款目标统计、64 条目标文件/记录反查清单和 154 条人工复核意见回填模板；dry-run 验证发现项 0。所有复核项仍为 pending/空白，不写库，不代表人工评审通过。
- 已新增第二阶段结构化复核意见回填预览：`stage2_structured_review_decision_preview/` 从 `stage2_structured_review_workbench/05-人工复核意见回填模板.csv` 读取 154 条拟决策并生成回填预览；当前状态为 `no_proposed_decisions`，154 条仍全部阻断，dry-run 验证发现项 0。该预览包不修改复核工作台，不写库，不代表人工评审通过。
- 已将第二阶段人工复核工作台接入 `qms:preimport-package` 命令层：新增 `--stage2-review-dir`，命令层 dry-run 显示 `stage2_review_status=pending`、`stage2_review_pending_decisions=154`，发现项 0；使用模拟人审包执行 `--apply-rehearsal --stage2-check --stage2-review-dir` 时返回 `blocked`，阻断项为 `stage2_review_not_approved`，防止第二阶段块级链接未经人工复核就进入后续导入路径。
- 已将第二阶段复核意见回填预览包接入 `qms:preimport-package` 命令层：新增 `--stage2-review-preview-dir`，命令层 dry-run 显示 `stage2_review_preview_status=passed`、`stage2_review_preview_blocking_items=154`；使用模拟人审包执行 `--apply-rehearsal --stage2-review-preview-dir` 时返回 `blocked`，阻断项为 `stage2_review_preview_has_blocking_items`，防止未通过预览的二阶段意见进入后续导入路径。
- 已新增治理就绪总览包：`governance_readiness_dashboard/` 将候选包结构、人工评审、字段确认、质量手册换版、受控发布、发布执行记录、人员学习、理解确认、问题反馈、第一阶段写库预览、第二阶段复核、二阶段回填预览和用户授权整理成 13 个总闸门、396 条人工处理任务；当前 `ready_for_lims_apply=no`、阻断闸门 12 个、阻断任务 392 条，dry-run 验证发现项 0。
- 已将治理就绪总览包接入 `qms:preimport-package` 命令层：新增 `--governance-readiness-dir`，完整组合 dry-run 显示 `governance_readiness_status=passed`、`governance_readiness_ready_for_lims_apply=no`、`governance_readiness_blocking_tasks=392`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_readiness_has_blocking_tasks` 和 `governance_readiness_not_ready_for_apply`，不写数据库。
- 已新增治理关闭工作台：`governance_closure_workbench/` 将治理总览中的 396 条人工处理任务转成 396 条证据采集项和 396 条拟关闭回填项；当前 `ready_for_governance_readiness_refresh=no`、392 条阻断项仍未关闭、0 条已接受关闭，dry-run 验证发现项 0。
- 已将治理关闭工作台接入 `qms:preimport-package` 命令层：新增 `--governance-closure-dir`，完整组合 dry-run 显示 `governance_closure_status=passed`、`governance_closure_open_blocking_items=392`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_has_open_blocking_items` 和 `governance_closure_not_ready_for_refresh`，不写数据库。
- 已新增治理关闭意见回填预览包：`governance_closure_decision_preview/` 读取治理关闭工作台中的 396 条拟关闭意见，生成 396 条预览行、392 条仍阻断关闭项和按闸门统计；当前 `ready_for_governance_readiness_refresh=no`、0 条已接受预览、392 条仍阻断，dry-run 验证发现项 0。
- 已将治理关闭意见回填预览包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-preview-dir`，完整组合 dry-run 显示 `governance_closure_preview_status=passed`、`governance_closure_preview_blocking_items=392`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_preview_has_blocking_items` 和 `governance_closure_preview_not_ready_for_refresh`，不写数据库。
- 已新增治理就绪刷新预览包：`governance_readiness_refresh_preview/` 读取治理就绪总览和治理关闭意见回填预览，生成 13 条总闸门刷新预览、396 条人工任务刷新预览、392 条仍阻断任务和刷新差异摘要；当前 `ready_for_lims_apply=no`、0 条关闭项被接受、392 条任务仍阻断，dry-run 验证发现项 0。
- 已将治理就绪刷新预览包接入 `qms:preimport-package` 命令层：新增 `--governance-readiness-refresh-dir`，完整组合 dry-run 显示 `governance_readiness_refresh_status=passed`、`governance_readiness_refresh_blocking_tasks=392`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_readiness_refresh_has_blocking_tasks` 和 `governance_readiness_refresh_not_ready_for_apply`，不写数据库。
- 已新增治理闭环执行包：`governance_closure_execution_pack/` 将治理关闭工作台中的 396 条关闭项组织成 60 个执行批次、50 行岗位签核、60 条交接复核和 396 条回填路径；当前 `ready_for_governance_closure_preview=no`，dry-run 验证发现项 0。
- 已将治理闭环执行包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-execution-dir`，完整组合 dry-run 显示 `governance_closure_execution_status=passed`、`pending_signature_rows=50`、`pending_handoff_checks=60`、`pending_route_items=396`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_execution_signatures_pending`、`governance_closure_execution_handoffs_pending`、`governance_closure_execution_routes_pending` 和 `governance_closure_execution_not_ready_for_preview`，不写数据库。
- 已新增治理关闭最小试点包：`governance_closure_pilot_pack/` 从治理闭环执行包抽取 5 个低阻断批次、5 条试点证据填写页和 5 条签核交接页；当前 `ready_for_governance_closure_preview=no`，dry-run 验证发现项 0。
- 已将治理关闭最小试点包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-pilot-dir`，完整组合 dry-run 显示 `governance_closure_pilot_status=passed`、`pending_pilot_evidence=5`、`pending_pilot_handoffs=5`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_pilot_evidence_pending`、`governance_closure_pilot_handoffs_pending` 和 `governance_closure_pilot_not_ready_for_preview`，不写数据库。
- 已新增治理关闭试点回填预览包：`governance_closure_pilot_return_preview/` 将 5 条试点证据映射回治理关闭工作台对应关闭项，生成 5 条映射、10 条拟回填源行预览和 55 条缺字段清单；当前 `ready_for_governance_closure_preview=no`，dry-run 验证发现项 0。
- 已将治理关闭试点回填预览包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-pilot-return-dir`，完整组合 dry-run 显示 `governance_closure_pilot_return_status=passed`、`missing_fields=55`、`blocking_items=5`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_pilot_return_missing_fields`、`governance_closure_pilot_return_has_blocking_items` 和 `governance_closure_pilot_return_not_ready_for_preview`，不写数据库。
- 已新增治理关闭试点源工作台回填补丁预演包：`governance_closure_pilot_source_update_rehearsal/` 将 10 条拟回填源行拆成 55 条逐字段补丁预演，55 条全部阻断，0 条可人工更新候选，`source_workbench_modified=0`，`database_write_performed=0`，dry-run 验证发现项 0。
- 已将治理关闭试点源工作台回填补丁预演包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-pilot-source-update-dir`，完整组合 dry-run 显示 `governance_closure_pilot_source_update_status=passed`、`blocked_patches=55`、`ready_patches=0`、`ready_for_source_workbench_update=no`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_pilot_source_update_has_blocked_patches` 和 `governance_closure_pilot_source_update_not_ready_for_source_update`，不写数据库。
- 已新增治理关闭试点人工执行工作簿：`governance_closure_pilot_operator_workbook/` 将 5 条试点主任务、55 条逐字段填写项、5 条签核交接项和 5 张任务卡整理成可人工执行工作面，`source_workbench_modified=0`，`database_write_performed=0`，dry-run 验证发现项 0。
- 已将治理关闭试点人工执行工作簿接入 `qms:preimport-package` 命令层：新增 `--governance-closure-pilot-operator-workbook-dir`，完整组合 dry-run 显示 `governance_closure_pilot_operator_workbook_status=passed`、`pending_workbook_items=5`、`pending_field_items=55`、`pending_handoff_items=5`、`ready_for_pilot_return_preview=no`；使用模拟人审包执行 apply-rehearsal 时仍返回 `blocked`，阻断项包含 `governance_closure_pilot_operator_workbook_items_pending`、`governance_closure_pilot_operator_workbook_fields_pending`、`governance_closure_pilot_operator_workbook_handoffs_pending` 和 `governance_closure_pilot_operator_workbook_not_ready_for_return_preview`，不写数据库。
- 已新增治理关闭试点真实执行交回包：`governance_closure_pilot_operator_handback/` 用于接收 5 条试点主任务、55 条逐字段交回项和 5 条签核交接项的真实结果，要求真实证据值、执行人、复核人、日期和交接状态，初始均为 pending，`source_workbench_modified=0`，`database_write_performed=0`，dry-run 验证发现项 0。
- 已将治理关闭试点真实执行交回包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-pilot-operator-handback-dir`，完整组合 dry-run 显示 `governance_closure_pilot_operator_handback_status=passed`、`pending_workbook_items=5`、`pending_field_items=55`、`pending_handoff_items=5`、`ready_for_pilot_return_preview=no`；使用模拟人审包执行 apply-rehearsal 和正式 apply 时均返回 `blocked`，阻断项包含真实交回未完成，正式 apply 还会阻断模拟完成包误用，不写数据库。
- 已新增治理关闭试点人工执行模拟完成包：`governance_closure_pilot_operator_completion_simulation/` 将 5 条试点主任务、55 条逐字段填写项和 5 条签核交接项标为模拟完成，65 行均带 `SIMULATED_COMPLETION_NOT_REAL_EXECUTION`，`source_workbench_modified=0`，`database_write_performed=0`，dry-run 验证发现项 0。
- 已将治理关闭试点人工执行模拟完成包接入 `qms:preimport-package` 命令层：新增 `--governance-closure-pilot-operator-completion-simulation-dir`，完整组合 dry-run 显示 `governance_closure_pilot_operator_completion_simulation_status=passed`、`ready_for_pilot_return_preview=yes`、`pending_items=0`、`pending_fields=0`、`pending_handoffs=0`、`marker_rows=65`；使用模拟人审包执行 apply-rehearsal 时，该模拟包可被识别但真实待办仍阻断；正式 `--apply` 会因 `governance_closure_pilot_operator_completion_simulation_not_allowed_for_apply` 返回 `blocked`，不写数据库。
- 已确认当前基线：条款骨架 29/29，现用手册工程包回链 0，程序年份错位 30 项，高风险 4 项、中风险 4 项。
- 用户已确认：已取得 CMA，CNAS 申请中；以 LIMS 当前导出的 2022 程序清单作为现行程序目录；jewelry-qms 目前只作为建设中系统，不写入手册正文，仅写入实施计划。

## 下一步

进入“人工评审候选手册 + 核对试填结果 + 确认 05-02 编号附件/表单归属 + 确认质量手册修订/换版路径 + 确认受控发布治理演练 + 确认机构人员学习实施包 + 确认发布执行记录模板 + 复核第二阶段块级链接并回填预览 + 先用治理关闭试点人工执行工作簿补齐 5 个低阻断批次 + 用真实执行交回包收回真实证据、执行人、复核人、日期和交接结果 + 仅把模拟完成包作为命令链路参考 + 用试点回填预览确认真实结果能安全回到源工作台 + 用源工作台补丁预演确认逐字段回填不会改错源表/源字段 + 按治理总览、治理关闭工作台、治理闭环执行包、治理关闭意见预览和治理就绪刷新预览关闭阻断项”。评审时重点核对组织事实、岗位职责、记录模板是否符合真实运行，以及审批签核、培训宣贯、旧版作废、实施有效性检查、人员理解确认、质量手册同编号修订策略、发布执行记录模板和第二阶段条款-程序-记录链接是否符合真实流程；填写第二阶段复核意见和治理关闭意见后宜先用试点人工执行工作簿验证证据、意见、签核、交接和回填路径是否接得住，再用真实执行交回包验证是否形成真实证据链，模拟完成包只用于对照复跑命令链路，再用试点回填预览确认缺字段与阻断项清零，再用源工作台补丁预演确认逐字段回填不会改错源表/源字段，然后扩展到闭环执行包全量分派与路径复核，导入前不得写生产库。

## 2026-07-08 测试完成态演练结果

- 已按用户授权生成测试完成态：`115-QMS测试完成态生成报告.md/json`。该步骤把交付包内的人审、二阶段复核、治理关闭、试点执行、真实交回、回填预览和治理刷新等 CSV/manifest 标成“用于测试链路验证的完成态”，并保留 `production_record=false`、不写数据库、不修改现用 Word 文件的边界。
- 已复核真实执行交回包：`116-QMS测试完成态真实交回包dry-run验证报告.md/json`，结果 `status=passed`，5 条主任务、55 个字段、5 条签核交接均完成，`database_write_performed=0`。
- 已跑通 LIMS 全量 `apply-rehearsal`：`117-LIMS预导入命令test-completed-apply-rehearsal报告.md/json`，结果 `status=rehearsal_ready`、`findings=0`、`database_write_performed=0`；覆盖 65 个文件候选、26 个记录模板、154 条二阶段复核、396 条治理任务、55 条试点字段、5 条真实交回任务等全链路闸门。
- 已给人员学习实施包增加测试专用豁免：只有 manifest 明确 `test_completion_for_rehearsal=yes` 且 `production_record=false` 时，LIMS 才允许 completed 状态参与非写库演练；普通人员学习包仍按“不得模拟真实培训完成”校验。
- 该结果只说明工程链路和闸门机制能跑通，不等同于真实人工评审、真实培训、真实受控发布或生产写库授权。
