# G3-A 云端上传校验与升级前快照记录 v0.1

记录日期：2026-07-15  
服务器：`101.200.41.200`  
测试域名：`qms.zhj-jc.com`  
执行范围：仅上传 G2 包、服务器侧 SHA 校验、`docker load`、镜像架构检查、升级前快照。  

## 1. 结论

G3-A 通过。

本阶段已完成：

- 创建服务器 staging 目录；
- 上传 G2 代码包、镜像包、SHA256 校验文件；
- 在服务器侧执行 SHA256 校验；
- 在服务器执行 `docker load`；
- 检查新镜像与 MySQL 镜像均为 `linux/amd64`；
- 对当前旧体验环境生成升级前快照；
- 复核 `current` 未切换，旧容器仍在运行。

本阶段未做：

- 未解压新 release 到 `releases/<release-id>`；
- 未切换 `current`；
- 未启动新版本；
- 未改动宝塔、Nginx、HTTPS、BasicAuth；
- 未修改服务器 `.env`。

## 2. 上传位置

```text
/www/server/jewelry-qms-experience/shared/incoming/regmon-20260715-64fdb2a/
```

上传文件：

```text
LIMS-zhj-experience-regmon-20260715-64fdb2a.tar.gz
LIMS-zhj-experience-images-regmon-20260715-64fdb2a.tar.gz
LIMS-zhj-experience-SHA256SUMS.txt
```

服务器侧文件大小：

```text
LIMS-zhj-experience-SHA256SUMS.txt                         241B
LIMS-zhj-experience-images-regmon-20260715-64fdb2a.tar.gz  699M
LIMS-zhj-experience-regmon-20260715-64fdb2a.tar.gz          11M
```

## 3. 服务器侧 SHA256 校验

通过：

```text
LIMS-zhj-experience-regmon-20260715-64fdb2a.tar.gz: OK
LIMS-zhj-experience-images-regmon-20260715-64fdb2a.tar.gz: OK
```

## 4. docker load 与镜像架构检查

加载结果：

```text
Loaded image: jewelry-qms-experience:amd64-64fdb2a
Loaded image: jewelry-qms-mysql:8.4-amd64
```

服务器侧镜像架构：

```text
jewelry-qms-experience:amd64-64fdb2a|amd64|linux|1212066034
jewelry-qms-mysql:8.4-amd64|amd64|linux|812881436
```

说明：服务器 `docker load` 后显示的是单架构镜像在服务器本地的 image id/size，可能不同于本机 buildx 记录；本阶段验收以 tag、`Architecture=amd64`、`Os=linux` 和 SHA 包校验为准。

## 5. 升级前快照

快照目录：

```text
/www/server/jewelry-qms-experience/shared/snapshots/20260715-125232-pre-regmon-64fdb2a/
```

快照内容：

```text
SHA256SUMS.txt
compose_ps.txt
current_release.txt
database.sql.gz
image_inspect.txt
```

快照 SHA 校验通过：

```text
database.sql.gz: OK
current_release.txt: OK
compose_ps.txt: OK
image_inspect.txt: OK
database.sql.gz gzip=OK
```

快照中 `database.sql.gz` 为当前旧体验环境数据库 dump 的压缩文件，保留在服务器，不拉回本机，不写入 Git。

## 6. 未切换确认

执行后 `current` 仍指向旧体验版本：

```text
CURRENT=/www/server/jewelry-qms-experience/releases/20260713-2474de0
```

旧容器仍运行：

```text
jewelry-qms-experience-app-1   jewelry-qms-experience:amd64-2474de0   Up 47 hours   127.0.0.1:18010->8000/tcp
jewelry-qms-experience-db-1    jewelry-qms-mysql:8.4-amd64            Up 47 hours (healthy)
```

## 7. 下一闸门建议

建议下一步进入 **G3-B：服务器解包但仍不切换**。

建议 G3-B 范围：

1. 在服务器创建：
   ```text
   /www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a/
   ```
2. 解压代码包到该 release 目录；
3. 仅检查目录结构、`compose.yaml`、`verify.sh`、`db-init` 权限；
4. 不改 `current`；
5. 不启动新版本；
6. 不改 `.env`。

G3-B 通过后，再单独申请 G4：切换 `current`、启动/升级、运行云端验证。
