# G2-R 本地迁移预演进度卡 v0.1

📇 进度卡 | G2-R 本地迁移预演 | 2026-07-15

## 当前版本

- G2-R 本地迁移预演包：v0.1
- 上位计划：`真实组织受控试运行升级计划-v0.2.md`

## 本次完成

1. 从本地 `jewelry_qms` 源库导出原始快照。
2. 恢复到临时库 `jewelry_qms_g2r_20260715_160955`。
3. 核对 79 张表、16 张关键表计数、11 张关键表校验和。
4. 生成脱敏迁移包：清空会话、替换用户密码、移除联系方式。
5. 删除临时库，完成回滚演练。
6. 生成“一单一库”候选 JSON，运行法规动态技能包校验器并生成复核包。
7. 跑 4 项结构/规则 smoke 和 4 个页面只读 HTTP 检查。

## 下一步

建议下一闸门为：

> 批准 G3-R：云端快照与上传校验；只上传和校验，不导入、不切换、不开放员工登录。

## 等你补齐

1. 是否确认 G3-R 使用本轮脱敏包，还是在 G3-R 前重新生成一次。
2. 灵活轮调授权签字人的具体范围。
3. 刘恒春代理质量负责人/场所质量协调人的回避边界。
4. 现行组织结构图、任命书、CMA 授权签字备案清单是否可作为 G3/G4 证据源。

## 关键决定

1. 原始源库快照只留本机，不上传云端。
2. 云端迁移候选包必须脱敏，且目标服务器重新生成账号密码。
3. 一单一库候选保持人工确认边界，不自动修改能力范围、合同评审、CMA 标志、报告模板、外来文件台账或培训授权。

