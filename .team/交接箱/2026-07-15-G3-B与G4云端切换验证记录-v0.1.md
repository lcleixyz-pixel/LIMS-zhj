# G3-B 与 G4 云端切换验证记录 v0.1

记录日期：2026-07-15  
服务器：`101.200.41.200`  
测试域名：`qms.zhj-jc.com`  
release id：`regmon-20260715-64fdb2a`  
应用镜像：`jewelry-qms-experience:amd64-64fdb2a`

## 1. 结论

G3-B 与 G4 均已通过。

已完成：

- G3-B：服务器解包到新 release 目录；
- G3-B：验证 release 目录结构、关键部署文件、迁移脚本权限；
- G4：备份服务器 `.env`；
- G4：更新 `QMS_IMAGE_TAG=amd64-64fdb2a`；
- G4：切换 `current` 到新 release；
- G4：对旧体验库执行本次升级所需的目标迁移；
- G4：重建并启动体验环境容器；
- G4：运行云端 `verify.sh`；
- G4：运行法规监测 schema 与命令冒烟；
- G4：验证公网入口仍由 HTTPS 与 BasicAuth 保护。

## 2. G3-B 解包验证

新 release 目录：

```text
/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a
```

G3-B 执行时 `current` 尚未切换：

```text
CURRENT_BEFORE=/www/server/jewelry-qms-experience/releases/20260713-2474de0
```

关键文件存在：

```text
jewelry-qms/deploy/experience/compose.yaml
jewelry-qms/deploy/experience/verify.sh
jewelry-qms/deploy/experience/db-init/02-apply-migrations.sh
jewelry-qms/database/migrations/20260714_regulatory_monitor.sql
jewelry-qms/database/migrations/20260704_external_change_events.sql
```

关键权限：

```text
deploy/experience/verify.sh                         -rwx------
deploy/experience/db-init/02-apply-migrations.sh    -rwx------
```

`compose.yaml` 与 `verify.sh` 均已包含法规监测 3 张关键表检查：

```text
qms_external_change_events
qms_external_change_candidates
qms_regulatory_monitor_runs
```

## 3. G4 切换与环境更新

切换前：

```text
OLD_CURRENT=/www/server/jewelry-qms-experience/releases/20260713-2474de0
OLD_TAG=amd64-2474de0
```

切换后：

```text
NEW_CURRENT=/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a
NEW_TAG=amd64-64fdb2a
```

`.env` 备份：

```text
/www/server/jewelry-qms-experience/shared/env-backups/.env.20260715-125713.before-regmon-20260715-64fdb2a
```

说明：`.env` 备份保留在服务器，不拉回本机，不写入 Git，不在聊天中暴露密码。

## 4. 数据库升级迁移

旧体验库不是首次初始化库，因此 MySQL `/docker-entrypoint-initdb.d/` 不会自动补齐新表。G4 只执行本次升级需要的目标迁移，未重放早期带 `DROP TABLE IF EXISTS` 的初始化迁移。

执行目标迁移：

```text
20260704_external_change_events.sql
20260714_qms_activity_responsibility_chain.sql
20260714_regulatory_monitor.sql
```

迁移前法规监测关键表：

```text
1/3
```

迁移后法规监测关键表：

```text
3/3
```

责任链关键表：

```text
6/6
```

## 5. Compose 启动与云端验证

容器已使用新镜像启动：

```text
jewelry-qms-experience-app-1   jewelry-qms-experience:amd64-64fdb2a   Up   127.0.0.1:18010->8000/tcp
jewelry-qms-experience-db-1    jewelry-qms-mysql:8.4-amd64            Up   healthy
```

`verify.sh` 通过：

```text
HTTP 200 http://127.0.0.1:18010/login/index
db health=healthy
regulatory tables=3/3
app 8000/tcp -> 127.0.0.1:18010
db port bindings={}
PASS: experience environment read-only verification completed
```

关键运行环境：

```text
APP_DEBUG=false
REGULATORY_MONITOR_ENABLED=0
CHROMIUM=/usr/bin/chromium-browser
```

命令入口：

```text
clear
check:reminders
qms:monitor-regulatory-changes
```

功能冒烟：

```text
regulatory_schema_smoke passed
regulatory_monitor_command_smoke passed
```

稳定性复查：容器运行约 1 分钟后再次运行 `verify.sh`，结果仍通过。

## 6. 公网入口验证

HTTP 跳转 HTTPS：

```text
http://qms.zhj-jc.com/ -> 301 -> https://qms.zhj-jc.com/
```

未带 BasicAuth 凭据访问 HTTPS 被拦截：

```text
https://qms.zhj-jc.com/             HTTP 401
https://qms.zhj-jc.com/login/index  HTTP 401
www-authenticate: Basic realm="Jewelry QMS Experience"
```

说明：公网入口仍有 BasicAuth 保护。登录页穿透验证需要 BasicAuth 密码，本记录不要求、不保存、不暴露该密码。

## 7. 回滚抓手

旧 release：

```text
/www/server/jewelry-qms-experience/releases/20260713-2474de0
```

G3-A 升级前快照：

```text
/www/server/jewelry-qms-experience/shared/snapshots/20260715-125232-pre-regmon-64fdb2a/
```

`.env` 备份：

```text
/www/server/jewelry-qms-experience/shared/env-backups/.env.20260715-125713.before-regmon-20260715-64fdb2a
```

若后续试用发现严重问题，建议按单独 G5 回滚闸门执行：先停止当前容器，恢复 `.env` 的 `QMS_IMAGE_TAG`，切回旧 `current`，再 `docker compose up -d --pull never` 并运行 `verify.sh`。

## 8. 已知边界

- 当前仍是体验测试环境，不是正式生产。
- `REGULATORY_MONITOR_ENABLED=0`，法规动态月检未在云端自动启用。
- 公网登录页在 BasicAuth 后面；未进行带 BasicAuth 凭据的穿透登录验证，以避免密码进入聊天或记录。
- 数据库已经为本次新模块补齐迁移；未执行破坏性全量迁移。

## 9. 下一步建议

建议进入 **G5：试运行观察**：

1. 由你或指定人员用 BasicAuth 进入测试域名；
2. 检查岗位/权限、手册治理、责任链、法规动态候选清单等页面；
3. 只录入模拟或明确可测试的数据；
4. 收集问题清单；
5. 若需要自动月检，再单独申请启用 `REGULATORY_MONITOR_ENABLED=1` 和定时策略。
