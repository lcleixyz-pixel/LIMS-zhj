# 自治治理与记录模板候选机器验证报告 v0.2

## 1. 结论

本子包机器门禁通过，但 `R2-FILE-003` 仍须独立验证。

| 检查 | 结果 |
|---|---|
| 记录模板候选检查器 | `valid=true`，0 项发现 |
| 记录模板候选回归测试 | 6/6 通过 |
| 自治台账 v0.2 检查器 | `valid=true`，0 项发现 |
| 自治台账 v0.2 回归测试 | 5/5 通过 |

## 2. 先红后绿记录

首次运行记录模板候选测试时，因 v0.2 检查器尚不存在而失败，确认测试能够阻断缺失实现。检查器建立后的第一次真实候选运行检出 86 项问题，主要是：

- 三段式身份检查未读取显式 `object_identity`；
- “暂无可证直接关联”的字段以空列表表示，未形成可审阅说明。

生成器随后显式写入 `object_identity`，并将空关联改为“未识别到可证直接关联；不据此假定无关联”。重新生成后，候选检查器 0 项发现，6 项回归测试全部通过。

自治台账第一次校验还检出 `SRC-TEMPLATE-SERVICE` 活工作树哈希漂移。该文件已被并行的沙箱门禁任务修改，不能用活文件冒充起跑基线。台账因此改为从冻结提交 `2503f15a34d3a0d079429353616d66c3a370eddc` 读取 Git 对象并复算 SHA256，随后台账门禁和 5 项回归测试全部通过。

## 3. 当前机器已证事实

- 12 张重点表，209 个逐字段条目；
- BG-04-03 共 19 个独立变体；
- 19 个变体身份均使用 `doc_number + source_file_sha1 + object_identity`；
- 现用记录源文件 161 个；
- 8013 现用基线模板 145 个，精确 schema 94 种；
- 重复 schema 13 组、64 个成员；
- 8013 当前模板总数 152，其中第二轮专用 SIM 夹具 7 个；
- 现有实例 103 项，均关联 7 项 SIM 夹具；
- 12 项重点现用模板实例为 0；
- 技术参数、单位、计算和限值没有候选值，保持 `evidence_insufficient`；
- `R2-FILE-001/002 = verified_candidate`，依据上一独立验证 `pass`；
- `R2-FILE-003 = draft_candidate`；
- `R2-FIND-007/008 = remediating`，均未关闭；
- 三个候选均未 `sim_applied`，复演均为 `not_run`。

## 4. 未由机器证明的事项

- 逐字段来源的专业判断是否充分；
- 19 个变体的技术方法载体是否完整；
- FILE-003 是否可推进 `verified_candidate`；
- 候选应用后的受影响场景是否通过。

这些事项须由新的独立验证和后续复演完成。

