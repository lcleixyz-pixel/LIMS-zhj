# 自治治理与 SIM 覆盖层机器检查报告 v0.1

> 检查日期：2026-07-19  
> 检查性质：编制者侧结构、哈希和边界检查；不是文件候选的独立合规验证。

## 1. TDD 证据

先建立回归测试，再建立检查器和台账：

- 红：首次运行因 `validate_governance_package.py` 尚不存在而报 `FileNotFoundError`，`FAILED (errors=1)`。
- 绿：实现检查器和候选包后，6 项回归测试全部通过，`Ran 6 tests ... OK`。

回归测试覆盖：

1. 当前真实子包通过；
2. 非法问题状态被拒绝；
3. 非法文件状态被拒绝；
4. 缺少客观依据回链被拒绝；
5. payload 哈希错误被拒绝；
6. 候选冒认正式效力被拒绝。

## 2. 检查器结果

执行：

```text
python3 机器检查/validate_governance_package.py
```

结果：

```json
{
  "valid": true,
  "finding_count": 0,
  "findings": []
}
```

## 3. 额外校验

- 两份 JSON 均通过 `python3 -m json.tool`；
- 本子包通过 `git diff --check`；
- 来源文件和三份 payload 的 SHA256 与台账一致；
- 8 条问题状态均在冻结枚举内；
- 3 份文件候选均保持 `draft_candidate`；
- 8 条发现均有来源、旧值、新值、影响对象、验证和复演回链；
- 7 条候选变更均有变更编号、字段来源和版本关系；
- 纯优化建议未进入治理发现和通过率。

## 4. 尚未执行

机器检查不能替代：

- 8013 当前模板的重新取证；
- 候选内容的独立验证；
- 候选应用到 SIM 覆盖层；
- 受影响场景复演；
- 内审、管理评审或模拟外审。

因此，本报告只证明子包结构和边界通过机器门禁，不给文件候选本身作通过结论。

