# 自治治理状态路由机器验证报告 v0.3

## 结论

治理路由状态推进通过机器检查：

- 新独立验证报告 SHA256 与冻结值一致；
- R2-FILE-003 仅从 `draft_candidate` 推进到 `verified_candidate`；
- R2-FILE-001/002 保持原 `verified_candidate` 记录，内容未改；
- 三份候选载荷及哈希相对 v0.2 未变；
- 三份候选的复演状态均为 `not_run`；
- 没有候选进入 `sim_applied`；
- R2-FIND-007/008 均从 `remediating` 推进到 `verifying`；
- R2-FIND-007/008 均未关闭，明确等待 8013 SIM 应用和受影响场景复演。

检查器结果：

```text
validate_governance_package_v03.py
valid=true
finding_count=0
```

回归测试：

```text
test_validate_governance_package_v03.py
5/5 passed
```

反向测试覆盖提前 `sim_applied`、提前关闭问题、候选哈希改变和擅自修改 FILE-001。

## 边界

本次没有修改三份候选，没有写 8013/8014，没有接触密封受限答案，也没有产生正式批准或发布效力。

