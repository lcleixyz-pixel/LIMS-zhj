# R2-FILE-003 独立验证交接说明 v0.2

## 1. 本次交接对象

本次只交接 `R2-FILE-003`：

- 阅读候选：`候选覆盖层/SIM-记录模板语义覆盖候选-v0.2.md`
- 机器载荷：`候选覆盖层/SIM-记录模板语义覆盖候选-v0.2.json`
- 只读快照：`候选覆盖层/8013记录模板新鲜复算-v0.2.json`
- 状态台账：`自治治理主台账-v0.2.json`

候选当前仍为 `draft_candidate`，验证状态为 `pending_independent_validation`。机器预检不是独立验证结论。

`R2-FILE-001`、`R2-FILE-002` 已由上一独立验证判定 `pass`，治理路由仅将两者推进为 `verified_candidate`；均未 `sim_applied`、未复演。本次不得重复解释为已经实施。

## 2. 冻结输入及哈希

| 输入 | SHA256 |
|---|---|
| `SIM-记录模板语义覆盖候选-v0.2.md` | `371ee52fefbe8683e65868a0032a242ef999fda8d52cc7a53960ec02192b3783` |
| `SIM-记录模板语义覆盖候选-v0.2.json` | `23a466896e8faa0917b907904f679b0009b4a65a83f95fc1db6693588645d7de` |
| `8013记录模板新鲜复算-v0.2.json` | `80cfc3df2b253cec9d35260f31d643048f77df8b1faef71bfeb56be70f1ce55a` |
| `自治治理主台账-v0.2.json` | `a63974ecf5c20d03a0e9d209d2bd082b3ae1cf075442114699ba274ab1a66801` |
| 上一独立验证报告 | `bf07a2e2cba7910840ece56b411389cfce9a449e165daa4585852e1f279832ed` |

旧轮 `记录模板逐字段治理候选-v0.2.md` 只作为素材，不得继承其结论。验证应以本次源文件、冻结事实、程序条款、8013 快照和候选载荷为准。

## 3. 独立验证必须回答的问题

1. 12 张重点表是否全部存在，并确实逐字段记录：
   - 源文件及 SHA256；
   - 来源定位；
   - 程序条款或冻结事实；
   - 业务含义；
   - 填写岗位；
   - 触发与必填规则；
   - 更正规则；
   - 关联对象；
   - 保存依据状态；
   - LIMS 映射；
   - 来源等级及候选处置。
2. 209 个字段是否均能由所列来源支撑；`sim_extension` 是否清楚区分于原表字段。
3. BG-04-03 的 19 个同号变体是否分别具备：
   - `doc_number + source_file_sha1 + object_identity`；
   - 源文件 SHA256；
   - 当前 schema 键；
   - 15 个可证通用字段；
   - `check_method`、`acceptance_criteria`、`measurement_data` 三类证据不足字段。
4. 是否错误地由表格预填值推导了技术参数、单位、重复次数、计算公式或判定限。本候选要求这些内容保持空值和 `evidence_insufficient`。
5. 8013 的 12 项重点基线模板实例数是否确为 0；孤儿键内容是否只形成预览规则，没有伪造迁移结果或数据库写入。
6. 161 个源文件、145 个现用基线模板、94 种精确 schema、13 个重复组、64 个重复成员的复算能否重现。
7. 候选是否保持 SIM-only、非受控、不替换现用文件、不写 8013。

## 4. 允许的独立结论

- `pass`：逐字段、19 变体、身份、哈希和孤儿键预览均可复现，且没有无依据技术值。
- `returned`：存在明确可修正的字段、身份、哈希、来源或状态错误。
- `evidence_insufficient`：核心来源仍不足，无法判定候选保真。

独立验证角色不得修改冻结候选。若结论为 `pass`，也只授权治理路由推进到 `verified_candidate`；应用 SIM 覆盖层和受影响场景复演是后续独立闸门。

## 5. 隔离要求

- 不读取编制者未交付的隐含推理。
- 不把机器检查报告作为独立结论。
- 不写 8013，不覆盖现用 Word，不修改产品代码。
- 不因后续需要复演而降低逐字段证据标准。

