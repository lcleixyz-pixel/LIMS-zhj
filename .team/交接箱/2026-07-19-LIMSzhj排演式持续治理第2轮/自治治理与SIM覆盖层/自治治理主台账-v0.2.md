# LIMS-zhj 第二轮自治治理主台账 v0.2

> 状态：SIM-only 非受控治理记录。FILE-001/002 仅依据独立验证结论推进到 `verified_candidate`；FILE-003 仍为 `draft_candidate`。任何候选均未 `sim_applied`。

## 1. 适用边界

- 演练编号：`SIM-GOV-R2-20260719`
- 不写 8013 数据库，不覆盖现用文件，不修改产品代码。
- 不自证，不形成正式批准、发布或生效。
- 旧轮记录候选只作素材，当前结论来自 161 个源文件、145 个现用模板的新鲜复算。

## 2. 文件候选状态

| 候选 | 类型 | 当前状态 | 独立验证 | 复演 | 说明 |
|---|---|---|---|---|---|
| R2-FILE-001 | manual_overlay | `verified_candidate` | `passed` | `not_run` | 质量手册第二轮SIM差异覆盖 |
| R2-FILE-002 | procedure_and_structure_overlay | `verified_candidate` | `passed` | `not_run` | 程序修订及新增废止结构候选 |
| R2-FILE-003 | record_template_overlay | `draft_candidate` | `pending_independent_validation` | `not_run` | 记录模板语义覆盖候选 |

FILE-001/002 的 `verified_candidate` 只表示独立文件核查通过；尚未应用 SIM 覆盖层，也未完成受影响场景复演。FILE-003 的机器检查通过不等于独立验证通过。

## 3. 问题状态

| 问题 | 分类 | 风险 | 当前状态 | 验证状态 | 候选 |
|---|---|---|---|---|---|
| R2-FIND-001 第四版手册61处程序引用仍指向2018版 | objective_document_gap | high | `remediating` | `pending_independent_validation` | R2-FILE-001 |
| R2-FIND-002 CX-05和CX-31存在8处历史程序引用 | objective_document_gap | medium | `remediating` | `pending_independent_validation` | R2-FILE-002 |
| R2-FIND-003 CX-21批准职责冲突且记录清单错挂 | objective_document_gap | high | `remediating` | `pending_independent_validation` | R2-FILE-001, R2-FILE-002 |
| R2-FIND-004 CX-28正常路径与不留样不分包冻结事实冲突 | objective_applicability_gap | high | `remediating` | `pending_independent_validation` | R2-FILE-001, R2-FILE-002, R2-FILE-003 |
| R2-FIND-005 CX-35导出层污染且正式适用状态证据不足 | objective_export_gap_with_evidence_limit | high | `remediating` | `pending_independent_validation` | R2-FILE-001, R2-FILE-002 |
| R2-FIND-006 方法场所和报告标志需逐项目分流 | objective_control_gap_candidate | high | `remediating` | `pending_independent_validation` | R2-FILE-001, R2-FILE-002, R2-FILE-003 |
| R2-FIND-007 当前模板字段语义保真需在8013重新取证 | evidence_gap | high | `remediating` | `pending_independent_validation` | R2-FILE-003 |
| R2-FIND-008 设备变体和系统验证记录承接方式证据不足 | evidence_gap | medium | `remediating` | `pending_independent_validation` | R2-FILE-003 |

R2-FIND-007/008 已因新鲜复算和 v0.2 逐字段候选进入 `remediating`，但仍不得关闭；须等待新的独立验证。

## 4. FILE-003 新增证据

- 12 张重点表逐字段候选：209 个字段，逐字段记录来源、定位、条款或冻结事实、业务含义、岗位、触发、必填、更正、关联、保存依据状态、LIMS 映射、来源等级。
- BG-04-03：19 个变体分别保留 `doc_number + source_file_sha1 + object_identity` 身份、SHA256、当前 schema 键、可证字段和证据不足字段。
- 技术参数、单位、计算和限值：未建立到方法或作业指导书的字段保持 `evidence_insufficient`，候选值为空。
- 孤儿键：8013 现有重点基线实例为 0，因此只形成迁移预览规则；不伪造迁移结果、不写数据库。

## 5. 独立验证和后续闸门

1. FILE-003 交给新的独立记录模板验证角色，只读检查冻结候选。
2. 通过后才可由治理路由推进 `verified_candidate`。
3. 之后才可应用为 SIM 覆盖层并复演受影响场景。
4. 复演通过前，R2-FIND-007/008 不得转为 `closed`。

机器可验完整台账见 `自治治理主台账-v0.2.json`。
