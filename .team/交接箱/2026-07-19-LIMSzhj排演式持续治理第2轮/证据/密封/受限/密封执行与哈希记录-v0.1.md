# LIMS-zhj 第二轮 8014 密封执行与哈希记录 v0.1

> 访问级别：`restricted / execution_evidence`  
> 截止：2026-07-19  
> 冻结后只读。

## 1. 环境预检

| 项目 | 结果 |
|---|---|
| Compose 项目 | `lims-zhj-rehearsal-r2-blind-20260719` |
| URL | `http://127.0.0.1:8014/login` 返回 HTTP 200 |
| 角色 | `QMS_REHEARSAL_ROLE=blind` |
| 运行编号 | `QMS_REHEARSAL_RUN_ID=SIM-GOV-R2-20260719-BLIND` |
| 最终 app 镜像 | `sha256:b7c4d2e3b5194f465e77ac06fa46daafb7a4d0f470f296d5835d14cc93046388` |
| 数据库 | MySQL 8.4；83 个基础表 |
| SIM 守卫 | 植入前通过；植入后通过 |

8014 首次启动使用的旧 app 镜像已在植入前单独重建为上述最终镜像；数据库、网络和数据卷未重建。

## 2. 红—绿预检

植入前先运行 `密封状态测试.sh`：

```text
[PASS] 83 base tables
[PASS] SIM-only guard
[FAIL] sealed templates installed expected=2 actual=0
PRETEST_EXIT=1
```

这证明测试能够在尚未植入时失败，而不是无条件通过。

密封 SQL 经最终镜像内的 fail-closed SQL 词法/允许清单校验，接受 11 条语句。随后由统一夹具包装器在单一事务中执行：

1. 执行前完整排演身份和 SIM 守卫；
2. 事务内前置 SQL 守卫；
3. 密封夹具；
4. 事务内后置 SQL 守卫；
5. 提交；
6. 提交后 PHP SIM 守卫。

植入后同一测试全部通过，覆盖环境身份、83 表、全 SIM、密封对象存在性及四项客观状态。

## 3. 数据库指纹

| 指纹 | 植入前 | 植入后 |
|---|---|---|
| 结构 + 各表行数 SHA256 | `97ce5ada804db0b19ba1ef4734572b3436974dc1d5e5318cd815aed2d54c3890` | `bf338aeb5669198e630ec95208f565b04aaafcc6013c3c50a823154ec7c9aacc` |
| 全库规范转储 SHA256 | `bacb0dda5f908f591dc46f58f0665ccc495c29190ab32becbd0e7b8a71c9a17e` | `e2485ae4e6b8c39d2b8fc7a89187b2c7d3ed54d38c57861ed142c1df70f3a24c` |
| 密封 ID 对象数 | `0` | 按密封答案列出的冻结对象集合 |
| 基础表数 | `83` | `83` |

全库规范转储命令使用 `mysqldump --single-transaction --skip-comments --skip-dump-date --hex-blob --order-by-primary`，只计算 SHA256，不保存数据库正文。

## 4. 植入后抽样计数

植入后数据库总计数用于辅助确认变化范围：

```text
record_form_templates=147
record_form_instances=4
employees=2
users=2
employee_appointments=1
approvals=1
histories=4
```

这些不是密封检出提示；密封分母和逐项答案仅见 `密封答案与回退说明-v0.1.md`。

## 5. 冻结声明

在植入后测试、全 SIM 守卫、HTTP 可用性和哈希固定完成后，8014 进入 `sealed_ready`。密封设计角色自此不再修改 8014。
