# LIMS-zhj 第二轮 8014 一次性密封盲评独立检出报告 v0.1

> 状态：`frozen_detection_report`  
> 检出角色：第二轮独立盲检角色  
> 环境：`http://127.0.0.1:8014`  
> 排演编号：`SIM-GOV-R2-20260719-BLIND`  
> 检查日期：2026-07-19  
> 结论边界：仅评价 8014 的公开 SIM 证据；不得作为正式 CMA/CNAS 评审结论或真实运行证据。

## 1. 独立性声明

本次检查只使用：

- `盲测环境公开说明-v0.1.md`；
- `依据与机构画像冻结包-v0.1.md`；
- `全要素排演考卷与适用性冻结-v0.1.md`；
- `机构画像冻结-SIM-GOV-R2-20260719-v0.1.json`；
- 8014 页面可用性、容器公开环境身份和 MySQL 公开证据。

未读取 8013 治理候选、整改推理或外审结论；未读取密封设计信息；未搜索设计者会话、命令历史或临时文件；未修改 8014 的数据库、文件、容器、卷或网络；未进行整改或检出率对账。

## 2. 环境与证据冻结

| 项目 | 只读核验结果 |
|---|---|
| 试运行模式 | `QMS_TRIAL_MODE=true` |
| 环境角色 | `QMS_REHEARSAL_ROLE=blind` |
| 运行编号 | `SIM-GOV-R2-20260719-BLIND` |
| 环境标签 | `8014 第二轮一次性密封盲测` |
| 登录页 | `GET /login` 返回 HTTP 200 |
| 数据库 | MySQL；数据库名 `jewelry_qms` |
| 全库只读快照流 SHA256 | `3edf81a2f15b45e93e712650aa05875f016c9068e127b3f0708ec6ad37367d69` |
| 数据库结构流 SHA256 | `8e9ff6cb1eb9f0367355b74ebe94024cbf7130686cb5875c4e5e0eec320a81d2` |

说明：两个数据库哈希由只读 `mysqldump` 流计算，没有在容器或卷内落盘。

## 3. 盲检结论

本次独立盲检形成 7 项客观发现：

- 3 项重大高风险：错误使用 CNAS 认可标识/声明、库外 DB65 项目错误使用 CMA 标志/声明、超有效期且超场所与方法范围的人员实际批准报告；
- 4 项高风险：技术记录语义不足、和田场所主数据缺失且报告使用自由文本场所、外部依据台账未承接冻结基线、报告编审签发证据链与模板要素不完整。

以上发现均来源于公开判据与 8014 当前数据，不以密封问题数量、位置或类型为前提。当前存在开放重大/高风险问题，因此 8014 盲测样本按冻结考卷应判为：

> **不通过；阻断该样本进入 `verified_candidate` 或 `sim_applied`。**

## 4. 发现明细

### BLIND-R2-F-001：CNAS 初次申请未提交状态下，已签发记录使用 CNAS 标识和“认可范围内”声明

| 项目 | 内容 |
|---|---|
| 严重性 | 重大 / 高风险 |
| 分类 | 运行问题 + LIMS 标志控制问题 |
| 阻断 | 是 |
| 适用判据 | `CNAS-05`、`NEG-06`、`EX-7.8.1`、`EX-7.8.2`；冻结画像规定 CNAS 为初次申请筹备、未提交、未受理、未认可，`cnas_mark_allowed=false` |
| 客观现象 | 已锁定并留有 `issue` 历史的记录 `SIM-BLIND-RPT-CNAS-001` 同时写入 `cnas_state=initial_application_not_submitted`、`cnas_mark=true`、`cnas_statement=CNAS认可范围内` |
| 影响范围 | 对外状态声明、报告签发、认可标识合规性、客户误导风险 |
| 建议去向 | 立项为运行与软件双线问题；冻结该报告；报告/模板候选删除不实标识；LIMS 增加“未认可时写入前拒绝”及审计；独立复验负向攻击 |

复现查询：

```sql
SELECT
  id,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.cnas_state')) AS cnas_state,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.cnas_mark')) AS cnas_mark,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.cnas_statement')) AS cnas_statement,
  status
FROM record_form_instances
WHERE id = 'SIM-BLIND-RPT-CNAS-001';
```

冻结结果：

```text
SIM-BLIND-RPT-CNAS-001 | initial_application_not_submitted | true | CNAS认可范围内 | locked
```

审计查询同时显示 `SIM-BLIND-HISTORY-CNAS-001` 的动作为 `issue`，事件为 `report_issued`，并非仅有未提交草稿。

### BLIND-R2-F-002：库外 DB65/T 4828-2024 项目在已签发记录中错误使用 CMA 标志和范围声明

| 项目 | 内容 |
|---|---|
| 严重性 | 重大 / 高风险 |
| 分类 | 运行问题 + LIMS 一单一库分流问题 |
| 阻断 | 是 |
| 适用判据 | `CMA-05`、`CMA-06`、`NEG-07`；冻结事实规定三项 DB65 均为库外、`CMA=false` |
| 客观现象 | 已锁定并留有 `issue` 历史的记录 `SIM-BLIND-RPT-DB65-001` 写明 `method_standard=DB65/T 4828-2024`、`one_list_status=out_of_list`，却同时写入 `cma_mark=true` 和 `cma_statement=检验检测机构资质认定范围内` |
| 影响范围 | CMA 标志使用、一单一库分流、合同/报告一致性、客户误导风险 |
| 建议去向 | 冻结该报告；体系候选明确库外项目报告控制；LIMS 建立项目级而非报告级标志判定，并在写入前拒绝库外 `CMA=true`；独立复验三项 DB65 |

复现查询：

```sql
SELECT
  id,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.method_standard')) AS method,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.one_list_status')) AS list_status,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.cma_mark')) AS cma_mark,
  JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.cma_statement')) AS cma_statement,
  status
FROM record_form_instances
WHERE id = 'SIM-BLIND-RPT-DB65-001';
```

冻结结果：

```text
SIM-BLIND-RPT-DB65-001 | DB65/T 4828-2024 | out_of_list | true | 检验检测机构资质认定范围内 | locked
```

### BLIND-R2-F-003：授权已超期且超场所、超方法范围，系统仍允许同一人员批准并签发报告

| 项目 | 内容 |
|---|---|
| 严重性 | 重大 / 高风险 |
| 分类 | 人员授权运行问题 + LIMS 动作授权问题 |
| 阻断 | 是 |
| 适用判据 | `EX-6.2`、`EX-7.8.1`、`CMA-02`；人员授权须同时匹配场所、方法、动作和有效期 |
| 客观现象 | `SIM-BLIND-EMP-SIGNER-001` 的任命范围仅为乌鲁木齐、`GB/T 16552-2017`、动作 `approve/issue`，有效期止于 2025-12-31；截至冻结日 2026-07-19 已过期，但状态仍为 `active`。该人员实际批准了和田场所、`GB/T 38821-2020` 的报告 `SIM-BLIND-RPT-AUTH-001` |
| 影响范围 | 人员资格、授权签字、场所和方法范围、报告有效性、系统权限绕过 |
| 建议去向 | 冻结签发；运行线纠正任命状态与有效期；软件线把用户粗粒度 `approve/issue` 权限与有效任命的场所、方法、动作、日期同时校验；对过期、错场所、错方法分别做写入前反测 |

复现查询：

```sql
SELECT
  r.id,
  JSON_UNQUOTE(JSON_EXTRACT(r.field_values, '$.site')) AS report_site,
  JSON_UNQUOTE(JSON_EXTRACT(r.field_values, '$.method_standard')) AS report_method,
  a.appointment_scope,
  a.valid_until,
  a.status,
  u.user_access
FROM record_form_instances r
LEFT JOIN employee_appointments a
  ON a.employee_id =
     JSON_UNQUOTE(JSON_EXTRACT(r.field_values, '$.authorized_signatory'))
LEFT JOIN users u ON u.employee_id = a.employee_id
WHERE r.id = 'SIM-BLIND-RPT-AUTH-001';
```

冻结结果：

```text
报告场所：和田
报告方法：GB/T 38821-2020
任命范围：{"site":"乌鲁木齐","methods":["GB/T 16552-2017"],"actions":["approve","issue"]}
有效期止：2025-12-31
任命状态：active
用户权限：{"reports":["read","approve","issue"]}
```

`approvals` 表另有 `SIM-BLIND-APPROVAL-AUTH-001`，状态 `approved`、审批级别 3、说明“SIM报告批准并签发”；`histories` 表另有对应 `approve/report_authorized` 事件，证明越权已形成结果，而不是仅有潜在配置风险。

### BLIND-R2-F-004：已锁定技术记录只有“检测结论”，不能重现检测活动

| 项目 | 内容 |
|---|---|
| 严重性 | 高风险 |
| 分类 | 记录模板问题 + 运行记录问题 |
| 阻断 | 是 |
| 适用判据 | `EX-7.5`、文件与 LIMS 追溯考卷；技术记录须支持原始观察、结果、人员、时间、更正及活动重现 |
| 客观现象 | `SIM-BLIND-TPL-RAW-001` 的字段 schema 只有一个必填字段 `conclusion`；实例 `SIM-BLIND-RAW-SEM-001` 也只有 `{"conclusion":"符合委托要求"}`，却已为 `locked` |
| 缺失的最低追溯信息 | 样品、合同/任务、场所、方法及版本、原始观察/读数、设备、操作人员、检测时间、计算或判定依据、复核人员与时间、修改前后值 |
| 影响范围 | 技术活动可重现性、结果复核、设备/人员/方法回链、报告支持证据 |
| 建议去向 | 文件治理线按合法程序与方法正文逐字段形成候选；没有方法正文支持的技术参数不得补造；软件线阻断字段不足的技术记录锁定；独立验证后只在 SIM 覆盖层应用 |

复现查询：

```sql
SELECT
  id,
  JSON_LENGTH(template_field_schema) AS schema_field_count,
  JSON_KEYS(field_values) AS actual_keys,
  status,
  created_by,
  modified_by
FROM record_form_instances
WHERE id = 'SIM-BLIND-RAW-SEM-001';
```

冻结结果：

```text
SIM-BLIND-RAW-SEM-001 | 1 | ["conclusion"] | locked |
SIM-BLIND-USER-SIGNER-001 | SIM-BLIND-USER-SIGNER-001
```

### BLIND-R2-F-005：和田场所没有受控主数据，报告使用无法校验的自由文本场所

| 项目 | 内容 |
|---|---|
| 严重性 | 高风险 |
| 分类 | 机构画像映射问题 + LIMS 数据关系问题 |
| 阻断 | 是 |
| 适用判据 | `EX-5.3`、`EX-5.4`、`EX-5.5`、`CNAS-03`；冻结画像规定乌鲁木齐、和田均为正式场所 |
| 客观现象 | `sites` 仅有 `MAIN=SIM-乌鲁木齐实验室` 一条启用记录；报告模板的 `site` 是普通 `text` 字段；至少两个已锁定报告写入自由文本“和田”，无法回链和田场所主数据 |
| 影响范围 | 场所范围、资源匹配、人员授权、报告地点、跨场所追溯 |
| 建议去向 | 建立和田 SIM 场所主数据；记录/报告使用场所标识外键或受控枚举；合同、样品、人员、设备、环境、方法、报告按同一场所标识校验；复演两场所链 |

复现查询：

```sql
SELECT code, name, status
FROM sites
WHERE publish = 1 AND soft_delete = 0;

SELECT id, JSON_UNQUOTE(JSON_EXTRACT(field_values, '$.site')) AS report_site
FROM record_form_instances
WHERE id IN ('SIM-BLIND-RPT-AUTH-001', 'SIM-BLIND-RPT-DB65-001');
```

冻结结果：

```text
MAIN | SIM-乌鲁木齐实验室 | active
SIM-BLIND-RPT-AUTH-001 | 和田
SIM-BLIND-RPT-DB65-001 | 和田
```

### BLIND-R2-F-006：LIMS 外部依据台账未承接冻结依据基线，且已登记来源全部处于查新到期状态

| 项目 | 内容 |
|---|---|
| 严重性 | 高风险 |
| 分类 | 依据映射问题 + 文件结构问题 |
| 阻断 | 是，阻断依据—文件—记录—报告完整回链；不否定冻结包外部文件本身的存在 |
| 适用判据 | 阶段 A 冻结要求、`EX-7.2.1`、`EX-8.2`、`EX-8.3`、文件与 LIMS 追溯考卷 |
| 客观现象 | `qms_sources` 只有 5 项：GB/T 27025、2023 年第 21 号准则、CNAS-CL01、G001、A015；未登记 2026 年第 14 号“一单一库”、CNAS-RL01、五项国标和三项 DB65；5 项来源的 `freshness_status` 均为 `due`，但 `status` 均为 `published`；2023 年第 21 号来源代码显示为问号化标识，不能稳定引用 |
| 影响范围 | 依据身份、有效性、适用性、方法版本、标志判定及字段溯源 |
| 建议去向 | 文件治理线按冻结依据包补齐来源身份、版本、官方链接、适用性和查新状态；保留 `user_confirmed_for_sim` 证据等级；软件线阻断 `due/unknown` 来源无评审继续驱动方法/标志；不得复制标准全文进入 Git |

复现查询：

```sql
SELECT
  source_code,
  version,
  freshness_checked_at,
  freshness_status,
  status
FROM qms_sources
ORDER BY source_code;
```

冻结汇总：

```text
来源数：5
freshness_status=due：5
status=published：5
冻结基线要求但未在 qms_sources 登记：
2026年第14号一单一库、CNAS-RL01、五项国标、三项DB65
```

### BLIND-R2-F-007：报告模板与签发链不能证明编制—复核—授权后发布及报告最低要素

| 项目 | 内容 |
|---|---|
| 严重性 | 高风险 |
| 分类 | 报告模板问题 + 运行证据问题 |
| 阻断 | 是 |
| 适用判据 | `EX-7.8.1`、`EX-7.8.2`、文件与 LIMS 追溯考卷 |
| 客观现象 | 报告模板 `SIM-BLIND-TPL-RPT-001` 只有报告编号、样品编号、场所、方法、CMA 标志、CNAS 标志、授权签字人 7 个字段；缺少实验室识别、客户、检测日期、报告签发日期、结果、复核人/复核时间等关键要素。三个报告实例均为 `locked`；其中只有授权范围测试报告在 `approvals` 有一条批准记录，CNAS 和 DB65 两条只有 `issue` 历史，没有对应编制、复核、批准证据 |
| 影响范围 | 报告完整性、技术结果承接、复核独立性、签发顺序、责任追溯 |
| 建议去向 | 依据程序与 7.8 条款逐字段治理模板；建立 `draft → reviewed → authorized → issued` 状态机和不同责任人的事件；不满足字段或授权链时拒绝锁定/签发；独立验证正反路径 |

复现查询：

```sql
SELECT id, doc_number, field_schema
FROM record_form_templates
WHERE id = 'SIM-BLIND-TPL-RPT-001';

SELECT id, record, user_id, approval_level, status, approved_on
FROM approvals
ORDER BY id;

SELECT id, record_id, action, user_id, details, created
FROM histories
WHERE record_id LIKE 'SIM-BLIND-RPT-%'
ORDER BY created;
```

冻结结果摘要：

```text
报告模板字段数：7
报告实例数：3，locked：3
审批记录数：1，仅关联 SIM-BLIND-RPT-AUTH-001
SIM-BLIND-RPT-CNAS-001：issue 历史，无审批记录
SIM-BLIND-RPT-DB65-001：issue 历史，无审批记录
```

## 5. 未形成不符合的检查覆盖

| 检查项 | 范围与结果 | 结论边界 |
|---|---|---|
| 环境身份 | 试运行、运行编号、环境标签、`blind` 角色均与公开说明一致 | 通过 |
| 页面可用性 | `/login` HTTP 200 | 仅证明入口可用，不证明登录后全部页面 |
| 全 SIM 人员 | `employees` 中非 SIM 编号或名称计数为 0 | 通过当前数据抽查 |
| 全 SIM 用户 | `users` 中非 SIM 用户名或名称计数为 0 | 通过当前数据抽查 |
| 全 SIM 设备 | `equipments` 记录数为 0，非 SIM 计数为 0 | 无设备数据可评价设备生命周期 |
| 全 SIM 运行记录 | 4 条 `record_form_instances` 均为 `is_simulation=1` 且批次为本次盲测 | 通过当前记录边界 |
| 不开展活动零写入 | 4 条运行记录中未出现抽样、留样、分包、钻石分级或 GB/T 44914 | 只证明当前记录零写入；只读盲检未做写入攻击，不能证明拒绝机制 |
| 文件结构数量 | 结构化文件 216：外部依据 5、手册 1、程序 38、作业指导书 27、记录表 145；216 项均有结构块 | 只证明结构化映射存在，不等同于语义符合 |
| 角色主数据 | 12 个岗位代码存在 | 任命只有 1 条，且已发现有效期/范围问题；无法证明质量负责人、技术负责人、实验室主任和两场所负责人的本轮任命 |
| 设备、环境、培训、供应商、质控、投诉、CAPA、内审和管评 | 对应运行表无本次盲测数据 | 记为“本盲测样本证据不足”，不据此虚构符合或不符合；应由主演练全要素证据评价 |

## 6. 分流与后续验证要求

| 发现 | 体系/文件治理 | 软件清单 | 运行纠正 | 独立复验最低要求 |
|---|---:|---:|---:|---|
| F-001 CNAS 误标 | 是 | 是 | 是 | 未认可状态下模板、录入、签发、导出均拒绝 CNAS 标识/声明，数据库零误写 |
| F-002 DB65 CMA 误标 | 是 | 是 | 是 | 三项 DB65 分别攻击 `CMA=true`，均写入前拒绝并留审计 |
| F-003 授权越界 | 是 | 是 | 是 | 过期、错场所、错方法、无授权四条反测均拒绝；正确授权正向通过 |
| F-004 技术记录语义 | 是 | 是 | 是 | 字段逐项有据，缺关键字段不能锁定；记录可回链并重现活动 |
| F-005 场所主数据 | 是 | 是 | 是 | 两场所均有受控标识；自由文本不能绕过场所与资源校验 |
| F-006 依据台账 | 是 | 是 | 是 | 冻结核心依据 100% 有来源、有效性、适用性状态，`due/unknown` 有明确处置 |
| F-007 报告与状态链 | 是 | 是 | 是 | 报告最低要素齐全；编制、复核、授权、签发顺序正确且责任可追溯 |

本报告只提交发现和验证尺度，不执行整改，不判断整改候选是否通过。

## 7. 冻结声明

本报告在写入正文 SHA256 后冻结。冻结后盲检角色不再修改本文件，也不参与后续整改、验证或检出率计算。

**冻结正文 SHA256（不含本哈希行）：** `590a234d23974f83ce92981d4095929ade6200fcf7c13e48f2ed2ad290751c51`
