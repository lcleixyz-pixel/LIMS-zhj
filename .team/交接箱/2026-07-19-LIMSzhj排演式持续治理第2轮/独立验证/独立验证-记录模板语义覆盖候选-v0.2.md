# 独立验证：记录模板语义覆盖候选 v0.2

> 验证角色：新的独立记录模板验证角色  
> 验证日期：2026-07-19  
> 演练编号：`SIM-GOV-R2-20260719`  
> 验证对象：`R2-FILE-003`  
> 验证方式：只读候选、源表、程序、冻结快照及 8013 当前数据；未修改候选、台账、现用文件、产品代码或 8013/8014 数据；未读取密封受限答案  
> 独立结论：**PASS**  
> 状态授权：**可以由治理路由把 `R2-FILE-003` 从 `draft_candidate` 推进到 `verified_candidate`；不得直接推进到 `sim_applied`。**

## 1. 验证边界

本结论只回答：

1. 12 张重点记录表的 209 个候选字段是否具有可核来源和业务语义；
2. `XZTC/BG-04-03` 的 19 个同号变体是否具有稳定身份、哈希和技术值阻断边界；
3. 旧实例为零时，孤儿键处理是否只是迁移预览而非伪造迁移结果；
4. 上一轮对 `R2-FILE-003` 的退回原因是否已经关闭。

本结论不表示：

- 145 张现用基线模板已经全部完成逐字段语义治理；
- 候选已经写入 8013、替换现用文件或正式发布；
- 受影响业务场景已经复演；
- `R2-FIND-007`、`R2-FIND-008` 已经关闭；
- 机构已经取得 CMA/CNAS 评审结论。

## 2. 冻结输入与完整性

交接说明登记的五项核心哈希经重新计算均一致：

| 输入 | 独立复算 SHA256 | 结果 |
|---|---|---|
| `SIM-记录模板语义覆盖候选-v0.2.md` | `371ee52fefbe8683e65868a0032a242ef999fda8d52cc7a53960ec02192b3783` | 一致 |
| `SIM-记录模板语义覆盖候选-v0.2.json` | `23a466896e8faa0917b907904f679b0009b4a65a83f95fc1db6693588645d7de` | 一致 |
| `8013记录模板新鲜复算-v0.2.json` | `80cfc3df2b253cec9d35260f31d643048f77df8b1faef71bfeb56be70f1ce55a` | 一致 |
| `自治治理主台账-v0.2.json` | `a63974ecf5c20d03a0e9d209d2bd082b3ae1cf075442114699ba274ab1a66801` | 一致 |
| 上一独立验证报告 | `bf07a2e2cba7910840ece56b411389cfce9a449e165daa4585852e1f279832ed` | 一致 |

候选 JSON 内嵌的 `fresh_recalculation` 与独立快照 JSON 规范化后逐字一致。候选保持：

```text
file_state=draft_candidate
verification.status=pending_independent_validation
replay.status=not_run
8013_database_write=false
current_file_write=false
product_code_write=false
```

提交验证时没有 `sim_applied` 状态或已应用声明。

## 3. 新鲜复算

### 3.1 文件和 8013 统计

独立复算结果：

| 项目 | 候选值 | 独立复算 | 结果 |
|---|---:|---:|---|
| 记录源文件 | 161 | 161 | 一致 |
| `.doc` / `.docm` / `.docx` | 113 / 1 / 47 | 113 / 1 / 47 | 一致 |
| 8013 非删除模板 | 152 | 152 | 一致 |
| 现用基线模板 | 145 | 145 | 一致 |
| 第二轮 SIM 夹具模板 | 7 | 7 | 一致 |
| 基线精确 schema | 94 | 94 | 一致 |
| 重复 schema 组 | 13 | 13 | 一致 |
| 重复 schema 成员 | 64 | 64 | 一致 |
| 全部实例 | 103 | 103 | 一致 |
| 基线模板实例 | 0 | 0 | 一致 |
| 12 项重点模板实例 | 0 | 0 | 一致 |
| 第二轮 SIM 夹具实例 | 103 | 103 | 一致 |

8013 复核只执行 `SELECT`/结构读取，没有 DML 或 DDL。103 项实例全部关联第二轮专用 SIM 夹具，候选没有把它们冒充为 145 项现用模板的保真证据。

### 3.2 源文件身份

12 张重点表与 19 个 `BG-04-03` 变体共有 30 个不重复源文件，其中通用 `BG-04-03` 同时属于两组。独立重新计算结果：

- 30/30 个源文件 SHA1 与候选一致；
- 30/30 个源文件 SHA256 与候选一致；
- 12/12 张重点表的 `textutil` 文本 SHA256 与候选一致；
- 19/19 个变体的 `textutil` 文本 SHA256 与候选一致；
- 12/12 个重点模板的 `doc_number + source_file_sha1 + schema_hash_prefix` 均与冻结快照及 8013 当前基线相符。

## 4. 12 张表与 209 个字段

### 4.1 数量和机器结构

| 检查 | 结果 |
|---|---|
| 重点表数量 | 12/12 |
| Markdown 字段行 | 209 |
| JSON 字段对象 | 209 |
| 每张表内字段键唯一 | 12/12 |
| 字段源文件和 SHA256 与所属记录一致 | 209/209 |
| 来源定位非空 | 209/209 |
| 程序条款或冻结事实非空 | 209/209 |
| 业务含义非空 | 209/209 |
| 填写岗位非空 | 209/209 |
| 触发规则非空 | 209/209 |
| 必填规则非空 | 209/209 |
| 更正规则非空 | 209/209 |
| 关联说明存在 | 209/209 |
| 保存依据状态非空 | 209/209 |
| 来源等级和候选处置非空 | 209/209 |

字段来源分层为：

| 来源等级 | 数量 | 验证结果 |
|---|---:|---|
| `direct_source` | 164 | 与源表栏目、评价项、签核区或正文区域相符 |
| `cross_source` | 11 | 源表字段与程序/冻结事实交叉承接，未冒充原表独立字段 |
| `sim_extension` | 23 | 均明确标为原表无独立栏、冻结扩展、程序承接或 SIM 治理元数据 |
| `evidence_insufficient` | 11 | 均未映射、未给候选值，并保持阻断/排除 |

候选没有把 `sim_extension` 写成原表字段。对 12 份源表逐份回看后，164 个直接来源字段对应的栏目、评价项和签核区域均可识别；11 个交叉来源字段没有越过其程序或冻结事实边界。

### 4.2 重点语义边界

- 合同评审增加的场所、CMA 范围、CNAS 状态和报告标志均标为第二轮冻结扩展，不改写原表身份。
- 样品台账把“不抽样、不留样”作为本轮 SIM 适用性控制，没有生成抽样或留样记录。
- `BG-19-01` 的电子记录集、检索测试和追加式修改规则没有被塞入原表字段，保持 `evidence_insufficient/exclude_pending_carrier`。
- `BG-26-01` 的系统版本、适用性验证、备份、恢复测试和系统限制没有被塞入软件登记表备注，保持 `evidence_insufficient/exclude_pending_carrier`。
- `BG-03-01` 的责任人、稳定性和分辨率被清楚标为程序承接扩展；没有生成设备技术值。
- 保存期限只登记现有一般规则和待复核状态，没有为各表编造具体期限。

## 5. `BG-04-03` 十九个变体

| 检查 | 结果 |
|---|---|
| 变体数量 | 19/19 |
| 唯一 SHA1 | 19/19 |
| 唯一三段式身份 | 19/19 |
| 身份公式 | 全部严格等于 `doc_number + source_file_sha1 + object_identity` |
| 对象身份边界 | 19/19 均标为源表身份，不冒充 SIM 设备 |
| 与 8013 当前 schema 对应 | 19/19 |
| 共同可证字段 | 每个变体均为 15 项 |
| 证据不足字段 | 每个变体均仅为 `check_method`、`acceptance_criteria`、`measurement_data` |
| 技术值授权 | 19/19 均为 `false` |

独立从 19 份源表抽取文本并去除空白后，名称、型号规格、编号、核查依据、核查资源、核查人员、过程记录、记录人、结果判定、审核意见、签名和日期等共同标签均可复现。

变体原表中的预填对象名称、数字或专属表格形态只用于识别源表。候选没有据此推导或填入：

- 具体核查方法；
- 判定标准或限值；
- 测量单位；
- 重复次数；
- 计算公式；
- 偏差或合格判定规则。

209 个字段的 `proposed_value` 均为空；11 个 `evidence_insufficient` 字段的 LIMS 映射均为空。未发现“为了闭环而编造”的技术值。

## 6. 孤儿键迁移预览

候选符合零实例边界：

```text
preview_only=true
database_write_performed=false
fresh_8013_baseline_instance_count=0
```

12 张表均有预览对象，共 49 条规则：

- 49/49 均为 `write_allowed=false`；
- 未知键规定保留原值并标记 `orphan_pending_review`；
- 类型不一致不得自动强转；
- 数组/子表不得在粒度未确认时自动迁移；
- 锁定实例不得原地覆盖；
- 同号变体必须先按三段式身份选定；
- 只有 1 条规则提出“类型兼容时可直接映射”，但仍为 `write_allowed=false`。

因此这里是可审计的迁移设计，不是已经执行的迁移结果。

## 7. 上一轮退回项关闭情况

上一独立验证对 `R2-FILE-003` 的四项退回理由，本轮验证如下：

| 上一轮缺口 | v0.2 证据 | 结论 |
|---|---|---|
| 12 表只有方向，没有逐字段候选 | 12 表、209 字段，人读和机器载荷一致 | 已关闭 |
| `BG-04-03` 19 变体未逐一核源 | 19 个独立 SHA、文本哈希、身份、schema 和字段边界 | 已关闭 |
| 同号变体身份不稳定 | 三段式身份 19/19 唯一并可重算 | 已关闭 |
| 孤儿键迁移预览缺失 | 重点基线实例为 0，形成 12 份只读预览，全部禁止写入 | 已关闭 |

`BG-19-01`、`BG-26-01` 和 `BG-03-01` 的扩展边界也已按上一轮要求区分为原表字段、程序/冻结扩展和证据不足字段。

## 8. 机器检查与独立反向检查

候选自带检查器新鲜运行：

```text
validate_record_template_candidate_v02.py
valid=true
finding_count=0

test_validate_record_template_candidate_v02.py
6/6 passed

validate_governance_package_v02.py
valid=true
finding_count=0

test_validate_governance_package_v02.py
5/5 passed
```

机器检查不是本结论的替代。本独立验证另行完成了源文件二进制哈希、文本哈希、字段属性、身份公式、8013 只读统计、技术值空载、迁移禁止写入和状态越权检查。

## 9. 结论与后续闸门

独立结论：

```text
PASS
R2-FILE-003: eligible_for_verified_candidate=true
R2-FILE-003: eligible_for_sim_applied=false
```

允许治理路由执行的唯一状态推进是：

```text
draft_candidate -> verified_candidate
```

后续仍必须依次完成：

1. 由治理路由登记本独立报告及其 SHA256；
2. 在新冻结的 8013 基线上应用为仅供 SIM 的覆盖层；
3. 验证应用过程没有写入现用文件或正式发布状态；
4. 复演 `EX-7.1`、`EX-7.4`、`EX-7.5`、`EX-7.8`、`EX-8.4` 及相关反向场景；
5. 复演通过后，才可讨论 `sim_applied` 和 `R2-FIND-007/008` 的关闭。

若应用或复演时出现字段丢失、变体错配、孤儿键覆盖、技术值自动填充或状态越权，本 PASS 应被重开。
