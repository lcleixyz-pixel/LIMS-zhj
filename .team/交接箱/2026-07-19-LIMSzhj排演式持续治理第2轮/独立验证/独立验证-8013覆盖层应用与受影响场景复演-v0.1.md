# 独立验证：8013 覆盖层应用与受影响场景复演 v0.1

> 验证角色：新独立复演验证角色  
> 验证环境：8013 主演练环境，只读  
> 验证日期：2026-07-20  
> 演练编号：`SIM-GOV-R2-20260719`  
> 总结论：**PASS**  
> 正式效力：无；本报告只授权 SIM 治理状态路由，不构成真人批准、受控发布、正式生效、CMA 能力或 CNAS 认可结论

## 1. 结论与授权

本次独立反向核验通过。允许治理路由在不改候选正文、不覆盖现用文件、不写 8014、不形成正式发布的前提下执行以下 SIM 状态登记：

| 对象 | 验证结论 | 允许的下一状态 |
|---|---|---|
| `R2-FILE-001` | PASS | `verified_candidate -> sim_applied` |
| `R2-FILE-002` | PASS | `verified_candidate -> sim_applied` |
| `R2-FILE-003` | PASS | `verified_candidate -> sim_applied` |
| 受影响场景复演 | PASS | `not_run/pending_independent_replay_verification -> passed` |
| `R2-FIND-001..008` | PASS | 允许在本轮 SIM 治理台账中登记为 `closed` |

上述授权只表示三份冻结候选已经在 8013 作为非受控 SIM 解释/检查覆盖层完成应用和复演。它不允许修改 145 个现用基线模板，不允许把候选转为正式文件，不允许把 SIM 内审、CAPA、管评或批准冒充真实运行结果。

## 2. 只读边界

- 未读取 `证据/密封/` 或其他密封受限材料。
- 未修改三份候选、独立报告、自治治理主台账、现用 Word、产品代码或数据库。
- 8013 数据核验使用直接 `SELECT`、只读运行 smoke 和业务策略回调计数；8 个禁止候选的持久化回调调用数为 0。
- 未访问或写入 8010、8011、8012、8014。
- 本次唯一新增文件为本报告。

验证时 8013 应用容器为 `lims-zhj-rehearsal-r2-main-20260719-app-1`，镜像身份 `sha256:77c77af553ef7c6de0919c113688f2d4625b6820ceef2778365918f824823d12`；数据库容器健康。环境 marker、数据库身份和 server UUID 一致，83 张数据库表均被排演数据策略显式分类。

## 3. 候选冻结哈希

直接对当前文件重新计算 SHA256，并与 `SHA256SUMS-v0.3.md`、应用清单逐项比对：

| 候选 | 当前 SHA256 | 结果 |
|---|---|---|
| `R2-FILE-001`：`SIM-质量手册差异覆盖候选-v0.1.md` | `cd0fb998304c19c1ddb8ecc322493549d47e59a9c34de9d4200b6b15fe3837ff` | 一致，未漂移 |
| `R2-FILE-002`：`SIM-程序与结构覆盖候选-v0.1.md` | `9a00d8b9d0dd3e0c002869edfa1231c64fcd624445ec47953143ea19f32b8494` | 一致，未漂移 |
| `R2-FILE-003`：`SIM-记录模板语义覆盖候选-v0.2.md` | `371ee52fefbe8683e65868a0032a242ef999fda8d52cc7a53960ec02192b3783` | 一致，未漂移 |
| `R2-FILE-003` 机器载荷 JSON | `23a466896e8faa0917b907904f679b0009b4a65a83f95fc1db6693588645d7de` | 一致，未漂移 |

前序独立报告当前哈希也与路由证据一致：

- `独立验证-半年运行与自治候选-v0.1.md`：`bf07a2e2cba7910840ece56b411389cfce9a449e165daa4585852e1f279832ed`
- `独立验证-记录模板语义覆盖候选-v0.2.md`：`ee8d59fc29eba6daaf14ed09afcdd98b22e051ad42af2e3f2add4dfb58c8e421`

## 4. 半年 v0.2 与记录链反向核验

当前 8013 的 `qms_rehearsal_six_month_v02_runtime_smoke.php` 新鲜运行结果为：

```text
qms_rehearsal_six_month_v02_runtime_smoke passed: 382 assertions
```

独立拆分查询结果如下：

| 核验项 | 8013 当前结果 | 判定 |
|---|---:|---|
| v0.2 新增 SIM 模板 | 7 | PASS |
| 业务链 | 16 | PASS |
| 每链阶段 | 6 | PASS |
| 业务阶段总数 | 96 | PASS |
| 字段超出冻结 schema | 0 | PASS |
| schema 值校验失败 | 0 | PASS |
| 原始记录 | 16；必需结构字段完整，真实技术结果声明为 false | PASS |
| 报告记录 | 16；编制、复核、技术授权、系统发放字段完整 | PASS |
| 原值保留更正历史 | 16 | PASS |
| 报告生命周期历史 | 64（每报告 4 条） | PASS |
| 指定历史合计 | 80 | PASS |
| 核心外部依据覆盖记录 | 16 | PASS |
| 既有 `due + published` 单独处置记录 | 5 | PASS |
| 既有 5 行 `qms_sources` | 仍为 `due + published`，未被覆盖 | PASS |

16 条报告均由报告日期和场所范围内有效的 `authorized_signatory` 完成技术批准/签发；质量负责人未冒充授权签字人。报告生命周期严格为：

```text
prepare -> review -> technical_authorize -> system_issue
```

系统发放人单列为 `SIM-R2-E-DOC`，未替代技术签发。

## 5. CMA/CNAS 分流和禁止写入

8013 当前 16 条报告的分流结果：

| 路径 | 方法数 | 两场所报告数 | 结果 |
|---|---:|---:|---|
| 五项国标 | 5 | 10 | `CMA=true` |
| 三项 DB65 | 3 | 6 | `CMA=false` |
| 全部报告 | 8 | 16 | `CNAS mark=false` |

8 项禁止候选均在持久化回调前被拒绝：

1. 抽样；
2. 留样；
3. 分包；
4. 钻石分级；
5. `GB/T 44914-2024` 检测；
6. CNAS 标志；
7. DB65 使用 CMA 标志；
8. 非 SIM 身份。

新鲜核验结果为 `rejected=8`、`callback_called=0`、禁止目标持久化记录 `0`；运行 smoke 同时确认拒绝前后数据库指纹一致。

## 6. 内审、CAPA 与管理评审

当前 8013 的纯查询运行核验全部通过：

| 核验项 | 当前结果 | 判定 |
|---|---|---|
| 内审计划 | 存在，状态 `completed` | PASS |
| 场所 | 乌鲁木齐、和田两场所 | PASS |
| 审核独立性 | 两场所均由独立 SIM 内审员审核，审核员不审核本人 | PASS |
| 冻结考卷覆盖 | 132/132 | PASS |
| 内审不符合 | 8 | PASS |
| 治理发现回链 | `R2-FIND-001..008` 全部逐项回链 | PASS |
| CAPA | 8 个，均与发现双向关联并关闭 | PASS |
| CAPA 独立性 | 8 个均为执行者与验证者分离 | PASS |
| CAPA 时序和完整性 | 验证、验证日期、有效性结论、有效性评价日期均完整 | PASS |
| 管评时序 | 晚于全部 CAPA 有效性评价 | PASS |
| 管评主席 | SIM 实验室主任 | PASS |
| 8.9.2 输入 | a—o 全部覆盖且有明确评价 | PASS |
| 8.9.3 输出 | a—d 共 4 项，责任人、期限、资源、完成证据和验证结论完整 | PASS |
| 最终批准 | SIM 实验室主任；`formal_approval=false` | PASS |
| 重开机制 | 已登记重大文件变化后的重开要求 | PASS |

因此，8 项治理发现具备在本轮 **SIM 治理台账** 中关闭的证据条件。这里的“关闭”不消除候选的非正式性质；真实文件批准、真实运行和真实外审仍需另行完成。

## 7. 现用 145 模板未被覆盖

8013 当前模板按治理身份拆分：

- `trial_batch IS NULL` 且未软删除的现用基线模板：**145**；
- v0.2 专用 SIM 模板：**7**；
- 另有旧 SIM 治理/运行模板，与现用基线身份分离。

现用 145 个模板数量未减少；v0.2 使用独立 `SIM-R2-V02-*` 身份和 `trial_batch=SIM-GOV-R2-20260719`，没有以同 ID 更新现用模板。现用基线模板未被覆盖。

## 8. 门禁判定

**最终判定：PASS。**

允许：

```text
R2-FILE-001/002/003: verified_candidate -> sim_applied
replay: passed
R2-FIND-001..008: closed（仅本轮 SIM 治理）
```

不允许：

- 将候选登记为正式批准、正式发布或真实生效；
- 用 SIM 证据替代真实六个月运行、真人签字或真实 CAPA；
- 修改或覆盖 145 个现用基线模板；
- 宣称已通过 CMA/CNAS 评审或已获得 CNAS 认可。

## 9. 进度卡

当前版本：`独立验证-8013覆盖层应用与受影响场景复演-v0.1.md`  
本次完成：三候选哈希、半年 v0.2、16 链、标志分流、禁止写入、内审/CAPA/管评和 145 模板隔离的独立只读复验。  
下一步：交治理路由更新自治治理台账和复演清单状态；不得由本独立验证角色直接改台账或数据库。  
关键决定：PASS；允许三候选 `sim_applied`、复演 `passed`、8 项发现在本轮 SIM 治理中关闭。
