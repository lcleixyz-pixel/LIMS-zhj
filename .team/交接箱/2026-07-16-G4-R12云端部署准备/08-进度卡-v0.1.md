# 进度卡 v0.1

项目：LIMS-zhj 云端体验部署准备

日期：2026-07-16

## 当前版本

```text
branch:     codex/g4-r12-pr31-integrated-deploy-prep
release_id: 20260716-g4r12-pr31
image:      jewelry-qms-experience:amd64-g4r12-pr31
```

## 本次完成

- 完成 PR #31 与 G4-R12 的本地集成候选版本；
- 生成并校验 `20260716-g4r12-pr31` 候选包；
- 补齐 C1 Draft PR 文案；
- 补齐 C2 云端只读体检、上传、SHA 校验、`docker load`、不切换 current 的执行清单；
- 补齐 C3 切换、验证和回退预案。

## 下一步

推荐先批准：

```text
批准 G4-R12-C1：推送集成分支并创建 Draft PR，不部署云端
```

再批准：

```text
批准 G4-R12-C2：云端只读快照 + 上传集成候选包 + SHA 校验 + docker load，不切 current
```

## 等你补齐

- 是否批准 C1；
- 是否在 C1 后继续批准 C2；
- C3 切换前，是否要求先人工再看一遍 Draft PR。

## 关键决定

- 旧候选包 `20260716-g4r12-r11e` 被 `20260716-g4r12-pr31` 取代；
- PR #31 暂不关闭，先由集成分支承接云端体验部署路径；
- 法规月度自动监测仍默认关闭，后续另行审批是否启用。
