# G4-R12-C0 PR31 集成与冲突治理记录 v0.1

日期：2026-07-16

## 结论

已在本机隔离 worktree 中完成 PR #31「法规动态监测与复核技能包」与 G4-R12 云端体验部署准备线的集成。

本轮没有推送 GitHub、没有上传云端、没有切换服务器 `current`、没有导入或修改云端数据库、没有开放员工登录。

## 为什么必须集成

G4-R12 本地候选包原本基于 `codex/g4-r12-cloud-deploy-prep`；PR #31 位于 `codex/regulatory-change-monitor-implementation`。

核查结果显示两条线不是简单先后关系：

```text
PR31 不是当前 G4-R12 HEAD 的祖先
当前 G4-R12 HEAD 也不是 PR31 的祖先
```

因此，直接使用旧候选包会漏掉 PR #31 的法规监测实现；直接使用 PR #31 又会漏掉 G4-R11/G4-R12 的本地治理和体验部署修正。

## 隔离分支

```text
worktree: /Users/lc.leixyz/Documents/AI工作台/01-项目代码/LIMS-zhj/.worktrees/g4-r12-pr31-integrated-deploy-prep
branch:   codex/g4-r12-pr31-integrated-deploy-prep
base:     da67211a docs(qms): record G4-R12 local version freeze
commit:   61266e22533f5cfdcdd2d4fbaca80b203e4ec26b
```

## 冲突治理摘要

本次合并出现 5 个文件冲突，均已人工选择治理路径：

| 文件 | 治理选择 |
|---|---|
| `jewelry-qms/app/model/QmsExternalChangeCandidate.php` | 保留 PR #31 更完整的模型字段、时间戳和 JSON 类型定义 |
| `jewelry-qms/app/view/layout/main.html` | 保留 G4-R12 的 `qms_can` 导航权限过滤，同时增加「法规监测运行」入口；避免重复菜单项 |
| `jewelry-qms/config/qms.php` | 质量负责人权限同时包含 `planningregulatorycandidate` 与 `planningregulatorymonitor` |
| `jewelry-qms/deploy/experience/db-init/02-apply-migrations.sh` | 保留 G4-R12 的稳健 Bash 迁移脚本，避免 PR #31 中 `/bin/sh` 与数组语法不匹配 |
| `jewelry-qms/route/app.php` | 同时保留法规候选池路由和法规监测运行路由 |

## 额外可部署性修正

1. `jewelry-qms/composer.lock` 已纳入版本控制，避免云端或后续构建时 Composer 依赖版本漂移。
2. `jewelry-qms/.gitignore` 已取消忽略 `composer.lock`。
3. `jewelry-qms/tests/regulatory_candidate_smoke.php` 已去除对 MySQL `PROCESS` 权限的测试依赖，改为验证失败前是否尝试写入候选项；这与体验环境最小权限边界一致。

## 本地验证

已通过的核心验证：

```text
qms_validation_smoke passed
record_forms_schema_smoke passed
record_forms_print_smoke passed
qms_experience_environment_smoke passed
qms experience deploy bootstrap smoke passed
record_forms_instance_smoke passed
qms_ui_navigation_template_smoke passed
rbac_controller_normalization_smoke passed
qms_g4r7_trial_readiness_smoke passed
qms_g4r9_stale_link_guard_smoke passed
qms_external_change_event_smoke passed
regulatory_schema_smoke passed
regulatory_impact_smoke passed
regulatory_candidate_smoke passed
regulatory monitor UI smoke: PASS
regulatory_promotion_smoke passed
regulatory_promotion_ui_smoke passed
regulatory export smoke: PASS
regulatory_monitor_command_smoke passed
regulatory_e2e_smoke passed
regulatory_candidate_concurrency_smoke passed
regulatory_dry_run_deadlock_smoke passed
regulatory_monitor_compose_smoke passed
regulatory_source_registry_smoke passed
deploy/experience/verify.sh passed
git diff --check passed
git diff --cached --check passed
```

说明：

- `regulatory_source_registry_smoke` 已用 `--network none --entrypoint php` 方式验证，确认不依赖外网。
- 一次性本地体验验证环境已关闭并删除临时 volume。

## 版本结论

旧候选包 `20260716-g4r12-r11e` 应标记为被取代；新的本地候选包应使用 `20260716-g4r12-pr31`。
