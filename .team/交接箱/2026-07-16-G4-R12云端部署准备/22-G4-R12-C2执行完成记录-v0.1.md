# G4-R12-C2 执行完成记录 v0.1

日期：2026-07-16

## 1. 用户授权

用户授权原文：

```text
批准 G4-R12-C2：先推送 C2 文档到 PR #32，然后执行服务器只读体检 + 升级前快照 + 上传候选包 + 服务器侧 SHA 校验 + docker load + 解压到新 release；不切 current、不启动、不导入、不开放员工登录。
```

## 2. 执行边界

本轮已执行：

- 推送 C2 文档提交到 PR #32；
- 服务器只读体检；
- 升级前快照；
- 上传候选包；
- 服务器侧 SHA 校验；
- `docker load`；
- 解压到新 release；
- 使用临时 env 做 `docker compose config` 检查。

本轮未执行：

- 未切换 `$BASE/current`；
- 未启动、停止或重建容器；
- 未修改 `$BASE/shared/.env`；
- 未导入或迁移云端数据库；
- 未开放员工登录；
- 未合并 PR；
- 未把 Draft PR 改为 Ready。

## 3. C2 前置状态

| 项目 | 结果 |
|---|---|
| PR | #32 |
| PR 状态 | Open / Draft |
| PR head | `codex/g4-r12-pr31-integrated-deploy-prep` |
| C2 文档推送后 PR 最新提交 | `e4b57c9018c9080fe3b6104067206836fa8179c7` |
| 本地/远端差异 | `0 0` |
| `jewelry-qms/` 树 | `863cfe4e48649155f4b4294004fa976a7fbdb654` |
| 本地候选包 SHA | 源码包 OK；镜像包 OK；release manifest OK |

说明：PR 推送后短暂出现 `mergeStateStatus=UNKNOWN`，等待 GitHub 重新计算后恢复为 `CLEAN`，再进入服务器步骤。

## 4. SSH 登录说明

默认 `ssh root@101.200.41.200` 因未命中 G1 专用密钥而被服务器拒绝，未进入服务器。随后按既有 G1 记录使用：

```text
~/.ssh/zhj_qms_g1_ed25519
```

并启用：

```text
IdentitiesOnly=yes
StrictHostKeyChecking=yes
```

G1 公钥指纹：

```text
SHA256:h4xIm6hrSDf1O+V1xWk96xlZnVSX5RZ1/MEwlDiWf0Y
```

## 5. C2-1 服务器只读体检

| 项目 | 结果 |
|---|---|
| hostname | `iZ2ze8qpogd088d26h0i04Z` |
| 时间 | `Thu Jul 16 13:51:45 CST 2026` |
| 内核/架构 | Alibaba Cloud Linux，`x86_64` |
| 磁盘 | `/dev/vda3` 49G，总使用 12G，可用 36G，24% |
| 内存 | 3.5GiB，总可用约 2.2GiB |
| Docker | `Docker version 26.1.3` |
| Docker Compose | `v2.27.0` |
| current | `/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a` |
| 当前 app 镜像 | `jewelry-qms-experience:amd64-64fdb2a-g4r3hotfix` |
| 当前 DB 镜像 | `jewelry-qms-mysql:8.4-amd64` |
| 当前体验环境 verify | `PASS: experience environment read-only verification completed` |

## 6. C2-2 升级前快照

首次快照和 retry 快照触发停点：目录中只有 `database.sql`，缺 `uploads.tar.gz` 和 `SHA256SUMS`，不能作为有效快照。原因是远端 here-doc 中的 `docker compose exec` 会读取标准输入，导致后续脚本行被容器命令吞掉。

已将 runbook 修正为对容器内 `exec` 命令添加 `< /dev/null`。

有效完整快照：

```text
/www/server/jewelry-qms-experience/shared/snapshots/20260716-135355-before-20260716-g4r12-pr31-complete
```

完整快照校验：

| 文件 | 字节数 | SHA 校验 |
|---|---:|---|
| `database.sql` | 4,958,836 | OK |
| `uploads.tar.gz` | 112 | OK |

说明：`uploads.tar.gz` 为 112B，是因为当前 `/app/public/uploads` 存在但为空目录。

不完整快照保留为执行痕迹，未删除：

```text
20260716-135213-before-20260716-g4r12-pr31
20260716-135328-before-20260716-g4r12-pr31-retry
```

## 7. C2-3 上传候选包

上传目录：

```text
/root/qms-upload-20260716-g4r12-pr31
```

服务器侧文件：

| 文件 | 大小 |
|---|---:|
| `LIMS-zhj-experience-20260716-g4r12-pr31-SHA256SUMS.txt` | 236B |
| `LIMS-zhj-experience-20260716-g4r12-pr31.tar.gz` | 1.2M |
| `LIMS-zhj-experience-app-image-20260716-g4r12-pr31.tar.gz` | 471M |
| `release-manifest-20260716-g4r12-pr31.SHA256.txt` | 108B |
| `release-manifest-20260716-g4r12-pr31.json` | 2.9K |

## 8. C2-4 服务器侧 SHA 校验与 docker load

服务器侧校验结果：

```text
LIMS-zhj-experience-20260716-g4r12-pr31.tar.gz: OK
LIMS-zhj-experience-app-image-20260716-g4r12-pr31.tar.gz: OK
release-manifest-20260716-g4r12-pr31.json: OK
```

镜像加载结果：

```text
Loaded image: jewelry-qms-experience:amd64-g4r12-pr31
architecture=amd64 os=linux
id=sha256:3d077fa26c0fb5ae9975fd73f1fcee027a75160dd190b6a61d08f0b56453a22c
```

## 9. C2-5 解压到新 release

新 release 目录：

```text
/www/server/jewelry-qms-experience/releases/20260716-g4r12-pr31
```

检查结果：

- `deploy/experience/compose.yaml` 存在；
- `deploy/experience/db-init/02-apply-migrations.sh` 可执行；
- release manifest 已复制到 release 目录；
- release manifest SHA 校验 OK；
- 解压时出现大量 `LIBARCHIVE.xattr.com.apple.provenance` 提示，为 macOS 扩展属性提示，命令退出码为 0。

解压后确认：

```text
current=/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a
```

即未切换 `current`。

## 10. C2-6 compose config 检查

使用临时环境文件：

```text
/www/server/jewelry-qms-experience/shared/.env.check-20260716-g4r12-pr31
```

检查结果：

```text
/tmp/qms-compose-20260716-g4r12-pr31.yaml
image: jewelry-qms-experience:amd64-g4r12-pr31
```

临时文件删除确认：

```text
temp_env_exists=no
temp_env_bak_exists=no
```

## 11. 禁止动作复核

| 禁止动作 | 结果 |
|---|---|
| 切换 `$BASE/current` | 未执行，current 仍指向 `regmon-20260715-64fdb2a` |
| `docker compose up -d` | 未执行 |
| `docker compose down -v` | 未执行 |
| 修改 `$BASE/shared/.env` | 未执行 |
| 数据库迁移 | 未执行 |
| 导入云端数据库 | 未执行 |
| 开放员工登录 | 未执行 |
| 合并 PR | 未执行 |
| Draft PR 改 Ready | 未执行 |

运行中容器复核：

```text
jewelry-qms-experience-app-1 jewelry-qms-experience:amd64-64fdb2a-g4r3hotfix
jewelry-qms-experience-db-1 jewelry-qms-mysql:8.4-amd64
```

说明：新镜像已加载但未运行。

## 12. C2 结论

C2 已完成：候选包已上传并完成服务器侧 SHA 校验，候选镜像已加载，新 release 已解压，compose config 已通过，完整升级前快照已生成并通过 SHA 校验。

当前仍停在 C3 之前：

- 新版本未启动；
- `current` 未切换；
- 云端页面仍运行旧 release；
- 员工登录状态未改变。

建议下一闸门：

```text
批准 G4-R12-C3：切换 current、启动/升级、运行云端验证；如失败按回退预案恢复。
```
