# G4-R12-C2 执行脚本草案说明 v0.1

日期：2026-07-16

## 结论

已为 C2 准备本地执行脚本草案：

```text
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh
```

本轮仅新增脚本草案和说明：

- 未推送 GitHub；
- 未 SSH 登录服务器；
- 未上传候选包；
- 未创建服务器快照；
- 未 docker load；
- 未切换 `current`。

## 设计意图

C2 涉及多段命令：本地 preflight、PR 分支同步、服务器只读体检、快照、上传、SHA 校验、docker load、release 解压和 compose config 检查。手工复制容易漏一步或越过停点，因此将命令拆成可单独执行的 mode。

脚本没有提供“一键全跑”模式，避免一次性越过多个风险点。

## 安全设计

默认安全：

- `help` 只显示帮助；
- `local-preflight` 只做本地和 GitHub 只读检查；
- 其他 mode 均要求显式环境变量：

```bash
export G4_R12_C2_APPROVAL="批准 G4-R12-C2"
```

未设置该变量时，脚本会拒绝执行远端写入、SSH 上传、服务器快照或 docker load。

## 支持的 mode

```text
help
local-preflight
push-docs
remote-readonly
snapshot
upload
verify-load
unpack-release
compose-check
```

## 当前前置状态

截至本说明创建时：

```text
local HEAD: 0f998fa2
origin branch HEAD: 0139b456
ahead: 2
```

ahead 的两个提交均为 C2 本地交接材料：

```text
b0a8b985 docs(qms): add G4-R12 C2 execution guard card
0f998fa2 docs(qms): add G4-R12 C2 completion record template
```

因此，正式执行 C2 前应先在用户授权范围内执行：

```bash
export G4_R12_C2_APPROVAL="批准 G4-R12-C2"
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh push-docs
```

然后再次执行：

```bash
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh local-preflight
```

## 后续执行建议

获批 C2 后，按以下顺序逐段执行并填入 `19-G4-R12-C2执行完成记录模板-v0.1.md`：

```bash
export G4_R12_C2_APPROVAL="批准 G4-R12-C2"

.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh push-docs
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh local-preflight
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh remote-readonly
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh snapshot
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh upload
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh verify-load
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh unpack-release
.team/交接箱/2026-07-16-G4-R12云端部署准备/scripts/g4-r12-c2-runbook.sh compose-check
```

## 禁止事项再次确认

脚本不包含以下动作，C2 期间也不得手工执行：

```bash
ln -sfn "$BASE/releases/20260716-g4r12-pr31" "$BASE/current"
docker compose --env-file "$BASE/shared/.env" -f deploy/experience/compose.yaml up -d
docker compose --env-file "$BASE/shared/.env" -f deploy/experience/compose.yaml down -v
sed -i 's/^QMS_IMAGE_TAG=.*/QMS_IMAGE_TAG=amd64-g4r12-pr31/' "$BASE/shared/.env"
```

这些动作留到 C3 单独审批。
