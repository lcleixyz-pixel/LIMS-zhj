# G4-R12-C 执行准备包 v0.1

日期：2026-07-16

## 当前状态

本机已形成 PR #31 + G4-R12 集成候选包，可以进入下一闸门审批。

仍未执行：

- 未推送 GitHub；
- 未创建新的 Draft PR；
- 未上传云端；
- 未在服务器 `docker load`；
- 未切换服务器 `current`；
- 未开放员工登录。

## 集成候选包

```text
release_id: 20260716-g4r12-pr31
commit:     61266e22533f5cfdcdd2d4fbaca80b203e4ec26b
image:      jewelry-qms-experience:amd64-g4r12-pr31
image_id:   sha256:5340c9b640df85c1ecb0e68bfb194f3f44e3e2e6ba2917048c91616f67b289ea
```

本地包目录：

```text
/Users/lc.leixyz/Documents/AI工作台/05-Codex输出归档/2026-07-16-QMS云端部署准备/G4R12-PR31集成候选发布包/
```

文件：

```text
LIMS-zhj-experience-20260716-g4r12-pr31.tar.gz
LIMS-zhj-experience-app-image-20260716-g4r12-pr31.tar.gz
LIMS-zhj-experience-20260716-g4r12-pr31-SHA256SUMS.txt
```

SHA256：

```text
e83b0552dbcba0d750f346fc560d96141b75cc5c8aad15147e03d4b672b4094d  LIMS-zhj-experience-20260716-g4r12-pr31.tar.gz
4b55517611a76e100b7a2c0802a7f41f5916d7379cf6cdb0c9a43875e40e0393  LIMS-zhj-experience-app-image-20260716-g4r12-pr31.tar.gz
```

包内容复核：

```text
SHA256 校验通过
镜像 architecture=amd64 os=linux
未发现本地 .env、docker/.env.docker、runtime、vendor、node_modules 或 public/uploads 正式/运行态内容
deploy/experience/db-init/02-apply-migrations.sh 在包内保持可执行
deploy/experience/.env.example 保留
composer.lock 已纳入源码包
```

## 建议的下一闸门

### C1：远端版本管理，不部署

如批准：

```text
批准 G4-R12-C1：推送集成分支并创建 Draft PR，不部署云端
```

执行边界：

- 推送 `codex/g4-r12-pr31-integrated-deploy-prep`；
- 创建 Draft PR；
- PR 描述写明旧 G4-R12 候选包已被 `20260716-g4r12-pr31` 取代；
- 不上传云端、不改服务器。

### C2：云端只读快照与上传校验，不切换

如批准：

```text
批准 G4-R12-C2：云端只读快照 + 上传集成候选包 + SHA 校验 + docker load，不切 current
```

执行边界：

- 服务器当前状态只读体检；
- 创建升级前快照；
- 上传 3 个候选包文件；
- 服务器侧 `sha256sum -c`；
- `docker load` 应用镜像；
- `docker image inspect` 验证 amd64；
- 不切换 `current`，不启动新版应用，不开放员工登录。

### C3：切换 current 并云端验证

仅在 C2 通过后再考虑：

```text
批准 G4-R12-C3：切换 current、启动集成版并运行云端验证
```

执行边界：

- 解压为新 release；
- 原子切换 `current`；
- `up -d --pull never`；
- 运行 `deploy/experience/verify.sh`；
- 做法规候选池、一单一库、表格模板、溯源链的页面级冒烟；
- 如验证失败，回退到上一个 release。

## 当前推荐

建议先走 C1，再走 C2。

原因：PR #31 与 G4-R12 已发生实质性集成，先保留远端审查入口更稳；云端动作放在 PR/Draft 形成后执行，可避免“服务器已经跑了但版本管理还没收口”的尴尬。
