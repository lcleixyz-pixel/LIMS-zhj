# G4-R12-C1/C2 放行前核对记录 v0.1

日期：2026-07-16

## 核对结论

当前已具备进入 C1 的本地条件；C2 的候选包和校验材料也仍保持一致。

本轮未执行外部写操作：

- 未推送 GitHub；
- 未创建或修改 PR；
- 未登录服务器执行写操作；
- 未上传候选包；
- 未切换云端 `current`；
- 未修改云端数据库或开放员工登录。

## 当前本地集成分支

```text
branch: codex/g4-r12-pr31-integrated-deploy-prep
HEAD:   79cb55c2 docs(qms): prepare G4-R12 C1 C2 C3 gates
```

最近提交链：

```text
79cb55c2 docs(qms): prepare G4-R12 C1 C2 C3 gates
ed7e9bed docs(qms): record G4-R12 PR31 integration package
61266e22 chore(qms): integrate PR31 with G4-R12 deploy prep
da67211a docs(qms): record G4-R12 local version freeze
b27054a8 chore(qms): freeze G4-R12 cloud deploy prep
adc243c2 Harden stale-link handling and record form validation
```

核对结果：

```text
git status --short --branch
## codex/g4-r12-pr31-integrated-deploy-prep
```

说明：集成 worktree 当前干净。

## PR #31 只读状态

```text
number:     31
title:      法规动态监测与复核技能包
state:      OPEN
isDraft:    true
head:       codex/regulatory-change-monitor-implementation
base:       main
updatedAt:  2026-07-15T06:32:27Z
url:        https://github.com/lcleixyz-pixel/LIMS-zhj/pull/31
```

判断：

- PR #31 仍是 Draft；
- 本地集成分支尚未推送；
- C1 创建新 Draft PR 前，应在 PR 正文中说明与 PR #31 的关系；
- 不建议同时合并 PR #31 和集成 PR。

## 候选包核对

候选包目录：

```text
/Users/lc.leixyz/Documents/AI工作台/05-Codex输出归档/2026-07-16-QMS云端部署准备/G4R12-PR31集成候选发布包/
```

文件：

```text
LIMS-zhj-experience-20260716-g4r12-pr31-SHA256SUMS.txt     236B
LIMS-zhj-experience-20260716-g4r12-pr31.tar.gz             1.2M
LIMS-zhj-experience-app-image-20260716-g4r12-pr31.tar.gz   470M
```

SHA 校验：

```text
LIMS-zhj-experience-20260716-g4r12-pr31.tar.gz: OK
LIMS-zhj-experience-app-image-20260716-g4r12-pr31.tar.gz: OK
```

镜像：

```text
jewelry-qms-experience:amd64-g4r12-pr31
architecture=amd64
os=linux
id=sha256:5340c9b640df85c1ecb0e68bfb194f3f44e3e2e6ba2917048c91616f67b289ea
```

## 主工作区旁路材料

主工作区仍有未纳入集成分支的旁路材料：

```text
.team/不好用清单.md
.team/交接箱/2026-07-16-多角度审核与优化方向/
```

判断：

- 这些材料属于页面体验复审和问题清单；
- 本轮不纳入部署候选包；
- 不应在 C1/C2 中被误删或误提交。

## C1 放行判断

C1 建议可执行，前提是用户明确批准：

```text
批准 G4-R12-C1：推送集成分支并创建 Draft PR，不部署云端
```

C1 执行后应验证：

- `git push` 成功；
- 新 Draft PR URL 已返回；
- PR 正文引用 `20260716-g4r12-pr31`；
- PR 正文说明 PR #31 与集成 PR 的关系；
- 不执行任何云端动作。

## C2 放行判断

C2 建议在 C1 完成后再执行，前提是用户明确批准：

```text
批准 G4-R12-C2：云端只读快照 + 上传集成候选包 + SHA 校验 + docker load，不切 current
```

C2 的关键停止线：

- 可以做服务器只读体检；
- 可以创建升级前快照；
- 可以上传候选包；
- 可以服务器侧 SHA 校验；
- 可以 `docker load`；
- 可以解压到新 release；
- 不得切换 `current`；
- 不得修改 `shared/.env` 的正式镜像标签；
- 不得启动新版本；
- 不得开放员工登录。

## 当前建议

下一步优先执行 C1。这样能先把版本管理和审查入口立住，再做服务器上传校验。
