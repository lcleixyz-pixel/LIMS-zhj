# G4-R12-C3 执行完成记录模板 v0.1

日期：2026-07-16

## 1. 用户授权

待填写用户授权原文：

```text
批准 G4-R12-C3：……
```

## 2. C3 前置状态

待填写：

```text
local_branch:
local_HEAD:
origin_branch_HEAD:
PR:
PR_state:
PR_isDraft:
PR_mergeStateStatus:
jewelry_qms_tree:
current_before:
target_release:
image:
snapshot:
```

最低要求：

- PR #32 为 Open / Draft / CLEAN；
- 本地分支与远端分支同步；
- `jewelry-qms/` 树为 `863cfe4e48649155f4b4294004fa976a7fbdb654`；
- C2 完整快照 SHA OK；
- 候选 release 已存在；
- 候选镜像已加载且为 `amd64/linux`。

## 3. C3 local-preflight

待粘贴输出：

```text
C3 local preflight:
```

## 4. C3 remote-preflight-readonly

待粘贴输出：

```text
C3 remote read-only preflight:
```

确认：

- [ ] 当前 `current` 仍非候选 release；
- [ ] 候选 release 存在；
- [ ] 候选镜像存在；
- [ ] 完整快照 SHA OK；
- [ ] 当前旧环境 verify OK；
- [ ] 临时 env 文件不存在。

## 5. C3 switch-and-verify

待填写：

```text
previous_release_file:
previous_release:
env_backup:
QMS_IMAGE_TAG_after:
REGULATORY_MONITOR_ENABLED_after:
current_after:
docker_compose_up_result:
verify_result:
running_images:
```

确认：

- [ ] 已记录 previous release；
- [ ] 已备份 `$BASE/shared/.env`；
- [ ] `QMS_IMAGE_TAG=amd64-g4r12-pr31`；
- [ ] `REGULATORY_MONITOR_ENABLED` 未开启；
- [ ] `current` 已指向候选 release；
- [ ] `docker compose up -d --pull never` 成功；
- [ ] `verify.sh` 通过；
- [ ] app 运行候选镜像；
- [ ] DB 未暴露公网端口。

## 6. 页面级云端冒烟

待填写：

| 检查项 | 结果 | 证据/备注 |
|---|---|---|
| 登录页可访问 | 待填 |  |
| 首页无 500 | 待填 |  |
| 体验环境警示仍显示 | 待填 |  |
| 本月待办措辞可读 | 待填 |  |
| 首页新建记录不再空 `template_id` | 待填 |  |
| 法规候选池入口可找到 | 待填 |  |
| 候选池无乱码 | 待填 |  |
| 一单一库候选影响范围覆盖六类影响 | 待填 |  |
| 候选池不自动修改受控对象 | 待填 |  |
| 组合包页理解难度观察 | 待填 |  |

## 7. 如失败：回退记录

仅失败时填写：

```text
failure_step:
failure_output_or_url:
rollback_started_at:
rollback_previous_release:
rollback_verify_result:
current_after_rollback:
running_images_after_rollback:
logs_preserved:
```

确认：

- [ ] 已回退 `current`；
- [ ] 已恢复 `$BASE/shared/.env`；
- [ ] 回退后 `verify.sh` 通过；
- [ ] 已记录失败证据；
- [ ] 未删除候选 release、快照或上传包。

## 8. C3 结论

待填写：

```text
C3_result:
cloud_entry:
known_risks:
next_gate:
```

建议下一闸门：

```text
G4-R12-C4：受控试运行入口复核、岗位人员试用安排、问题登记与是否开放员工登录的单独审批。
```
