# G4-R12-C3 执行完成记录 v0.1

日期：2026-07-16

## 1. 用户授权

用户授权原文：

```text
批准 G4-R12-C3：先推送 C3 准备提交到 PR #32，然后切换 current、启动/升级、运行云端验证；如失败按回退预案恢复。
```

## 2. 执行边界

本轮已执行：

- 推送 C3 准备提交到 PR #32；
- 本地 preflight；
- 服务器只读 preflight；
- 记录 previous release；
- 备份 `$BASE/shared/.env`；
- 将 `QMS_IMAGE_TAG` 切换为 `amd64-g4r12-pr31`；
- 将 `$BASE/current` 切换到候选 release；
- 执行 `docker compose up -d --pull never`；
- 运行 `deploy/experience/verify.sh`；
- 执行公网和登录后页面级冒烟。

本轮未执行：

- 未执行 `docker compose down -v`；
- 未删除旧 release；
- 未删除快照；
- 未删除上传包；
- 未开启法规月度自动监测；
- 未导入正式业务数据或客户敏感信息；
- 未开放员工大范围登录；
- 未合并 PR；
- 未把 Draft PR 改 Ready。

## 3. C3 前置状态

| 项目 | 结果 |
|---|---|
| PR | #32 |
| PR 状态 | Open / Draft / CLEAN |
| PR head | `codex/g4-r12-pr31-integrated-deploy-prep` |
| C3 准备提交 | `a2a56d9073ba4e2f075326d339128ebb33bf7e35` |
| `jewelry-qms/` 树 | `863cfe4e48649155f4b4294004fa976a7fbdb654` |
| previous current | `/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a` |
| target release | `/www/server/jewelry-qms-experience/releases/20260716-g4r12-pr31` |
| target image | `jewelry-qms-experience:amd64-g4r12-pr31` |
| target image inspect | `amd64/linux sha256:3d077fa26c0fb5ae9975fd73f1fcee027a75160dd190b6a61d08f0b56453a22c` |
| 完整快照 | `/www/server/jewelry-qms-experience/shared/snapshots/20260716-135355-before-20260716-g4r12-pr31-complete` |

## 4. C3 local-preflight

结果：

```text
C3 local preflight OK
```

确认：

- 本地分支与远端分支同步；
- PR #32 为 Open / Draft / CLEAN；
- `jewelry-qms/` 树符合候选包记录。

## 5. C3 remote-preflight-readonly

结果：

```text
current=/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a
target=/www/server/jewelry-qms-experience/releases/20260716-g4r12-pr31
image=amd64/linux sha256:3d077fa26c0fb5ae9975fd73f1fcee027a75160dd190b6a61d08f0b56453a22c
database.sql: OK
uploads.tar.gz: OK
PASS: experience environment read-only verification completed
```

确认：

- 当前 `current` 在切换前仍非候选 release；
- 候选 release 存在；
- 候选镜像存在；
- 完整快照 SHA OK；
- 当前旧环境 verify OK；
- 临时 env 文件不存在。

## 6. C3 switch-and-verify

切换结果：

```text
QMS_IMAGE_TAG=amd64-g4r12-pr31
previous=/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a
current=/www/server/jewelry-qms-experience/releases/20260716-g4r12-pr31
jewelry-qms-experience-app-1 jewelry-qms-experience:amd64-g4r12-pr31
jewelry-qms-experience-db-1 jewelry-qms-mysql:8.4-amd64
PASS: experience environment read-only verification completed
```

切换后复核：

```text
previous_file=/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a
env_backup_exists=yes
QMS_IMAGE_TAG=amd64-g4r12-pr31
REGULATORY_MONITOR_ENABLED=<unset>
```

说明：`REGULATORY_MONITOR_ENABLED` 未设置时按 compose 默认值 `0`，即法规月度自动监测未开启。

## 7. 公网和登录冒烟

| 检查项 | 结果 |
|---|---|
| HTTP 根路径 | `301` 到 `https://qms.zhj-jc.com/` |
| HTTPS 未认证 | `401`，BasicAuth 生效 |
| BasicAuth 后登录页 | `200` |
| 管理员登录 POST | `302` |
| 登录后 dashboard | `200` |
| 体验环境警示 | 存在：“体验测试环境 / 禁止录入正式业务数据、客户资料或其他敏感信息” |
| 首页空 `template_id` 链接 | 未发现 |
| 本月待办 | 存在 |
| 法规候选池导航 | 存在 |

凭据来源为本机 macOS 钥匙串，未写入聊天、Git 或交接文件。

## 8. 页面级云端冒烟

| 页面/能力 | HTTP | 结果 |
|---|---:|---|
| `/planning/regulatory-candidates` | 200 | 法规候选池可访问；未检出乱码；可见“一单一库” |
| `/planning/change-events` | 200 | 变更事件页面可访问；未检出乱码 |
| `/planning/regulatory-candidates/view?id=reg-2026-samr-14-yidanyiku` | 200 | 候选详情可访问；未检出乱码 |
| `/record_form_template/index` | 200 | 记录模板页可访问；“填写”链接均带具体 template_id |
| `/record_form_template/review` | 200 | 模板复核页可访问；“试填”链接均带具体 template_id |
| `/record_form_instance/index` | 200 | 记录实例页可访问 |
| `/doc_template/index` | 200 | 文档模板页可访问 |
| 抽检记录试填链接 | 200 | 未出现“记录模板不存在” |
| `/planning/traceability` | 200 | 溯源链页可访问，但发现 1 个旧内容替换字符，见下方记录 |

“一单一库”候选详情影响范围复核：

- 能力项目 / CMA 标志：可见；
- 体系文件：可见；
- 人员授权：可见；
- 设备校准：可见；
- LIMS 规则：可见；
- 培训：可见；
- 候选池只给人工确认，不自动修改受控对象：页面有明确提示。

## 9. 页面冒烟发现

### 9.1 溯源链页旧内容存在 1 个替换字符

页面：`/planning/traceability`

定位：

```text
GB/T 27025-2019 7.6 测量不确定度的评�
```

判断：

- 该问题不是候选池乱码复发；候选池、候选详情和变更事件页面均未检出乱码；
- 本地交接材料中同一依据原文存在正确文本“测量不确定度的评定”；
- 该问题属于溯源链旧内容显示/数据清洗问题；
- 本轮 C3 未获授权修改云端数据，因此未直接修复；
- 该问题登记为 C4/试运行问题，不触发 C3 技术回退。

## 10. 回退状态

未执行回退。

回退材料已具备：

- previous release 文件存在；
- env 备份存在；
- 完整升级前快照存在且 SHA OK；
- C3 runbook 已提供 `rollback` mode。

## 11. C3 结论

C3 命令级切换和页面级云端冒烟通过，云端体验环境当前运行候选版本：

```text
/www/server/jewelry-qms-experience/releases/20260716-g4r12-pr31
jewelry-qms-experience:amd64-g4r12-pr31
```

当前仍保持体验测试环境边界：

- BasicAuth 仍开启；
- 页面体验环境警示仍显示；
- MySQL 未暴露公网端口；
- 法规月度自动监测未开启；
- 未开放员工大范围登录；
- PR #32 仍为 Draft，未合并。

下一建议闸门：

```text
G4-R12-C4：受控试运行入口复核、岗位人员试用安排、问题登记与是否开放员工登录的单独审批。
```
