# G4-R12-B 本地版本冻结记录 v0.1

日期：2026-07-16

## 结论

已完成本地版本冻结，形成独立分支和提交。

本轮仍未上传云端、未切换 `current`、未导入云端数据库、未开放员工登录、未推送 GitHub。

## 分支与提交

```text
branch: codex/g4-r12-cloud-deploy-prep
base:   adc243c2640b61a6e9eeb2de7dc3dd23b83036bf
commit: b27054a8 chore(qms): freeze G4-R12 cloud deploy prep
```

说明：

- `base` 与当时的 `origin/main` 一致。
- `commit` 纳入 G4-R10/G4-R11/G4-R12 放行准备相关代码、测试和交接记录。
- 本记录文件是在 `commit b27054a8` 之后追加，用于说明冻结边界。

## 已纳入冻结的范围

代码与配置：

- `jewelry-qms/app/controller/RecordFormInstance.php`
- `jewelry-qms/app/view/layout/main.html`
- `jewelry-qms/app/view/record_form_instance/index.html`
- `jewelry-qms/app/view/record_form_instance/view.html`
- `jewelry-qms/compose.yaml`
- `jewelry-qms/docker/.env.docker`
- `jewelry-qms/deploy/experience/db-init/02-apply-migrations.sh`

测试：

- `jewelry-qms/tests/record_forms_instance_smoke.php`
- `jewelry-qms/tests/qms_ui_navigation_template_smoke.php`
- `jewelry-qms/tests/qms_experience_deploy_bootstrap_smoke.php`

交接记录：

- `.team/交接箱/2026-07-16-G4-R10测试残留清理/`
- `.team/交接箱/2026-07-16-G4-R11试运行放行硬伤治理/`
- `.team/交接箱/2026-07-16-G4-R12云端部署准备/`

## 明确未纳入的内容

以下内容仍留在工作区，未纳入本地冻结提交：

- `.team/不好用清单.md`：用户/旁路问题清单，不属于本轮部署候选包。
- `.team/交接箱/2026-07-16-多角度审核与优化方向/`：全量页面巡检证据目录，体量约 12M，属于评审证据，不直接进入本轮发布冻结。

## 提交前验证

```text
qms_validation_smoke passed
record_forms_schema_smoke passed
record_forms_print_smoke passed
qms_experience_environment_smoke passed
qms experience deploy bootstrap smoke passed
record_forms_instance_smoke passed
qms_ui_navigation_template_smoke passed
rbac_controller_normalization_smoke passed
qms_g4r7_trial_readiness_smoke passed
qms_g4r9_stale_link_guard_smoke passed
qms_external_change_event_smoke passed
git diff --check passed
git diff --cached --check passed
```

敏感信息粗扫：

```text
未发现正式密钥；命中项均为示例口令、开发 APP_KEY 或代码字段名。
```

## 与候选包关系

候选发布包仍使用：

```text
release_id: 20260716-g4r12-r11e
image_tag:  jewelry-qms-experience:amd64-g4r12-r11e
```

候选包生成在本地冻结提交前，但源码包只包含 `jewelry-qms/`，本轮提交后未再改变 `jewelry-qms/` 内容，因此候选包与冻结代码保持一致。

## 下一闸门

建议进入 G4-R12-C：

1. 如需远端审查：推送 `codex/g4-r12-cloud-deploy-prep` 并创建 Draft PR。
2. 如需直接云端准备：先做云端只读快照，再上传候选包并执行服务器侧 SHA 校验、`docker load`、镜像架构检查。
3. 仍不切换云端 `current`，除非用户另行批准。

