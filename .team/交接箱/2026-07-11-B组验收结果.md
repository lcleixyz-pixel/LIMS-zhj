# B 组验收结果（2026-07-11）

> 依据：`.team/交接箱/2026-07-11-B组验收指引.md`  
> 执行：重建容器后按清单逐项实测

## 总评

**B 组通过（B1/B2/B3）。**

## 清单勾选

### 重建
- [x] `docker compose up -d --build app` 成功
- [x] 日志含 `crond started — daily check:reminders at 08:00`

### B1
- [x] 启动日志有 crond
- [x] `php think check:reminders --type=all` 不报错（现返回 7 类计数）
- [x] `date` 显示 `CST` / `TZ=Asia/Shanghai`

### B2
- [x] `--type=equipment_verification` 不报错
- [x] `--type=audit_schedule` 不报错
- [x] `--type=source_freshness` 不报错
- [x] `--type=all` 覆盖 7 类不报错
- [x] 字段核对：`equipment_maintenances.next_due_date` + `maintenance_type=verification`；`audit_schedules.audit_date` + `status=planned`；`qms_sources.next_freshness_due` + `status=published`
- [x] 造临期数据：插入 verification、`next_due_date=明天` → 跑检查生成「期间核查到期提醒」1 条

### B3
- [x] `/calendar/index` HTTP 200，标题「本月体系待办（2026年7月）」
- [x] 空态显示「本月暂无到期待办」；有通知后显示表格（到期日/剩余天数/即将到期徽章/前往处理）
- [x] 顶部菜单有「本月待办」
- [x] `staff_test` 可访问（HTTP 200，未拒权）
- [x] B2 造数后日历可见「期间核查到期提醒 | 2026-07-12 | 1 天 | 即将到期」

## 小问题（不挡验收）

1. 期间核查通知的 `type` 落库为 `calibration`（复用现有枚举），日历「类型」列显示 calibration 而非「期间核查」。可后续扩枚举或映射中文标签。
2. `/tmp/reminders.log` 随容器重建清空；3 天连续日志需稳定运行后观察。

## 测试残留（可留可清）

- 设备 `EQ-B-001` / id `...b001`
- 期间核查计划 id `...b002`
- 通知「期间核查到期提醒」due=2026-07-12
