# 云服务器体验测试部署：G2-B 本地候选包验证记录

- 日期：2026-07-13
- 状态：**通过；停在上传服务器之前**
- 代码分支：`codex/qms-cloud-experience`
- 代码提交：`be2a9cca9d91c62a5cafe2541e894a8df744507d`
- 基础分支：`codex/remote-sync-20260713`（基点 `b07a031`）
- Release ID：`20260713-be2a9cc`
- 应用镜像：`jewelry-qms-experience:amd64-be2a9cc`
- MySQL 镜像：`jewelry-qms-mysql:8.4-amd64`

## 1. 本阶段边界

本阶段只在本机隔离 worktree 和独立 Compose 项目中实现、构建、验证并打包。未上传文件，未连接云服务器，未修改宝塔、Nginx、域名、证书或云端文件。现用本机 `8010/3307` 容器全程保持运行，收尾时 `http://127.0.0.1:8010/login` 仍返回 HTTP 200。

## 2. 实现结果

1. 新增体验环境 Compose：应用仅发布 `127.0.0.1:18010`；数据库不发布宿主机端口；数据库只在 internal 网络，应用同时连接数据库内网与普通 edge 网络。
2. 不挂载 `node_modules` 或应用缓存卷；只持久化数据库、运行时和上传文件。
3. 页面固定显示“体验测试环境”和“禁止录入正式业务数据、客户资料或其他敏感信息”。
4. 增加宝塔 Nginx/BasicAuth 示例、只读验证脚本、首次部署、更新、备份恢复演练、release 回退和磁盘清理说明。
5. 修复首次初始化缺口：基础 SQL 固定使用 `jewelry_qms`，体验配置已与其一致；全新数据卷会顺序执行 19 个迁移文件；模板表为空时装入 9 个内置样板，重复启动不会覆盖已有模板。
6. Playwright 从 `1.53.0` 升级到 `1.61.1`，消除 `GHSA-7mvr-c777-76hp`；最终镜像 `npm audit --omit=dev` 为 0 个漏洞。

## 3. 架构与镜像验证

| 对象 | 结果 |
|---|---|
| 应用镜像 | `architecture=amd64 os=linux`，约 495 MB |
| MySQL 镜像 | `architecture=amd64 os=linux`，约 239 MB |
| 应用容器运行架构 | `x86_64` |
| MySQL 容器运行架构 | `x86_64` |
| Chromium | Alpine 系统 Chromium，实际完成 PDF 输出 |
| 数据库宿主机端口 | `{}`，未发布 |
| 应用宿主机端口 | 仅 `127.0.0.1:18010` |

说明：Docker Desktop 对多架构 `mysql:8.4` 顶层 manifest 的本地 tag 解析会跟随当前平台。为确保导出的是 x86_64 子镜像，本次按 amd64 子 manifest 摘要 `sha256:ef9038553b7ea407704f16770e407ffd32f5566f125d0d94f63ff736af1d43f8` 拉取后再标记为 `jewelry-qms-mysql:8.4-amd64`；离线包 manifest 已确认包含该 tag。

## 4. 功能与持久化验证

- 全新数据卷启动：70 张业务表，最新迁移表 `qms_external_change_events` 存在，初始化服务成功装入 9 个模板。
- 模拟链路：9/9 个模板完成“保存 → 打印 HTML → PDF”，PDF 单文件约 101–299 KB，PDF 元数据显示 Headless Chrome/Skia、A4 页面。
- 测试记录标题统一为 `TEST-LINK-*`，字段内容含“【测试】”“用于链路验证的模拟内容，非受控记录”。未使用真实客户或正式业务数据。
- 重启候选应用和数据库后：9 条已生成记录、9 份 PDF、1 份链路报告均保留。
- 重复执行初始化：输出 `experience bootstrap skipped: 9 active templates already exist`，未覆盖已有模板。
- 升级 Playwright 后再次执行 PDF 链路：9/9 通过。

最终回归通过：

- `qms_validation_smoke.php`
- `record_forms_schema_smoke.php`
- `record_forms_print_smoke.php`
- `qms_experience_environment_smoke.php`
- `qms_experience_deploy_bootstrap_smoke.php`
- PHP 语法检查 6/6
- Compose 配置检查、Shell 语法检查、只读 `verify.sh`
- Git 工作树干净，`git diff --check` 通过

## 5. 离线发布包

本机目录：

`/Users/lc.leixyz/Documents/AI工作台/05-Codex输出归档/2026-07-13-QMS云服务器体验部署/G2B候选发布包/`

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `LIMS-zhj-experience-20260713-be2a9cc.tar.gz` | 10,592,074 bytes | `68d52cbd3ca0bf5f1ff9a94c3d1fbc5676421173ad33a1746b7b90082aa1b74b` |
| `LIMS-zhj-experience-images-20260713-be2a9cc.tar.gz` | 732,895,758 bytes | `83e3f3f2168a5b0dfdc407760768c467195f4e06353209d1a730e107eef78963` |
| `LIMS-zhj-experience-SHA256SUMS.txt` | 227 bytes | 包含以上两项校验值 |

校验结果：两个 gzip 完整；代码包包含 Compose、README、验证脚本和迁移初始化脚本；镜像包 manifest 同时包含应用与 MySQL 的已验证 tag；`shasum -a 256 -c` 两项均为 `OK`。

## 6. 风险与下一闸门

- 应用目前没有提交 `composer.lock`，镜像构建阶段会解析当时可用的 PHP 依赖。由于云端只加载本次已验证的离线镜像、不重新构建，G2 体验部署可继续；后续正式生产化前应单独补齐 Composer 锁文件与依赖升级验证。
- 本机候选容器、网络、测试卷和临时密码文件已清理；已验证镜像和离线包保留。
- 下一步是计划任务 6 的真实云端变更。必须再次获得用户明确批准后，才可上传两个包、在服务器校验 SHA-256、执行 `docker load`、创建 release/shared 目录、生成服务器本地密码、启动容器并配置宝塔站点、HTTPS 和 BasicAuth。
