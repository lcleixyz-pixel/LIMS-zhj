# 云服务器体验测试部署执行记录 v1.0

> 日期：2026-07-13  
> 执行依据：`2026-07-13-云服务器体验测试部署计划-v0.2.md`  
> 用户授权：批准任务 6 云端部署  
> 结论：**任务 6 通过；体验入口已上线，停止在任务 7 创建体验账号之前**

## 一、部署对象与版本

| 项目 | 实际值 |
|---|---|
| 服务器 | `101.200.41.200`，Alibaba Cloud Linux 3，`x86_64` |
| 测试域名 | `https://qms.zhj-jc.com` |
| Release ID | `20260713-be2a9cc` |
| Git 分支 | `codex/qms-cloud-experience` |
| Git 提交 | `be2a9cca9d91c62a5cafe2541e894a8df744507d` |
| GitHub PR | `#29`：`https://github.com/lcleixyz-pixel/LIMS-zhj/pull/29` |
| 应用镜像 | `jewelry-qms-experience:amd64-be2a9cc` |
| MySQL 镜像 | `jewelry-qms-mysql:8.4-amd64` |
| 服务器 release | `/www/server/jewelry-qms-experience/releases/20260713-be2a9cc/` |
| current | `releases/20260713-be2a9cc` |

## 二、部署前保护与校验

1. 重新扫描服务器 ED25519 主机指纹，实时值与 G1 已核对记录完全一致：`SHA256:PKX3/4c3+a1xpHhUHd3Pn0UR2jPn5PyT4oTfT1/3W60`；随后才恢复严格 SSH 连接。
2. 部署前 `https://zhj-jc.com` 返回 HTTP 200；服务器无现有容器和镜像，`127.0.0.1:18010` 未占用。
3. 现有站点配置和 Nginx 主配置已备份到 `/root/qms-deploy-backup-20260713-be2a9cc/`：
   - `zhj-jc.com.conf` SHA-256：`a386866ad394418c4472aef3cada8a83bcc3791c57ff688d5402d14e13d44e90`
   - `nginx.conf` SHA-256：`dd87d0dad8fd394fb7e5f0fbe6420bc2fc02a50196d144600b732b87771c65ef`
4. 服务器端复核离线包：
   - 代码包：`68d52cbd3ca0bf5f1ff9a94c3d1fbc5676421173ad33a1746b7b90082aa1b74b`，校验 `OK`
   - 镜像包：`83e3f3f2168a5b0dfdc407760768c467195f4e06353209d1a730e107eef78963`，校验 `OK`
5. 校验文件已保存为 `/www/server/jewelry-qms-experience/shared/release-manifests/20260713-be2a9cc-SHA256SUMS.txt`；上传到 `/tmp` 的约 709 MB 临时包已在完成验证后清理。

## 三、实际部署结果

### 3.1 容器与数据库

- 两张镜像均由本地离线包执行 `docker load`，服务器未运行 `docker build`，未临时从 Docker Hub 拉取镜像。
- 应用和 MySQL 镜像均为 `architecture=amd64 os=linux`；容器内 `uname -m` 均为 `x86_64`。
- 数据库初始化得到 70 张表，最新迁移表存在，自动装入 9 个内置记录模板，当前记录实例为 0。
- 应用只发布 `127.0.0.1:18010`；MySQL `PortBindings={}`，没有宿主机端口。
- `bootstrap` 一次性容器退出码为 0；应用持续运行；MySQL 状态为 healthy。

### 3.2 服务器专用配置

- `/www/server/jewelry-qms-experience/shared/.env`：`root:root 600`。
- `/www/server/jewelry-qms-experience/shared/.htpasswd`：`root:www 640`。
- `shared/`：`root:www 750`，只允许 Nginx 工作用户穿过目录读取 `.htpasswd`；`.env` 仍不可被 `www` 读取。
- 数据库密码均在服务器生成，未进入 Git、聊天或交接文档。
- 计划 v0.2 示例仍写有 `MYSQL_DATABASE=jewelry_qms_experience`，但 G2-B 已证明基础 SQL 固定创建并使用 `jewelry_qms`；本次按已验证候选配置使用 `MYSQL_DATABASE=jewelry_qms`。这是经本地从零初始化验证后的必要校准，不修改已批准计划原文。

### 3.3 宝塔、HTTPS 与 BasicAuth

- 通过宝塔内置站点 API 新增 `qms.zhj-jc.com`，站点 ID 为 2；未编辑现有 `zhj-jc.com` 站点配置。
- 通过宝塔内置 ACME/Let's Encrypt 申请并安装独立证书：
  - Subject/SAN：`qms.zhj-jc.com`
  - 签发机构：Let's Encrypt YR2
  - 有效期：2026-07-13 至 2026-10-11
  - 宝塔续签订单：1 条，状态 `valid`，验证方式 `http`
  - 私钥权限已从面板生成后的 644 收紧为 `root:root 600`
- 新站点 Nginx 配置 SHA-256：`d959a6e96206871c967ec18a2c6c2294fa8bd53ee7bc91b489bc7a906691182c`；`nginx -t` 通过后才 reload。
- HTTP 自动 301 到 HTTPS；ACME challenge 路径保持匿名可访问，避免自动续签被 BasicAuth 阻断。
- BasicAuth 用户名：`qms-experience`。密码明文只保存在本机 macOS 钥匙串：
  - 服务名：`qms.zhj-jc.com-basic-auth`
  - 账户名：`qms-experience`
  - 未写入聊天、Git 或交接文件；服务器临时明文文件已删除。

## 四、最终验证证据

| 验证项 | 结果 |
|---|---|
| `verify.sh` | PASS |
| `qms_validation_smoke.php` | passed |
| `record_forms_schema_smoke.php` | passed |
| `record_forms_print_smoke.php` | passed |
| `qms_experience_environment_smoke.php` | passed |
| `qms_experience_deploy_bootstrap_smoke.php` | passed |
| 外部 HTTP | 301 |
| 外部 HTTPS，无 BasicAuth | 401 |
| 外部 HTTPS，错误 BasicAuth | 401 |
| 外部 HTTPS，正确 BasicAuth | 200 |
| 页面体验环境警示 | 已显示 |
| ACME challenge 绕过 BasicAuth | 通过 |
| 现有网站（服务器内/本机外部） | 均为 HTTP 200 |
| Nginx 配置 | syntax is ok / test is successful |

验证过程中发现并修复一项权限问题：`.htpasswd` 虽为 `root:www 640`，但父目录最初为 `root:root 700`，Nginx 错误日志明确显示 `Permission denied`，导致提交 BasicAuth 时返回 500。将 `shared/` 调整为 `root:www 750` 后，无凭据/错误凭据均为 401，正确凭据为 200；`.env` 保持 600。

## 五、资源水位

| 项目 | 部署后值 |
|---|---|
| 应用容器内存 | 约 32 MiB |
| MySQL 容器内存 | 约 490 MiB |
| 系统可用内存 | 约 2.3 GiB |
| Swap | 1 GiB，总使用约 27 MiB |
| 系统盘 | 49 GiB，总使用约 11 GiB，可用约 36–37 GiB，23%–24% |
| Docker 镜像 | 2 个，约 2.024 GB |
| Docker volumes | 3 个，约 229 MB |

## 六、停点与未执行事项

任务 6 到此结束。以下属于任务 7 或后续 G4/运维演练，尚未执行：

- 未使用或修改 QMS 默认管理员密码；
- 未创建质量负责人和一般人员体验账号；
- 未写入任何模拟记录、附件或云端 PDF；
- 未向其他员工发送入口；
- 未执行云端模拟数据重启持久化、数据库快照恢复和 release 回退演练。

下一步必须单独批准任务 7；在此之前，公网入口虽已上线，但只有掌握本机钥匙串中 BasicAuth 密码的人可进入 QMS 登录页。
