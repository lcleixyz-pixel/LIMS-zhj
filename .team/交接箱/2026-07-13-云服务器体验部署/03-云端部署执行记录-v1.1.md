# 云服务器体验测试部署执行记录 v1.1

> 日期：2026-07-13  
> 执行依据：`2026-07-13-云服务器体验测试部署计划-v0.2.md`  
> 用户授权：批准任务 6 云端部署；版本控制管理由 Codex 直接执行  
> 结论：**任务 6 通过；审查修复版已成为云端现行版本；停止在任务 7 前**  
> 替代关系：v1.1 替代 v1.0；v1.0 保留为首次上线审计历史

## 一、现行部署版本

| 项目 | 实际值 |
|---|---|
| 服务器 | `101.200.41.200`，Alibaba Cloud Linux 3，`x86_64` |
| 体验入口 | `https://qms.zhj-jc.com` |
| Release ID | `20260713-2474de0` |
| Git 分支 | `codex/qms-cloud-experience` |
| Git 提交 | `2474de0` |
| GitHub PR | `#29`，已合并；merge commit `5aba81b39ed0289c357612cf10eb8705415da766` |
| 应用镜像 | `jewelry-qms-experience:amd64-2474de0` |
| MySQL 镜像 | `jewelry-qms-mysql:8.4-amd64` |
| current | `/www/server/jewelry-qms-experience/releases/20260713-2474de0` |
| 回退版本 | `20260713-be2a9cc`，release 与镜像均保留 |

## 二、升级前保护与发布校验

1. 增量代码包和应用镜像包在本机及服务器分别复核 SHA-256，结果均为 `OK`：
   - 代码包：`a4aa6797239419cbea37063a59787cc6c5c3e13e422a2c6899c06065207a07c3`
   - 应用镜像包：`946591e0387f24faa231dffc850a0a5810ecc340f0b53c1bdff94d58256081ec`
2. 切换前生成数据库快照：`shared/snapshots/20260713-2474de0-before-update.sql.gz`。
   - 权限：`root:root 600`
   - 大小：15,607 bytes
   - `gzip -t`：通过
   - `CREATE TABLE`：70 条
   - 导出结束标记：存在
3. 数据库业务账号没有 `PROCESS` 权限，因此使用 MySQL 官方支持的 `--no-tablespaces` 参数导出，不为业务账号扩大权限。
4. 远端脚本执行时为 `docker compose exec` 显式断开标准输入，防止其消费后续校验脚本。前两次未完成的快照尝试均发生在版本切换前，未改变运行服务；最终快照覆盖无效中间文件后才放行。
5. 新镜像由本地离线包执行 `docker load`，服务器未构建、未访问 Docker Hub；镜像检查为 `architecture=amd64 os=linux`。
6. 校验清单保存在 `shared/release-manifests/20260713-2474de0-SHA256SUMS.txt`；服务器 `/tmp` 中本次上传文件及诊断临时文件已清理。

## 三、代码审查修复

初版 `be2a9cc` 的体验初始化只统计未软删除模板，不能严格表达“模板表为空”。审查后改为统计模板表全部行数，只有总行数为 0 才装入内置模板。云端升级后的 bootstrap 输出为：已有 9 个模板，跳过初始化；未产生重复模板。

迁移文件实际数量复核为 **20**，已在本地候选记录 v1.1 中更正。

## 四、最终验证证据

| 验证项 | 结果 |
|---|---|
| `current` 与镜像 tag | `20260713-2474de0` / `amd64-2474de0` |
| `verify.sh` | PASS |
| 5 项冒烟测试 | 5/5 passed |
| 数据库 | healthy；70 张表 |
| 模板 / 实例 | 9 / 0 |
| 应用端口 | 仅 `127.0.0.1:18010` |
| MySQL 宿主机端口 | 无 |
| 外部 HTTP | 301 |
| HTTPS，无凭据 / 错误凭据 | 401 / 401 |
| HTTPS，正确 BasicAuth | 200 |
| 页面体验环境警示 | 已显示 |
| 现有网站 `https://zhj-jc.com` | 200 |
| Nginx | 配置语法与测试通过 |
| TLS 证书 | `qms.zhj-jc.com`；Let's Encrypt YR2；有效至 2026-10-11 |

权限复核：`.env` 为 `root:root 600`，`.htpasswd` 为 `root:www 640`，TLS 私钥为 `root:root 600`。BasicAuth 用户名为 `qms-experience`；密码明文只保存在本机 macOS 钥匙串服务 `qms.zhj-jc.com-basic-auth`，未进入 Git、聊天或文档。

## 五、资源与回退

- 系统盘约 49 GiB，使用约 11 GiB，可用约 36 GiB。
- Docker 当前 3 张镜像、2 个运行容器、3 个持久卷；数据库卷未重建。
- 如后续版本异常，可把 `current` 与 `QMS_IMAGE_TAG` 退回 `20260713-be2a9cc` / `amd64-be2a9cc` 后重新启动；旧 release 和镜像未删除。
- 本次只创建并验证了升级前数据库快照，尚未执行恢复演练；恢复演练仍属于后续批准范围。

## 六、停点

任务 6 到此结束。未修改 QMS 默认管理员密码、未创建体验账号、未写入模拟记录或附件、未向员工发送入口，也未执行数据库恢复和 release 回退演练。下一步必须单独批准任务 7。

## 七、版本控制收口

PR #29 在云端同版本验证通过后合并进入 `main`。合并前 GitHub 状态为 `MERGEABLE / CLEAN`，head 为 `2474de0de44d17acbb709ec99b48fe994898c713`；合并后复核 `origin/main` 已包含该修复提交。远程功能分支按合并命令清理，本地隔离 worktree 暂时保留用于审计与必要复查。
