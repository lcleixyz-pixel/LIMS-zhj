# 云服务器体验测试部署：G2-B 本地候选包验证记录 v1.1

> 日期：2026-07-13
> 状态：**通过；作为云端现行发布候选**
> 替代关系：v1.1 替代未带版本号的初始记录；初始记录保留作审计历史

## 一、版本与审查修复

| 项目 | 实际值 |
|---|---|
| Git 分支 | `codex/qms-cloud-experience` |
| Git 提交 | `2474de0de44d17acbb709ec99b48fe994898c713` |
| Release ID | `20260713-2474de0` |
| 应用镜像 | `jewelry-qms-experience:amd64-2474de0` |
| MySQL 镜像 | `jewelry-qms-mysql:8.4-amd64` |
| GitHub PR | `#29` |

代码审查发现原初始化守卫只统计“未软删除模板”，不等价于“模板表为空”。若表内只有软删除模板，重复初始化可能再次写入内置模板。v1.1 已改为按模板表总行数判断：只有总行数为 0 才初始化；有任意历史模板行均跳过。该修复先修改测试并确认旧实现失败，再修改实现并通过回归。

同时更正初始记录中的数量：`database/migrations/` 实际为 **20 个迁移文件**，不是 19 个。

## 二、最终验证

- 最终应用镜像为 `linux/amd64`；本机完成构建，`npm audit --omit=dev` 为 0 个漏洞。
- `qms_experience_deploy_bootstrap_smoke.php` 已验证“按表总行数守卫初始化”。
- 以下 5 项冒烟测试全部通过：
  - `qms_validation_smoke.php`
  - `record_forms_schema_smoke.php`
  - `record_forms_print_smoke.php`
  - `qms_experience_environment_smoke.php`
  - `qms_experience_deploy_bootstrap_smoke.php`
- PHP 语法检查与 `git diff --check` 通过。

## 三、增量离线发布包

本机目录：

`/Users/lc.leixyz/Documents/AI工作台/05-Codex输出归档/2026-07-13-QMS云服务器体验部署/G2B候选发布包/`

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `LIMS-zhj-experience-20260713-2474de0.tar.gz` | 10,592,089 bytes | `a4aa6797239419cbea37063a59787cc6c5c3e13e422a2c6899c06065207a07c3` |
| `LIMS-zhj-experience-app-image-20260713-2474de0.tar.gz` | 492,989,974 bytes | `946591e0387f24faa231dffc850a0a5810ecc340f0b53c1bdff94d58256081ec` |
| `LIMS-zhj-experience-20260713-2474de0-SHA256SUMS.txt` | 230 bytes | 包含以上两项校验值 |

两个 gzip 文件完整，`shasum -a 256 -c` 两项均为 `OK`。本增量包只更新应用镜像；MySQL 继续使用已在初始候选包中验证的 `jewelry-qms-mysql:8.4-amd64`。

## 四、边界

该候选只用于体验测试部署，不等同于正式生产发布。默认管理员密码修改、体验账号创建、模拟记录写入、数据库恢复演练和 release 回退演练仍需按后续闸门另行执行。
