# 质量工作台改造交接报告 v0.1

## 结论

已在 8021 测试环境完成第一版“质量工作台”底座装配。

- 新增入口：`/quality-workbench`
- 已生成系统评审项目：8 个
- 已生成任务：12 条
- 当前状态：7 个可验收，1 个被阻断
- 阻断项：`岗位职责评审` 未读取到有效职责链版本
- 文件链路结论：65 份 GOV-TRIAL/0.3、29 个要素、119 条制度—要素链路、254 条章节链路均满足工作台校验；仍保留语义人工复核任务

本结论只表示 8021 测试环境内的技术试装状态，不构成纸质体系正式结论、不触碰 8010、不发布、不签署、不迁移真实记录。

## 已实现

1. 新增三张轻量表：
   - `quality_review_projects`
   - `quality_review_tasks`
   - `quality_review_events`
2. 新增服务：
   - `QualityWorkbenchService`
3. 新增命令：
   - `php think qms:refresh-quality-workbench`
   - 写入必须加：`--apply --ack-quality-workbench`
4. 新增页面：
   - `/quality-workbench`
   - `/quality-workbench/projects`
   - `/quality-workbench/projects/view?id=...`
5. 新增顶部菜单：
   - `质量工作台`
6. 接入的第一版来源：
   - 文件链路
   - 记录表单模板
   - 职责链版本
   - 设备与计量
   - 人员能力
   - 内审
   - 管理评审
   - CAPA / 不符合 / 投诉 / 外部变化

## 8021 写入结果

第一次写入：

```json
{
  "created_projects": 8,
  "updated_projects": 0,
  "created_tasks": 12,
  "updated_tasks": 0,
  "soft_deleted_tasks": 0,
  "events": 20
}
```

第二次重跑：

```json
{
  "created_projects": 0,
  "updated_projects": 8,
  "created_tasks": 0,
  "updated_tasks": 12,
  "soft_deleted_tasks": 0,
  "events": 0
}
```

说明：重跑不会新增重复对象。

## 数据库核对

```json
{
  "documents_03": 65,
  "trial_ready_templates": 104,
  "record_instances": 13,
  "projects": 8,
  "tasks": 12,
  "events": 20
}
```

项目状态：

```json
[
  {"status": "blocked", "c": 1},
  {"status": "ready_for_acceptance", "c": 7}
]
```

任务状态：

```json
[
  {"severity": "blocker", "status": "open", "c": 1},
  {"severity": "info", "status": "review_required", "c": 2},
  {"severity": "warning", "status": "review_required", "c": 9}
]
```

重复任务键：0。

## 验证

已通过：

```bash
php tests/qms_final_candidate_trace_sync_smoke.php
php tests/qms_final_candidate_document_access_smoke.php
php tests/qms_quality_workbench_smoke.php
```

规则验证：

- 有 blocker 时不能验收通过
- `waive` / 保留处理必须填写说明
- `--apply` 未带 `--ack-quality-workbench` 会拒绝写入

页面验证：

- `/quality-workbench`：可加载
- `/quality-workbench/projects`：可加载
- `/quality-workbench/projects/view?id=<文件链路项目>`：可加载

回归页面验证：

- `/dashboard/index`
- `/planning/index`
- `/document/index`
- `/planning/traceability`
- `/record_form_template/index`

上述页面均未出现“系统发生错误”。

## 证据文件

- 数据库快照：`数据库快照/8021-before-quality-workbench-20260823.sql.gz`
- 写入报告：`质量工作台写入报告.json`
- 干跑报告：`质量工作台干跑报告.json`
- HTTP 页面证据：`验证证据/*-v2.html`

## 待人工验收重点

1. 在浏览器打开 `http://127.0.0.1:8021/quality-workbench`。
2. 看首页是否能直观看到：
   - 今日待办
   - 阻断事项
   - 可验收项目
   - 质量风险
   - 最近变更
3. 打开 `文件链路评审`，确认它是否能回答：
   - 这页在评什么
   - 当前结论是什么
   - 下一步谁处理什么
4. 打开 `岗位职责评审`，确认 blocker 是否清楚提示“缺有效职责链版本”。
5. 不要把 8021 的“项目验收”理解成纸质体系正式发布；它只是系统内工作台项目状态。
