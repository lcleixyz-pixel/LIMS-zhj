# 四角色中文友好整改实现计划

> **面向 AI 代理的工作者：** 按本计划在当前会话内逐任务实施；每个行为先写失败测试，再写最少实现，最后在 8021 浏览器复验。

**目标：** 修复四角色审计中的关键阻断，并让主要页面使用清晰、可执行的中文业务语言。

**架构：** 保留现有控制器、服务、模板和权限架构；新增小型展示服务承担 Markdown 阅读化、文件状态中文化和通知展示；环境监控字段从治理蓝图源头修正，并通过受限脚本只回填 8021 的指定 SIM 模板。

**技术栈：** ThinkPHP 8、PHP 8.4、Bootstrap、MySQL 8.4、现有 PHP smoke 测试、浏览器实操。

---

## 文件结构

- 创建 `jewelry-qms/app/service/QmsReadableMarkdownService.php`：安全渲染治理 Markdown。
- 创建 `jewelry-qms/app/service/DocumentPresentationService.php`：修订说明、结构状态和在线编辑可用性。
- 创建 `jewelry-qms/app/service/NotificationPresentationService.php`：通知中文类型、对象和操作文案。
- 创建 `jewelry-qms/app/view/document/controlled_print_form.html`：受控打印事前确认。
- 创建 `jewelry-qms/database/migrations/20260728_controlled_print_recipient.sql`：接收人字段。
- 创建 `jewelry-qms/scripts/qms_four_role_ux_trial_backfill.php`：仅在试运行环境回填指定模板。
- 创建 `jewelry-qms/tests/qms_four_role_chinese_ux_smoke.php`：静态交互契约。
- 创建 `jewelry-qms/tests/qms_four_role_chinese_ux_runtime_smoke.php`：服务和数据口径回归。
- 修改相关控制器、视图、蓝图服务、字段服务、路由及 compose 挂载。

### 任务 1：先锁定四类缺陷

- [ ] 编写 `qms_four_role_chinese_ux_smoke.php`，断言个人待签、正确筛选表达式、中文连续正文、受控打印确认页、在线编辑门禁和新人页面文案。
- [ ] 扩充 G2 第二批蓝图 smoke，断言环境监控月份、日期、温湿度、符合性、记录人和条件必填配置。
- [ ] 运行两个测试，确认因功能缺失而失败。

### 任务 2：技术负责人个人待办与筛选

- [ ] 把首页待审数字改为当前用户 `pending` 审批数。
- [ ] 为文件列表增加 `pending_for_me=1` 查询，首页待办直接进入本人队列。
- [ ] 修正模板条件表达式的括号，防止一个筛选值让多个 `<option>` 同时选中。
- [ ] 运行任务 1 测试和现有 UX smoke，确认通过。

### 任务 3：质量员文件阅读

- [ ] 为 Markdown 服务编写标题、清单、表格、引用和转义测试并确认失败。
- [ ] 实现安全 Markdown 阅读化。
- [ ] 将连续正文页改为“可阅读正文”，技术路径移入折叠区。
- [ ] 将 JSON 修订说明与结构状态转换为中文摘要。
- [ ] 运行新旧结构化文件 smoke。

### 任务 4：通知中心

- [ ] 为通知展示服务编写中文类型、对象标题、动作文案测试并确认失败。
- [ ] 通知按创建时间倒序获取，保留未读状态但不以内部 ID 排序。
- [ ] 表格改为清晰的通知卡片，显示完整内容、业务类型、时间和唯一主操作。
- [ ] 运行通知 smoke。

### 任务 5：新人记录员

- [ ] 修正 G2 第二批环境监控字段蓝图。
- [ ] 为字段服务增加 `month`、单位及 `required_when` 校验测试并确认失败。
- [ ] 实现月份、单位、必填、逐格标签和条件必填。
- [ ] 将记录列表改名为“已填记录”，主入口改为“开始填一张新表”。
- [ ] 运行受限回填脚本，只更新 8021 的 `SIM-XZTC/BG-02-01`。
- [ ] 用浏览器验证保存草稿的错误提示和成功下一步。

### 任务 6：文件管理员修订与受控打印

- [ ] 新增打印准备页和路由；文件详情只链接准备页，不立即写日志。
- [ ] 增加用途、份数、接收人、确认勾选的服务端校验。
- [ ] 增加接收人迁移并仅在 8021 执行。
- [ ] 仅在真实可用时显示在线编辑。
- [ ] 发起修订页显示拟生成版本、旧版保留和后续签批说明。
- [ ] 对 Markdown/文本附件渲染完整受控打印正文；其他格式如实提示后续动作。
- [ ] 运行文件控制、受控打印和修订 smoke。

### 任务 7：8021 四角色复验

- [x] 运行 PHP 语法检查、专项 smoke、既有 UX/文件/记录 smoke。
- [x] 重载 8021 应用代码，不重建或写入 8010。
- [x] 质量员复验文件列表、文件详情和连续正文。
- [x] 技术负责人复验首页待签、本人队列和通知。
- [x] 新人记录员复验环境监控表。
- [x] 文件管理员复验修订、在线编辑门禁和打印确认。
- [x] 归档浏览器截图、整改报告和剩余项。
