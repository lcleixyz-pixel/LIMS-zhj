# P1-2 追溯选择项版本与乱码治理实施验收

日期：2026-07-28

环境：8021 治理试运行沙箱

结论：通过

## 完成内容

- 追溯对象统一显示来源、版本、中文状态和业务名称。
- 外部条款、手册章节和记录表格默认只显示本文件候选；其他对象通过“显示历史/其他版本”展开。
- 同编号同标题程序文件优先显示 `GOV-TRIAL/0.2`，其他版本仍可展开选择。
- 记录模板候选使用确定性版本解析，不再依赖数据库返回顺序；治理目标版本为 `GOV-TRIAL/0.2`。
- 含 Unicode 替换字符 `�` 的对象不进入新增关系选择器。
- 已被隔离的候选不能形成“已带入但选择框无对象”的伪成功提示。
- JavaScript 不可用时，所有非乱码选项仍保留在原生 HTML 选择框中。

## 非破坏性边界

- 没有删除、重命名、合并或换挂数据库对象。
- 13 条疑似乱码外部条款未进入新增选择器。
- 18 条引用这些条款的既有历史关系仍保留并可在“当前追溯关系”中查看。
- 有效追溯关系在自动化和浏览器验收前后均为 3182 条。
- 未提交新增关系表单，未迁移真实运行记录，未写入或改动 8010。
- 纸质体系仍是唯一正式体系。

## 真实数据治理结果

| 检查项 | 结果 |
|---|---:|
| 手册同号同标题重复组 | 18 |
| 记录模板同规范编号重复组 | 2 |
| 程序文件同编号同标题重复组 | 1 |
| 隔离的疑似乱码外部条款 | 13 |
| 保留的乱码条款历史关系 | 18 |

## 8021 页面验收

以 CX-03-02“相关记录”内容块实测：

| 检查 | 结果 |
|---|---|
| 记录表格默认范围 | 显示 9 个本文件候选，97 个历史/其他版本默认折叠 |
| BG-30-04 版本区分 | 可展开看到 `GOV-TRIAL/0.2 / 草稿` 与 `GOV-TRIAL/0.1 / 试运行就绪` |
| 手册搜索 `6.4` | 默认只显示 `SIM-XZTC/SC / GOV-TRIAL/0.1 / 试运行就绪 / 6.4 设备` |
| 展开手册历史 | 可看到 `XZTC/SC / 第四版 / 已发布 / 6.4 设备` |
| 关闭历史开关 | 已选第四版仍保持显示和选中，不被静默清空 |
| 程序文件版本区分 | 默认 `GOV-TRIAL/0.2`，展开后可见 `A/2` |
| 外部条款全量 | 475 条非乱码可选项，`�` 数量为 0 |
| 页面警示 | 显示“已隔离 13 条疑似乱码外部条款；已有历史关系未删除” |

## 自动化与审查证据

- 5 个 PHP 文件语法检查通过。
- 19 项追溯、候选、治理工作台及运行时回归通过。
- 真实数据运行结果：外部条款候选 16、手册章节候选 2、记录表格候选 9。
- 全量候选定向保持 939 项可定向、0 项阻断。
- 语义治理状态保持 `missing_primary=19`、`suspected_mismatch=18`。
- 独立代码复核第一轮发现 2 项 Important，均已补失败测试并修复；第二轮确认无 Critical/Important。

## 版本记录

- `10918da9`：实施计划
- `de8e5370`：版本、状态与乱码治理规则
- `025469e7`：历史/其他版本渐进展开
- `dd151c09`：确定性候选版本解析与隔离候选预填拦截

## 下一步

文件仍在治理换版中，软件功能已经具备。下一项宜做 P1-3：把“当前追溯关系”从宽表改为按主链、职责、运行入口和辅助关系分组的中文卡片，减少复核人员横向找列的负担；仍保持只改显示、不批量改写关系。
