# Hermes 验收问题修复与复核

- 日期：2026-07-26
- 环境：`http://127.0.0.1:8021`
- 边界：仅治理试运行模拟环境；未修改 8010 数据库。

## 已修复

1. 日期字段不再显示 `{}`，按模型业务格式显示，例如 `2026-07-26`。
2. 业务页面字段名称与治理字段标签不一致时，使用唯一原值安全定位，标题更正现已投影到原表。
3. `Training Plan Id` 改为“所属培训计划”。
4. “要批注的字段”改为“要更正的字段”，“原值（冻结保留）”改为“原记录（不可修改）”。
5. 成功、警告和错误提示增加辅助技术可识别的提示角色。
6. 通知数字增加“未读通知”说明。

## 验证结果

- 10 项修改治理及记录更正自动测试全部通过。
- PHP 语法检查及 `git diff --check` 通过。
- 8021 页面复核：
  - 培训日期回填为 `2026-07-26`；
  - 标题原值和当前有效值同时显示；
  - 通知入口显示为“10 条未读通知”；
  - 冻结拦截提示已进入辅助功能树。

## 保留边界

本次修复不把 Hermes 原报告升级为“全项目验收通过”。记录模板修订、记录表格实例专用更正链和直接删除拦截仍应按集中验收说明补充人工抽查。
