# 记录详情 UI 速赢实现计划 v0.1

> **面向 AI 代理的工作者：** 使用 executing-plans 在现有 8021 挂载分支内实施；按 TDD 先红后绿，只提交本任务文件。

**目标：** 降低记录详情页误操作风险和视觉噪声，同时保留现有更正审批及 PDF 链。

**架构：** 控制器只收紧 PDF 生成状态；详情模板负责操作分组、阅读/批注模式和更正类型默认值。无数据库迁移、无新依赖。

**技术栈：** ThinkPHP 8、Bootstrap 5、原生 JavaScript、PHP smoke 测试、8021 浏览器验收。

---

### 任务 1：冻结原始 PDF 生成入口

**文件：**
- 修改：`jewelry-qms/app/controller/RecordFormInstance.php`
- 测试：`jewelry-qms/tests/record_forms_instance_smoke.php`

- [x] 增加失败断言：`canExportPdf()` 只允许 `draft`。
- [x] 运行实例 smoke，确认因 `generated` 仍可生成而失败。
- [x] 将 PDF 生成条件收紧为记录状态必须是 `draft`。
- [x] 重跑实例 smoke，确认通过。

### 任务 2：详情页阅读与操作速赢

**文件：**
- 修改：`jewelry-qms/app/view/record_form_instance/view.html`
- 测试：`jewelry-qms/tests/qms_record_correction_decision_smoke.php`

- [x] 增加失败断言，要求存在：
  - `modal-dialog-scrollable`
  - `recordAnnotationModeToggle`
  - `annotation-mode-active`
  - “原始 PDF 已冻结”
  - “更多”
  - 非空原值默认 `amendment`
- [x] 运行决策 smoke，确认因新交互缺失而失败。
- [x] 重排顶部操作；移除已生成记录的生成和禁用编辑入口。
- [x] 增加阅读/批注模式切换，默认隐藏编辑提示。
- [x] 增加更正类型智能默认及可滚动弹窗。
- [x] 重跑决策 smoke，确认通过。

### 任务 3：8021 实页与回归验收

**文件：**
- 更新：`.team/交接箱/2026-07-26-记录字段级更正/记录详情UI速赢实现计划-v0.1.md`

- [x] 在 1280×720 页面确认主操作、更多菜单和冻结状态。
- [x] 切换批注模式，确认蓝色标记只在开启后出现。
- [x] 点已有备注字段，确认默认“修改内容（保留原值）”。
- [x] 点新增整行，确认固定“补充内容”。
- [x] 确认弹窗底部按钮完整可见。
- [x] 取消全部面板，不提交申请；核对数据库计数和文件哈希不变。
- [x] 运行更正、当前状态 PDF、实例及打印相关 smoke。
- [x] 只提交本任务文件并推送现有 Draft PR。

## 验收记录

- 视口：1280×720。
- 顶部可见：`下载当前状态 PDF`、`申请更正`、`原始 PDF 已冻结`、`更多`、`返回`；不再出现 `生成PDF` 和禁用 `编辑`。
- 默认阅读模式：7 个“点击批注”标记全部隐藏；进入批注模式后显示 7 个字段标记和 1 个新增整行入口。
- 已有备注字段：原值为“仅限8021，不作为正式运行记录”，默认类型为 `amendment`。
- 新增整行：默认类型为 `supplement`，其余类型禁用，显示 5 个结构化字段。
- 弹窗：正文区域可滚动；底部按钮下沿为 691px，小于 720px 视口高度。
- 数据未变：结构化更正申请仍为 2 条，更正记录仍为 3 条；最近时间分别保持 `2026-07-26 03:38:11`、`2026-07-26 03:38:12`。
- 原字段值 SHA-256 保持 `0c117514c9f038dc27c85d42ec18f36e6ddf2e95aaac5d61a538ce58905eacb3`。
- 原始 PDF SHA-256 保持 `bfbca84c6735ab6305858238584cf157df390ccdcc050f025e3611a20d94e9c3`。
- 6 项 smoke 全部通过：字段级更正、审批、当前状态 PDF、归档包、实例、打印。

## 版本记录

| 版本 | 日期 | 变更 | 状态 |
|---|---|---|---|
| v0.1 | 2026-07-26 | 记录详情 UI 速赢执行计划及实页验收 | 已完成并纳入现有 Draft PR |
