# Hermes 补充集中验收任务单

- 日期：2026-07-26
- 执行人：Hermes
- 环境：`http://127.0.0.1:8021`
- 待验版本：Git 提交 `e6ff3065`
- 前置报告：`Hermes-集中验收结果与UX问题清单-v0.1.md`
- 修复记录：`Codex-Hermes验收问题修复与复核-v0.1.md`

## 一、任务卡

- **我要做什么**：补验记录表格实例更正链、记录模板换版边界、冻结记录删除拦截，并复核本轮修复。
- **材料在哪里**：本任务单列出的 8021 地址和两个既有报告。
- **不能碰什么**：不得访问或写入 8010；不得改代码、数据库、账号、密码和权限；不得操作非 `SIM` 数据。
- **做完怎么算成功**：每个步骤都有页面事实和证据，结果分别标为“通过 / 不通过 / 阻断 / 未测试”，不得用局部检查替代全项结论。

## 二、执行纪律

1. 只通过 8021 页面操作，使用现有 `sim_preparer`、`sim_approver` 账号。
2. 不使用 `admin` 绕过权限；账号无入口时记为“阻断”。
3. 允许批准下文指定的既有 SIM 更正申请；除此以外不得新增、删除、作废或修改数据。
4. 每一步记录：
   - 登录账号；
   - 页面地址；
   - 操作前状态；
   - 实际操作；
   - 操作后状态；
   - 申请编号或记录编号；
   - 截图或页面文字证据。
5. 页面没有看到的内容不得根据代码、旧报告或实施方说明推定为“通过”。
6. 原报告保持不变，另行提交补充验收结果。

## 三、任务 A：记录表格实例专用更正闭环

### A-1 指定对象

- 记录地址：
  `http://127.0.0.1:8021/record_form_instance/view?id=aa16b7d2-c15c-418e-b8e1-e98441fe880b`
- 记录编号：`SIM-XZTC/BG-35-03`
- 待处理申请：`1ade0856-ba82-424f-8103-4cf7bd0c98e9`
- 目标字段：记录日期
- 原记录：`2026-07-25`
- 更正值：`2026-07-26`
- 原因：填写错误

### A-2 批准

1. 以 `sim_approver` 登录并打开指定记录。
2. 确认待处理卡片明确显示：
   - 申请编号；
   - 字段；
   - 原记录；
   - 更正值；
   - 原因；
   - 申请人和时间。
3. 只处理申请 `1ade0856-ba82-424f-8103-4cf7bd0c98e9`。
4. 处理意见填写：
   `Hermes补充验收：记录日期更正经核对通过`
5. 点击批准。

### A-3 回检

批准后逐项确认：

- 申请状态变为已批准；
- 原记录 `2026-07-25` 仍保留；
- 当前有效值显示为 `2026-07-26`；
- 更正记录显示申请编号、更正人、批准人、时间和原因；
- 之前已有的更正记录未丢失；
- 页面没有出现通用“当前有效状态与更正链”面板，仍使用记录实例专用更正区；
- “下载当前状态 PDF”中的记录日期为 `2026-07-26`；
- “下载原始 PDF”中的记录日期仍为 `2026-07-25`；
- “打印更正附页”能看出 `2026-07-25 → 2026-07-26`。

### A-4 判定

上述任一关键项缺失，任务 A 判为“不通过”；PDF 无法打开或生成时不得以页面显示代替 PDF 验证。

## 四、任务 B：记录模板修订/换版边界

### B-1 指定对象

- 模板地址：
  `http://127.0.0.1:8021/record_form_template/view?id=7f25f19b-bf3a-4bed-ab7d-290e644be35d`
- 模板编号：`SIM-XZTC/BG-35-03`
- 模板名称：`[治理试运行] 试剂耗材处置记录表`
- 当前版本：`GOV-TRIAL/0.1`
- 当前状态：试运行就绪

### B-2 只读检查

不得点击“停止使用”、作废、删除、批准或编辑，只检查：

- 页面不显示通用字段更正面板；
- 已进入试运行的模板不提供直接删除；
- 已进入试运行的模板不允许原地覆盖式编辑；
- 页面是否提供明确的“复制为新版本 / 发起换版 / 修订草稿”等入口；
- 如提供换版入口，是否能说明旧版保留、新版编号或版本、来源版本和换版说明；
- 历史记录实例是否仍指向填报时的模板快照。

### B-3 判定

- 只有看到了可执行的新版本路径和旧版保留证据，才能判为“通过”。
- 如果只有“停止使用”“作废/换版”文字，但没有实际新版本入口或版本关联证据，判为“不通过”，不得根据文字提示推定链路已完成。

## 五、任务 C：冻结记录直接删除拦截

### C-1 指定对象

- 记录详情：
  `http://127.0.0.1:8021/training/view?id=8c7d2600-0000-4000-8000-000000000001`
- 直接删除地址：
  `http://127.0.0.1:8021/training/delete?id=8c7d2600-0000-4000-8000-000000000001`

### C-2 操作

1. 以 `sim_preparer` 打开详情页，确认记录存在且状态为已冻结。
2. 记录当前标题及两条既有更正记录。
3. 访问上面的直接删除地址。
4. 预期结果：
   - 自动返回记录详情；
   - 显示“不能删除，请提交更正并保留原始记录”类提示；
   - 记录仍能打开；
   - 学时和标题两条既有更正记录仍存在。

### C-3 异常处理

如果记录真的被删除，立即停止后续操作，把任务 C 判为“不通过”，完整记录页面结果，不要自行新建记录补回。

## 六、任务 D：本轮修复复核

在培训记录详情页确认：

- 选择“培训日期”后，原记录显示 `2026-07-26`，不得显示 `{}`；
- 培训主题原值带删除线，当前有效标题显示“（修订）”；
- 下拉字段显示“所属培训计划”，不再显示 `Training Plan Id`；
- 表单显示“要更正的字段”和“原记录（不可修改）”；
- 通知数字能读出“未读通知”含义；
- 冻结拦截提示正文可在页面辅助功能结构中读取。

## 七、交付要求

### 结果文件

保存为：

`.team/交接箱/2026-07-26-记录字段级更正/Hermes-补充集中验收结果-v0.1.md`

### 证据目录

截图或导出证据保存到：

`.team/交接箱/2026-07-26-记录字段级更正/Hermes-补充集中验收证据/`

### 结果表

结果文件必须包含：

| 任务 | 结果 | 直接证据 | 未通过/阻断原因 |
|---|---|---|---|
| A 记录实例专用更正闭环 | 通过/不通过/阻断/未测试 | 申请编号、页面、PDF | - |
| B 记录模板修订/换版边界 | 通过/不通过/阻断/未测试 | 页面入口、版本证据 | - |
| C 冻结记录删除拦截 | 通过/不通过/阻断/未测试 | 返回页面、提示、记录仍存在 | - |
| D 修复项复核 | 通过/不通过/阻断/未测试 | 页面文字或截图 | - |

最终结论必须按实际结果写：

- A、B、C、D 全部通过：可写“本次补充验收通过”；
- 任一项不通过：写“本次补充验收不通过”，列出缺陷；
- 任一项阻断或未测试：写“证据不足，不能判定全部通过”。

不得再使用“核心看起来正常”“实施方已验证”“自动测试通过”等内容代替 Hermes 自己的页面验收证据。
