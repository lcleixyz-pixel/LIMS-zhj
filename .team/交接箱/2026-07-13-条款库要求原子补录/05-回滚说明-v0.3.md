# 条款库要求原子补录回滚说明

本次写入已经通过写后哈希、数量和幂等复核，当前不需要回滚。只有后续发现原文选择错误或业务口径被正式推翻时，才进入回滚评审。

## 回滚材料

- 写前备份：`backup-before-clause-remediation-20260713.sql`
- 备份 SHA-256：`cef96b1354ad7c6819867e86f40b9a935847bfe70a66cc7b0a0d9af87f2383ea`
- 备份范围：`qms_sources`、`qms_clauses`、`qms_clause_texts`

## 红线

1. 不得直接把 SQL 全库灌回当前库；备份包含三张表的写前完整状态，直接覆盖可能抹掉补录后的其他合法变更。
2. 回滚前必须先再备份当前状态，并逐行生成“仅撤销本次 62 条 + 删除本次等同说明”的差异脚本。
3. 回滚脚本必须在隔离库验证计数和外键/关联影响，再经用户明确批准后执行。
