# 条款库要求原子补录执行与验证记录

- 执行日期：2026-07-13
- 执行系统：当前运行中的 jewelry-qms
- 写入方式：单事务、要求原子白名单、同编号冲突阻断

## 写库前

- SQL 备份大小：237,407 字节，161 行。
- SQL 备份 SHA-256：`cef96b1354ad7c6819867e86f40b9a935847bfe70a66cc7b0a0d9af87f2383ea`。
- 预演：`total=62 / insert=62 / existing=0 / conflict=0`。
- 来源分布：GB/T 27025 为 7，G001 为 37，A015 为 18。
- 排除复核：GB `3.1–3.9` 为 0；A015 `2.1–2.8` 为 0。

## 写库后

- 二次预演：`total=62 / insert=0 / existing=62 / conflict=0`。
- CNAS-CL01 等同说明状态：`existing`。
- 62 条均有 `qms_clause_texts` 原文，数据库文本 SHA-256 与核验 Markdown 提取结果一致。
- 活动条款中，同一来源同一编号重复数：0。
- 受控提取方式：`reviewed_markdown_requirement_atom`。

## 系统可用性

- `/login`：HTTP 200。
- `/planning/clauses` 未登录访问：HTTP 302，按预期跳转登录。
- `qms_clause_remediation_smoke.php`：通过。
- `qms_clause_remediation_live_verify.php`：通过。
- `qms_planning_ui_smoke.php`：通过。
- `qms_planning_schema_smoke.php`：通过。

## 已知环境差异

旧的 `qms_planning_service_smoke.php` 会检查仓库清单中的私有 PDF，但当前 `参考/` 路径已经拆分且该 PDF 不在原位置，因此该旧测试在本次改动前即失败。本次没有把它冒充为通过；补录使用的是用户提供并随包归档的 Markdown 原文。
