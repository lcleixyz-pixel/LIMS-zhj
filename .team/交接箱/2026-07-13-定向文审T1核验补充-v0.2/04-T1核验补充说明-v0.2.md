# T1 核验补充说明

- 版本：v0.2
- 日期：2026-07-13

## 核验材料

1. 用户提供的 `markdown.zip`，共 38 份 Markdown，压缩完整性检查无错误。
2. 当前运行中的 jewelry-qms：`qms_sources` 与 `qms_clauses`，全程只读查询。
3. Claude 回传的 `T1-点名册对账清单-v0.1.md` 与 `定向文审进度卡-v0.1.md`。

## 解析规则

- 同时捕获 Markdown 标题编号、正文行首编号和 `5.5a)` 这类字母子项。
- 标题编号允许直接接中文、括号等字符，不以“编号后必须有空格”为前提。
- 只接受 1–8 开头的准则编号，排除年份、页码和正文引用。
- CL01 的 `7.8.1.3、7.8.3.2、8.6.2` 因 OCR 换行合并未形成独立行，已通过原文精确检索并结合等同采用关系恢复；未把正文中的普通交叉引用误计为新条款。
- 附件 4 从 HTML 表格单元格 `<p>编号</p>` 中提取，并保留星号条款的基础编号。
- 所有编号去重后再与数据库集合比较。

## 对 v0.1 的修正

| 项目 | v0.1 | v0.2 | 修正原因 |
|---|---:|---:|---|
| GB/T 27025 官方编号 | 193 | **194** | `7.8.2` 标题编号后直接接括号，被旧规则漏掉 |
| GB/T 27025 系统差额 | -15 | **-16** | 逐号集合差确定为 16 项 |
| G001 官方完整条款标识 | 45 | **63** | 旧规则漏掉无空格节点和字母子项 |
| A015 官方完整条款标识 | 39 | **42** | 漏计 `6.2.5c)、7.7.1a)、7.7.1j)` |
| 附件 4 | 27 + 疑似章级 1 | **28，逐号一致** | 已确认系统编号 `2`，集合差为 0 |
| CL01 去向 | 不适用/等同指针 | **适用 + 等同映射** | “不适用”会造成错误的合规语义 |
| T1 状态 | 已完成但另有 T1b | **核验完成，系统整改未执行** | 消除状态矛盾 |

## 证据等级

- GB/T 27025：原文遍历 + 数据库逐号集合差，闭合。
- CL01：原文遍历 + 与 GB/T 27025 结构比较；CL01 共 197 个编号，等同映射方案需用户在未来系统变更前确认。
- G001/A015：原文遍历；系统为 0，补录规模闭合。
- 附件 4：原文与数据库逐号集合完全相等，闭合。

## 未执行事项

本核验没有生成预导入包，也没有执行补录、映射、改名或数据库写入。若后续要修条款库，应另开受控系统整改任务，先出预览和回滚说明，再由用户授权。

当前代码内置的 G001/A015 模型纲要与本次原文完整标识集合并不完全相同，后续不得直接以现有模型纲要写库；应先按本包清单校准导入源和字段语义。
