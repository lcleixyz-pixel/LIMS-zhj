# Codex 全量 UX 审计核实、整改与 8021 验收

**日期：** 2026-07-27

## 1. 任务边界

- 审计来源：`jewelry-qms/.team/交接箱/2026-07-27-全量UX审计36项问题清单.md`
- 实施目标：核实问题后整改，并入当前项目主体，在 8021 治理试运行环境验证。
- 禁止范围：不修改、不迁移、不重启 8010 正式运行栈；不把 8021 试运行数据作为正式体系证据。

## 2. 总体结论

| 分类 | 数量 | 结论 |
|---|---:|---|
| 需要代码整改 | 21 | 已实施并在 8021 验收 |
| 已修复、清单过期或当前未复现 | 11 | 保留现状，不重复造功能 |
| 不采用原建议或留待口径明确 | 4 | 记录理由，不以扩大权限或破坏审计语义换取表面通过 |

本轮还通过真实页面复验发现并修复了两个更深层原因：

1. 技术负责人已有设备查看岗位权限，但“资源管理”父菜单仍按旧模块权限隐藏，现已统一父子菜单权限。
2. 改密地址未命中受保护路由时会绕过页面上下文；现已统一小写路由、固定兼容路由优先级，并为改密动作增加控制器级认证和“重置他人密码”二次授权。

## 3. 已实施的 21 项

- `NEW-4`：图表空状态改为安全渲染组件，消除脚本闭合符号泄露。
- `NEW-14`：更正待办改为“更正申请中心”；申请人看本人进度，审批岗位看待处理事项。
- `NEW-15`：不扩大批准人设备权限；仪表盘隐藏设备死链并说明责任岗位。
- `NEW-21`：新增 `equipment_period_checks` 幂等迁移、缺表降级、设备下拉和可见范围过滤。
- `NEW-22`：合规缺口按目标动作权限显示入口；无权时显示责任岗位和下一步。
- `NEW-23`：资源管理父菜单、设备台账、期间核查统一使用岗位动作权限。
- `NEW-5 / L2`：仪表盘和审计点名列表的空状态补充可执行下一步。
- `NEW-9`：培训类型中文化。
- `NEW-13`：记录 PDF 状态改为“已生成（不可直接编辑）”。
- `NEW-17`：空关联字段显示“未关联”。
- `NEW-18`：所有登录用户可进入本人改密；兼容地址受认证保护；重置他人密码需用户管理写权限。
- `NEW-24`：内审计划、员工、用户列表改用业务表头。
- `NEW-25`：评审决议不再暴露“员工 ID”字段名。
- `NEW-27`：审计点名页面统一使用引导型空状态。
- `L1`：冻结状态增加 `🔒` 和明确视觉标识。
- `NEW-2`：环境说明由紧急警报改为状态信息。
- `NEW-3`：登录页支持配置试运行支持联系人。
- `NEW-10`：通用删除确认补充“删除后无法恢复”。
- `NEW-19`：Copilot 模式改为“通用问答 / 当前页面问答 / 评审专家”。
- `NEW-20`：文件详情以文件标题为主、编号为辅。

## 4. 核实后未重复整改的 11 项

- `S1 / M1 / M2 / M3`：第一轮成果仍有效。
- `M4`：当前重定向后会重新查询待处理申请，页面复验未再出现旧列表不刷新。
- `NEW-6`：“选择模板填写”当前已是主色主按钮。
- `NEW-11`：两类 SIM 账号当前通知操作能力一致，原描述已过期。
- `NEW-16`：实际视觉已有“原值删除线 + 当前值强调”；DOM 文本采集把两者拼接成连续文本。
- `NEW-26`：系统已存在 `critical / major / minor` 三级严重度，8021 规则数据也覆盖三级。
- `NEW-28`：文件结构化页现已有总体摘要和工作台引导。
- `L3`：属于外部浏览器自动化工具兼容，不是应用缺陷。

## 5. 明确不按原建议实施的 4 项

- `NEW-1`：三个 SIM 账号的环境口令与数据库哈希当前一致。不增加启动时自动改密，避免掩盖配置漂移。
- `NEW-7`：保留“资源管理”顶层菜单，通过“设备与计量 / 人员与培训 / 供应商”分组降低复杂度，不拆成更多顶层菜单。
- `NEW-8`：“本月待办”尚无单一、稳定的计数口径，不展示可能误导的 badge。
- `NEW-12`：通知“已读”是状态写入，继续使用 POST，不改成可被预取或收藏触发的 GET。

## 6. 8021 实际验收证据

### 环境和迁移

- 登录页：HTTP 200。
- `equipment_period_checks`：迁移前不存在，迁移后存在，共 11 列、3 个索引。
- 幂等复跑：迁移前后记录数均为 0，没有重复或破坏数据。
- 8021 数据库：运行中且健康。
- 8021 应用：重建后运行中。

### 三岗位页面

- `sim_preparer`
  - 可登录；仪表盘不显示设备死链，显示“需设备管理或技术负责人岗位查看”。
  - “修改密码”和“更正申请中心”均正常打开。
  - 更正中心不显示审批按钮。
  - 直接访问期间核查仍被岗位权限拦截。
- `sim_reviewer`
  - 可登录；显示“资源管理 / 设备台账 / 期间核查计划”。
  - 期间核查页正常打开，数据库已初始化，不再出现 500。
- `sim_approver`
  - 可登录；不显示设备和期间核查入口。
  - 更正申请中心正常打开；当前没有待处理申请，因此页面没有决策表单。
  - 服务级核对确认最高管理者具备更正决策权限。
- 三类账号页面均未记录浏览器控制台错误。

### 改密安全边界

未登录访问下列大小写兼容地址均返回 302 并转向 `/login/index`：

- `/user/changepassword`
- `/user/changePassword`
- `/login/changepassword`
- `/login/changePassword`

### 自动化

- 16 个相关测试文件全部通过。
- 动作权限契约 `A01-A16` 全部通过。
- 设备场所契约 `ESC01-ESC04` 全部通过。
- 设备调拨契约 `ET01-ET05` 全部通过。
- 本轮涉及的 16 个 PHP 文件全部通过语法检查。
- `git diff --check 2f7247c9...HEAD` 通过。

## 7. 8010 边界核对

本轮前后容器身份和启动时间未变化：

- `jewelry-qms-app-1`
  - ID：`ca494e28eb529563800d0190daa62f39320f3c3357f9dd0747a71f79126ecb21`
  - 启动时间：`2026-07-20T18:44:38.948916001Z`
- `jewelry-qms-db-1`
  - ID：`7829f75096994e5639b5efc29a8a0e1e50238dcf5a462c1b8ac39d4557ce9fd2`
  - 启动时间：`2026-07-18T03:23:07.605780677Z`

结论：8010 应用和数据库未被本轮重建、重启或迁移。

## 8. 版本与主要产物

- 当前主体分支：`codex/semantic-trace-guard-mvp`
- 本轮合并后提交：`26a22625`
- 设计：`docs/superpowers/specs/2026-07-27-全量UX审计整改-design.md`
- 实施计划：`docs/superpowers/plans/2026-07-27-全量UX审计整改.md`
- 回归契约：`jewelry-qms/tests/qms_ux_audit_remediation_smoke.php`
- 数据迁移：`jewelry-qms/database/migrations/20260727_equipment_period_checks.sql`

## 9. 当前保留边界

- 期间核查“新增计划”写入没有用现有三个 SIM 账号强行验证，因为三者都不是设备管理员；没有为了测试扩大岗位任命。
- 当前 8021 期间核查表为空，这是正常初始状态。
- 登录页支持联系人显示为责任角色，不记录个人联系方式。
- 纸质体系仍是唯一正式体系；8021 继续用于治理试运行和软件迭代。
