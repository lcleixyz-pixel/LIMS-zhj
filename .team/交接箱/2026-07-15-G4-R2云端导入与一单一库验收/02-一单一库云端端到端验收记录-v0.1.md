# “一单一库”云端端到端验收记录 v0.1

## 结论

“一单一库”候选已进入云端体验环境的隔离候选池，状态保持 `pending`，来源信任保持 `unverified`，未 promoted 为正式外部变更事件，未自动修改任何正式受控对象。

## 候选信息

- 候选 ID：`reg-2026-samr-14-yidanyiku`
- 标题：市场监管总局关于对检验检测机构资质认定实施“一单一库”管理的公告
- 监测运行：`g4r2-run-yidanyiku-20260715`
- 运行方式：`manual`
- 运行状态：`completed`
- 来源信任：`unverified`
- 候选状态：`pending`
- 初步适用性：`needs_review`
- 相关性：`high`
- promoted_event_id：`NULL`

## 六类影响结果

| 影响类别 | 云端候选结论 | 验收口径 |
|---|---|---|
| 能力项目 / CMA 标志 | `likely` | 识别到能力项目对照、CMA 标志控制影响，需人工确认 |
| 体系文件 | `likely` | 识别到外来文件、体系文件和模板复核影响，需人工确认 |
| 人员授权 | `possible` | 识别到授权边界和岗位职责复核影响，证据不足处需人工核实 |
| 设备校准 | `no_match` | 仅表示现有规则未命中；不得解释为“不适用” |
| LIMS 规则 | `likely` | 识别到候选隔离、禁止自动改对象的系统规则影响 |
| 培训 | `likely` | 识别到相关岗位培训/宣贯影响，不能自动生成培训完成记录 |

## 候选写入边界

本次只写入：

1. `qms_regulatory_monitor_runs`：1 条 G4-R2 手工运行记录；
2. `qms_external_change_candidates`：1 条 pending 候选记录。

本次未写入、未修改：

1. `qms_external_change_events`：仍为 0；
2. 体系文件 `documents`：仍为 39；
3. 员工 `employees`：仍为 12；
4. 记录模板 `record_form_templates`：仍为 145；
5. 记录实例 `record_form_instances`：仍为 12；
6. 结构化文件 `qms_structured_documents`：仍为 190。

证据文件：

- `evidence/yidanyiku-e2e-result.txt`
- `evidence/yidanyiku-relevance-adjustment.txt`
- `evidence/yidanyiku-formal-counts-before.tsv`
- `evidence/yidanyiku-formal-counts-after.tsv`

## 中间问题与处理

1. `source_mode` 枚举不一致。
   - 候选包来源模式来自外部 JSON，云端表只接受 `html_list` / `manual_only`。
   - 本次作为人工上传复核包，映射为 `manual_only`。
2. `relevance` 枚举不一致。
   - 候选包相关性字段为中文说明句，云端表只接受 `high` / `medium` / `low` / `unknown`。
   - 首次保守映射为 `unknown`，随后依据“需纳入质量管理体系外部依据候选复核”的语义调整为 `high`。
3. 两次失败写入均在事务提交前被数据库拒绝，无候选或运行记录残留。

## 后续人工确认事项

1. 官方来源核验：需人工确认官方正文、附件、发布日期、生效日期和是否现行。
2. 机构适用性：需结合本机构业务范围、两场所授权项目、CMA 备案和实施日期人工确认。
3. 受控变更：如确认适用，再由质量负责人/技术负责人决定是否进入正式外部变更事件、体系文件修订、培训和模板调整流程。

