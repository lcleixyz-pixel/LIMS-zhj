# G4-R2 问题台账 v0.1

| 编号 | 问题 | 级别 | 状态 | 处理 |
|---|---|---|---|---|
| G4R2-01 | 云端校验 `.sha256` 时引用本地路径 | 低 | 已处理 | 改用 `SHA256SUMS.basename.txt` 校验上传包 |
| G4R2-02 | 快照 `current-file-list.txt` 首次为空 | 低 | 已处理 | `current` 为符号链接，改用 `find -L` 补全并重算 SHA |
| G4R2-03 | 管理员 bcrypt 哈希经 shell 写入时被 `$` 展开破坏 | 中 | 已处理 | 通过 MySQL 标准输入重写哈希，登录验证通过 |
| G4R2-04 | 候选包 `source_mode` 与云端枚举不一致 | 低 | 已处理 | 人工上传复核包映射为 `manual_only` |
| G4R2-05 | 候选包 `relevance` 为中文说明句，与云端枚举不一致 | 低 | 已处理 | 先保守映射，后调整为 `high` 并保留证据 |
| G4R2-06 | “一单一库”来源信任仍为 `unverified` | 中 | 留待人工 | 需人工核验官方正文、附件和机构适用性 |
