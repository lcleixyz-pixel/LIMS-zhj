# G4-R10 测试残留清单确认与执行记录 v0.1

日期：2026-07-16

## 结论

G4-R10 已完成。本轮没有硬删除业务数据，而是对唯一仍生效的 smoke 变更日志执行归档式清理：

- `qms_document_change_logs.id = 1a4ae7b8-2662-4004-9ad5-4a237a15a53f`
- `revision_note = 组合包影响清单跳转 smoke：XZTC/CX-26-2022 记录要求`
- 处理方式：`soft_delete` 从 `0` 更新为 `1`

随后重新生成组合包，最新组合包从 v5 升为 v6，`latest_change_impact_count = 0`。

## 清理前清单

当前库中：

| 对象 | 当前状态 |
|---|---|
| `EQ-B-001` / `B组验收测试设备` | 0 条活动残留 |
| `TEST-LINK-20260709-191600` 记录实例 | 0 条活动残留 |
| `TEST-LINK-20260709-191600` 文件 | 0 个残留文件 |
| `QP-COVERAGE-SMOKE` / `记录要求覆盖缺口程序` | 0 条活动残留 |
| `qms_document_change_logs` 中活动 smoke 变更日志 | 1 条 |

证据：

- `evidence/before-smoke-change-logs.tsv`

## 备份

全库备份：

- `jewelry-qms/runtime/private-backups/g4-r10-clean-20260716-0215/local-before-g4-r10-full.sql`
- SHA256：`3917c73a0a9648cafd4fdd520726203991dbe68e4ba99f0fe457ac54f9347154`

组合包相关备份：

- `jewelry-qms/runtime/private-backups/g4-r10-clean-20260716-0215/manifest-before-g4-r10.json`
- `jewelry-qms/runtime/private-backups/g4-r10-clean-20260716-0215/qms_system_package_v5-before-g4-r10.md`

说明：备份目录位于 `runtime/private-backups/`，不应提交到版本库。

## 执行动作

执行最小更新：

```sql
UPDATE qms_document_change_logs
SET soft_delete = 1,
    modified = NOW()
WHERE id = '1a4ae7b8-2662-4004-9ad5-4a237a15a53f'
  AND soft_delete = 0
  AND revision_note = '组合包影响清单跳转 smoke：XZTC/CX-26-2022 记录要求';
```

命中结果：

```text
affected_rows = 1
```

随后重新生成体系文件组合包。

## 生成结果

最新组合包：

```json
{
  "latest_package_version": 6,
  "latest_archive_path": "runtime/qms_structured/system_package/archive/qms_system_package_20260716_094731_69582843.md",
  "latest_content_sha256": "1e99cdef5df8b3af7f7cb4130faffc41f05bfc14be48eff6bcf8c561d8bc6dec",
  "latest_change_impact_count": 0,
  "latest_block_trace_count": 795,
  "rows": 795,
  "historical_snapshot_rows": 0,
  "bad_edit_urls": 0,
  "bad_review_urls": 0
}
```

证据：

- `evidence/after-package-v6-summary.json`
- `evidence/after-clean-counts.tsv`
- `evidence/after-testlink-files.txt`

## 复验

自动化烟测：

```bash
docker compose -f jewelry-qms/compose.yaml exec -T app php tests/qms_g4r9_stale_link_guard_smoke.php
```

结果：

```text
qms_g4r9_stale_link_guard_smoke passed
```

```bash
docker compose -f jewelry-qms/compose.yaml exec -T app php tests/qms_g4r7_trial_readiness_smoke.php
```

结果：

```text
qms_g4r7_trial_readiness_smoke passed
```

组合包文件检索：

```bash
rg -n "组合包影响清单跳转 smoke|QP-COVERAGE-SMOKE|TEST-LINK-20260709-191600|EQ-B-001|B组验收测试设备" \
  jewelry-qms/runtime/qms_structured/system_package/qms_system_package.md \
  jewelry-qms/runtime/qms_structured/system_package/archive/qms_system_package_20260716_094731_69582843.md
```

结果：未检出。

## 浏览器页面复验说明

本轮尝试打开 `/planning/structures/package` 做浏览器截图时，浏览器会话跳转到登录页，说明当前浏览器登录态失效；为避免触碰账号/密码，本轮未将浏览器截图作为证据。验收依据改用后端只读摘要、组合包文件检索和自动化烟测。

## 未做事项

- 未部署云端。
- 未清理云端数据。
- 未硬删已 `soft_delete=1` 的历史 smoke 日志。
- 未删除历史组合包归档。
