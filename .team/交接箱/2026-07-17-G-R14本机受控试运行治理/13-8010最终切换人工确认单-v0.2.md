# 8010 最终切换人工确认单

> 版本：v0.2
> 状态：等待用户人工批准
> 修订原因：本机现用 8010 为工作区源码挂载，不是既有 `current` 发布结构。

## 技术前置条件

- [x] PR #33 与 G‑R14 回归通过
- [x] 开放 P0 为 0
- [x] 15 项模板端到端通过
- [x] 岗位越权和场所越权测试通过
- [x] 8011 重建后关键页面无 500 和乱码
- [x] 8011 验收后数据库快照恢复演练通过
- [x] 8010 当前仍可用且未被 G‑R14 修改
- [x] 已准备不挂载 `/app` 的本机不可变镜像发布 Compose
- [x] 已用自动测试约束快照、`current`、回退和云端边界
- [ ] 生成 8010 切换当刻的数据库、上传目录、容器配置和源代码状态快照
- [ ] 构建并隔离验证现用 8010 的不可变回退镜像
- [ ] 创建 Git 外本机发布根目录，让 `current` 先指向已验证旧 release
- [ ] 用户最终人工确认

## 用户批准语句

如人工检查 8011 后认可，可明确回复：

> 批准 G-R14 最终切换：先冻结现用 8010 为不可变回退 release，并生成升级前快照和校验；
> 再通过本机 Git 外 current 指针只替换 app 容器。失败立即回退，必要时恢复数据库快照。
> 不得部署云端。

未收到上述或同等明确授权前，不构建现用回退镜像、不创建生效 `current`、不执行迁移、
不停止或替换 8010。
