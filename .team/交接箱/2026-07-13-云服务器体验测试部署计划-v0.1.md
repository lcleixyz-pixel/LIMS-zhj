# 云服务器体验测试部署实施计划 v0.1

> **面向 AI 代理的工作者：** 必需子技能：使用 `executing-plans` 逐任务实施；每个涉及云服务器写入、软件安装、端口调整或域名配置的任务，必须先取得用户单独授权。步骤使用复选框（`- [ ]`）跟踪进度。

**目标：** 在不影响云服务器现有网页的前提下，将 `jewelry-qms` 部署为供实验室人员体验、熟悉和测试的临时环境。

**架构：** 云服务器上的宝塔 Nginx 继续承载现有网页，并通过独立测试子域名反向代理到仅监听 `127.0.0.1:18010` 的 Jewelry QMS 容器。应用与 MySQL 使用独立 Docker Compose 项目、独立网络和持久卷；只使用模拟数据，不承载正式业务数据。Mac 仍是开发环境，云端只接收本地验证通过的发布包。

**技术栈：** 宝塔 Linux 面板 11.1.0、Nginx、Docker Engine、Docker Compose、ThinkPHP 8、PHP 8、MySQL 8.4、Node.js/Chromium、HTTPS。

---

## 任务卡

- **我要做什么：** 建立一套可通过浏览器访问的云端 Jewelry QMS 体验测试环境。
- **材料在哪里：** `/Users/lc.leixyz/Documents/AI工作台/01-项目代码/LIMS-zhj`；目标云服务器为现有宝塔 Linux 服务器。
- **不能碰什么：** 不覆盖现有网页，不导入真实生产数据，不开放 MySQL 到公网，不在云端直接开发，不将 `.env` 或密码写入 Git。
- **做完怎么算成功：** 现有网页保持可访问；测试子域名可登录；核心填报、上传、打印/PDF 可用；容器重启后模拟数据仍存在；页面明确标识“体验测试环境”；有更新与回退方法。

## 当前已知条件

| 项目 | 已知值 | 规划判断 |
|---|---:|---|
| 云服务器 | 阿里云，宝塔 Linux 面板 11.1.0 | 可作为体验测试候选机 |
| CPU | 2 核，用户提供的监控峰值约 3.66% | 小规模体验预计可用，仍需部署后观察 |
| 内存 | 4 GiB，用户提供的已用约 0.65 GiB | 只常驻一套体验环境；测试版不再并行常驻第二套 |
| 系统盘 | 50 GiB，用户提供的已用约 7.58 GiB | 初期可用；镜像、日志、上传和备份必须设容量观察 |
| 公网带宽 | 200 Mbps | 体验访问足够 |
| 现有负载 | 一个网页 | 上线前记录现有站点入口，部署前后各验证一次 |

## 本阶段明确不做

- 不作为正式生产系统，不录入检测报告、客户资料、人员身份证明或受控体系原件。
- 不建设集群、高可用、双机热备和复杂监控平台。
- 不将宝塔面板、SSH、MySQL、容器内部端口全面开放公网。
- 不把 Mac 本机的现用数据库或 `public/uploads/` 直接搬到云端。
- 不在云服务器目录内直接编辑业务代码。
- 不执行 QMS 正式受控发布、真实审批或生产库写入。

## 执行闸门

| 闸门 | 条件 | 未满足时 |
|---|---|---|
| G0 计划批准 | 用户批准本计划 | 只保留计划，不连接服务器 |
| G1 只读体检 | 操作系统、架构、Docker、端口、磁盘和现有网页基线已记录 | 不安装、不部署 |
| G2 变更授权 | 用户单独批准云服务器写入、子域名和宝塔 Nginx 配置 | 不执行服务器变更 |
| G3 隔离验证 | Compose 配置通过；数据库无公网端口；应用仅监听回环地址 | 不开放测试域名 |
| G4 体验验收 | 现有网页正常；登录、填报、上传、PDF、重启持久化通过 | 不交给其他人员使用 |

## 实施阶段将创建或修改的文件

> 下列文件是后续执行计划时的目标；本次仅编写计划，不创建这些部署文件。

- 创建：`jewelry-qms/deploy/experience/compose.yaml` — 云端体验环境专用 Compose。
- 创建：`jewelry-qms/deploy/experience/.env.example` — 非敏感配置键清单。
- 创建：`jewelry-qms/deploy/experience/nginx.conf.example` — 宝塔 Nginx 反向代理参考片段。
- 创建：`jewelry-qms/deploy/experience/verify.sh` — 部署后只读验证脚本。
- 创建：`jewelry-qms/deploy/experience/README.md` — 发布、更新、快照和回退说明。
- 创建：`jewelry-qms/tests/qms_experience_environment_smoke.php` — 体验环境标识 smoke。
- 修改：`jewelry-qms/config/qms.php` — 从环境变量读取体验环境标签与提示。
- 修改：`jewelry-qms/app/middleware/Auth.php` — 向登录后的主布局提供体验环境标签。
- 修改：`jewelry-qms/app/controller/Login.php` — 向登录页提供体验环境标签。
- 修改：`jewelry-qms/app/view/layout/main.html` — 显示体验环境横幅。
- 修改：`jewelry-qms/app/view/login/index.html` — 登录页显示“禁止录入正式或敏感数据”。
- 修改：`docs/DEPLOYMENT.md` — 增加体验测试部署入口及与正式生产的边界。

---

### 任务 1：只读体检云服务器并建立现有网页基线

**文件：**
- 创建：`.team/交接箱/2026-07-13-云服务器体验部署/01-服务器只读体检记录.md`

- [ ] **步骤 1：取得只读体检授权和连接方式**

记录服务器公网地址、SSH 登录方式、现有网页 URL、拟用测试子域名。不要在计划或交接文件中记录密码、私钥、宝塔安全入口和完整访问令牌。

- [ ] **步骤 2：记录操作系统、架构和资源**

在服务器执行：

```bash
uname -a
uname -m
cat /etc/os-release
nproc
free -h
df -h /
```

预期：获得真实 OS、`x86_64`/`aarch64` 架构、CPU、内存和系统盘余量。若是 `aarch64`，停止并先验证 Chromium/镜像兼容性。

- [ ] **步骤 3：只读检查 Docker 与监听端口**

```bash
docker version
docker compose version
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Ports}}\t{{.Status}}'
ss -lntup
```

预期：明确 Docker/Compose 是否存在，以及 `80`、`443`、`18010` 是否冲突。若 Docker 不存在，停止在 G1，另行提交安装方案和授权请求。

- [ ] **步骤 4：记录部署前网页状态**

```bash
read -r -p '请输入已确认的现有网页 HTTPS 地址: ' EXISTING_SITE_URL
export EXISTING_SITE_URL
curl -fsSIL "$EXISTING_SITE_URL" | sed -n '1,12p'
```

预期：返回 HTTP `200` 或现有站点的正常重定向链；把状态码、时间和 URL 写入体检记录。

- [ ] **步骤 5：形成 G1 结论**

体检记录必须明确写出：Docker 是否可用、架构是否兼容、端口是否冲突、磁盘可用量、现有网页基线，以及进入 G2 前需要用户批准的实际变更清单。

---

### 任务 2：编写体验环境 Compose 与非敏感配置模板

**文件：**
- 创建：`jewelry-qms/deploy/experience/compose.yaml`
- 创建：`jewelry-qms/deploy/experience/.env.example`

- [ ] **步骤 1：先写 Compose 结构验证命令**

运行：

```bash
cd jewelry-qms
docker compose --env-file deploy/experience/.env.example \
  -f deploy/experience/compose.yaml config
```

预期：在文件尚未创建时失败，证明验证命令能捕获缺失部署配置。

- [ ] **步骤 2：创建体验环境 Compose**

`jewelry-qms/deploy/experience/compose.yaml` 使用以下完整结构：

```yaml
name: jewelry-qms-experience

services:
  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-jewelry_qms_experience}
      MYSQL_USER: ${MYSQL_USER:-jewelry_qms}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      TZ: Asia/Shanghai
    volumes:
      - db_data:/var/lib/mysql
      - ../../database/jewelry_qms.sql:/docker-entrypoint-initdb.d/01-jewelry-qms.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p\"$${MYSQL_ROOT_PASSWORD}\" --silent"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s
    networks:
      - qms_internal

  app:
    build:
      context: ../..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_DEBUG: "false"
      TZ: Asia/Shanghai
      DB_TYPE: mysql
      DB_HOST: db
      DB_NAME: ${MYSQL_DATABASE:-jewelry_qms_experience}
      DB_USER: ${MYSQL_USER:-jewelry_qms}
      DB_PASS: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      DB_PORT: "3306"
      DB_CHARSET: utf8mb4
      DEFAULT_LANG: zh-cn
      QMS_ENV_LABEL: 体验测试环境
      QMS_ENV_NOTICE: 禁止录入正式业务数据、客户资料或其他敏感信息
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
      PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH: /usr/bin/chromium-browser
    ports:
      - "127.0.0.1:${QMS_HOST_PORT:-18010}:8000"
    shm_size: "256mb"
    volumes:
      - runtime_data:/app/runtime
      - uploads_data:/app/public/uploads
      - node_modules:/app/node_modules
    networks:
      - qms_internal

networks:
  qms_internal:
    internal: true

volumes:
  db_data:
  runtime_data:
  uploads_data:
  node_modules:
```

关键检查：`db` 不得出现 `ports`；`app` 只能绑定 `127.0.0.1`；不得挂载云服务器源码目录到 `/app`。

- [ ] **步骤 3：创建非敏感环境模板**

`jewelry-qms/deploy/experience/.env.example`：

```dotenv
QMS_HOST_PORT=18010
MYSQL_DATABASE=jewelry_qms_experience
MYSQL_USER=jewelry_qms
MYSQL_PASSWORD=local-validation-password-not-for-server
MYSQL_ROOT_PASSWORD=local-validation-root-password-not-for-server
```

真实 `.env` 只在服务器生成；仓库根 `.gitignore` 已覆盖 `.env.*`，执行时仍需用 `git status --short` 确认密码文件未被追踪。

- [ ] **步骤 4：重新验证 Compose**

```bash
cd jewelry-qms
docker compose --env-file deploy/experience/.env.example \
  -f deploy/experience/compose.yaml config >/tmp/qms-experience-compose.rendered.yaml
rg -n '127\.0\.0\.1:18010|QMS_ENV_LABEL|internal: true' /tmp/qms-experience-compose.rendered.yaml
if rg -n 'published: "?3306"?' /tmp/qms-experience-compose.rendered.yaml; then
  echo 'FAIL: MySQL is published to host'
  exit 1
fi
```

预期：Compose 渲染成功，应用只绑定回环地址，数据库没有宿主机发布端口。

- [ ] **步骤 5：提交部署配置实现**

```bash
git add jewelry-qms/deploy/experience/compose.yaml \
  jewelry-qms/deploy/experience/.env.example
git diff --cached --check
git commit -m "feat(deploy): 增加云端体验环境配置"
```

---

### 任务 3：加入清晰的体验环境标识

**文件：**
- 创建：`jewelry-qms/tests/qms_experience_environment_smoke.php`
- 修改：`jewelry-qms/config/qms.php`
- 修改：`jewelry-qms/app/middleware/Auth.php`
- 修改：`jewelry-qms/app/controller/Login.php`
- 修改：`jewelry-qms/app/view/layout/main.html`
- 修改：`jewelry-qms/app/view/login/index.html`

- [ ] **步骤 1：编写失败的环境标识 smoke**

创建 `jewelry-qms/tests/qms_experience_environment_smoke.php`：

```php
<?php
declare(strict_types=1);

function assert_true(bool $condition, string $message): void
{
    if (!$condition) {
        fwrite(STDERR, "Assertion failed: {$message}\n");
        exit(1);
    }
}

putenv('QMS_ENV_LABEL=体验测试环境');
putenv('QMS_ENV_NOTICE=禁止录入正式业务数据、客户资料或其他敏感信息');

$config = require __DIR__ . '/../config/qms.php';
assert_true(($config['environment_label'] ?? '') === '体验测试环境', 'environment label comes from env');
assert_true(
    ($config['environment_notice'] ?? '') === '禁止录入正式业务数据、客户资料或其他敏感信息',
    'environment notice comes from env'
);

$auth = file_get_contents(__DIR__ . '/../app/middleware/Auth.php');
$login = file_get_contents(__DIR__ . '/../app/controller/Login.php');
$main = file_get_contents(__DIR__ . '/../app/view/layout/main.html');
$loginView = file_get_contents(__DIR__ . '/../app/view/login/index.html');

assert_true(str_contains($auth, "'environmentLabel'"), 'auth assigns environment label');
assert_true(str_contains($auth, "'environmentNotice'"), 'auth assigns environment notice');
assert_true(str_contains($login, "'environmentLabel'"), 'login assigns environment label');
assert_true(str_contains($login, "'environmentNotice'"), 'login assigns environment notice');
assert_true(str_contains($main, 'environmentLabel'), 'main layout renders environment label');
assert_true(str_contains($main, 'environmentNotice'), 'main layout renders environment notice');
assert_true(str_contains($loginView, 'environmentLabel'), 'login view renders environment label');
assert_true(str_contains($loginView, '禁止录入'), 'login view includes the no-real-data warning');

putenv('QMS_ENV_LABEL');
putenv('QMS_ENV_NOTICE');
$defaultConfig = require __DIR__ . '/../config/qms.php';
assert_true(($defaultConfig['environment_label'] ?? '') === '', 'default label is empty');
assert_true(($defaultConfig['environment_notice'] ?? '') === '', 'default notice is empty');

echo "qms_experience_environment_smoke passed\n";
```

测试覆盖：未设置变量时默认标签为空；设置 `QMS_ENV_LABEL` 和 `QMS_ENV_NOTICE` 时配置可读；登录控制器、认证中间件和两个页面模板都包含对应赋值或显示入口。

运行：

```bash
cd jewelry-qms
php tests/qms_experience_environment_smoke.php
```

预期：首次失败，指出配置键、视图赋值或模板标识尚不存在。

- [ ] **步骤 2：实现最小环境标识**

配置键固定为：

```php
'environment_label' => getenv('QMS_ENV_LABEL') ?: '',
'environment_notice' => getenv('QMS_ENV_NOTICE') ?: '',
```

`Auth.php` 的 `View::assign()` 数组增加：

```php
'environmentLabel' => $qmsConfig['environment_label'] ?? '',
'environmentNotice' => $qmsConfig['environment_notice'] ?? '',
```

`Login.php` 在返回登录视图前改用一次数组赋值：

```php
$qmsConfig = Config::get('qms', []);
View::assign([
    'systemTitle' => $qmsConfig['title'] ?? 'QMS',
    'environmentLabel' => $qmsConfig['environment_label'] ?? '',
    'environmentNotice' => $qmsConfig['environment_notice'] ?? '',
]);
```

登录页与登录后主布局仅在 `environmentLabel` 非空时显示黄色横幅，正文使用 `environmentNotice`。默认本机环境变量为空，因此不改变当前本机界面。

- [ ] **步骤 3：运行 smoke 与语法检查**

```bash
cd jewelry-qms
php -l config/qms.php
php -l app/middleware/Auth.php
php -l app/controller/Login.php
php tests/qms_experience_environment_smoke.php
```

预期：三个 PHP 文件语法检查通过，环境标识 smoke 输出 `qms_experience_environment_smoke passed`。

- [ ] **步骤 4：提交环境标识实现**

```bash
git add jewelry-qms/config/qms.php \
  jewelry-qms/app/middleware/Auth.php \
  jewelry-qms/app/controller/Login.php \
  jewelry-qms/app/view/layout/main.html \
  jewelry-qms/app/view/login/index.html \
  jewelry-qms/tests/qms_experience_environment_smoke.php
git diff --cached --check
git commit -m "feat(ui): 标识云端体验测试环境"
```

---

### 任务 4：编写 Nginx 反向代理、验证与运维说明

**文件：**
- 创建：`jewelry-qms/deploy/experience/nginx.conf.example`
- 创建：`jewelry-qms/deploy/experience/verify.sh`
- 创建：`jewelry-qms/deploy/experience/README.md`
- 修改：`docs/DEPLOYMENT.md`

- [ ] **步骤 1：创建宝塔 Nginx 参考片段**

反向代理目标固定为：

```nginx
location / {
    proxy_pass http://127.0.0.1:18010;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 180s;
    client_max_body_size 25m;
}
```

实际域名和证书由宝塔站点配置生成，示例文件不得写入真实域名、服务器 IP 或面板信息。

- [ ] **步骤 2：创建只读验证脚本**

`verify.sh` 必须执行并汇总：Compose `ps`、应用回环地址 HTTP 状态、数据库健康状态、磁盘使用、容器内 PHP 版本，以及 MySQL 未发布公网端口检查。脚本只读，不创建、删除或修改数据。

- [ ] **步骤 3：编写发布与回退说明**

README 固定采用以下目录约定：

```text
/www/server/jewelry-qms-experience/
├── current -> releases/<release-id>/
├── releases/
└── shared/
    ├── .env
    └── snapshots/
```

说明必须包含：首次部署、启动、停止、查看日志、创建测试快照、升级、切换 `current`、回退上一个 release，以及禁止在 `current` 内直接编辑代码。

- [ ] **步骤 4：更新总部署文档**

在 `docs/DEPLOYMENT.md` 增加“云端体验测试环境”入口，明确其不等于正式生产部署，并链接 `jewelry-qms/deploy/experience/README.md`。

- [ ] **步骤 5：验证 Shell 与文档**

```bash
bash -n jewelry-qms/deploy/experience/verify.sh
rg -n '体验测试|禁止录入|127\.0\.0\.1:18010|回退' \
  jewelry-qms/deploy/experience/README.md \
  jewelry-qms/deploy/experience/nginx.conf.example \
  docs/DEPLOYMENT.md
git diff --check
```

预期：Shell 语法通过；关键边界、端口和回退说明均可检索。

- [ ] **步骤 6：提交说明与脚本**

```bash
git add jewelry-qms/deploy/experience docs/DEPLOYMENT.md
git diff --cached --check
git commit -m "docs(deploy): 补充体验环境运维与回退说明"
```

---

### 任务 5：本地构建并验证候选发布包

**文件：**
- 产出：本机临时发布压缩包，不纳入 Git
- 产出：`.team/交接箱/2026-07-13-云服务器体验部署/02-本地候选包验证记录.md`

- [ ] **步骤 1：运行部署配置验证**

```bash
cd jewelry-qms
docker compose --env-file deploy/experience/.env.example \
  -f deploy/experience/compose.yaml config >/tmp/qms-experience-compose.rendered.yaml
```

预期：退出码为 `0`。

- [ ] **步骤 2：构建并启动本地候选环境**

使用只对本机有效的随机测试密码文件；该文件不得进入 Git：

```bash
cd jewelry-qms
cp deploy/experience/.env.example /tmp/qms-experience.env
python3 - <<'PY'
from pathlib import Path
import secrets
p = Path('/tmp/qms-experience.env')
s = p.read_text()
s = s.replace('local-validation-password-not-for-server', secrets.token_urlsafe(24), 1)
s = s.replace('local-validation-root-password-not-for-server', secrets.token_urlsafe(24), 1)
p.write_text(s)
PY
docker compose --env-file /tmp/qms-experience.env \
  -f deploy/experience/compose.yaml up -d --build
```

预期：`app` 与 `db` 健康启动，宿主机只出现 `127.0.0.1:18010`。

- [ ] **步骤 3：运行核心验证**

```bash
cd jewelry-qms
docker compose --env-file /tmp/qms-experience.env \
  -f deploy/experience/compose.yaml exec -T app php tests/qms_validation_smoke.php
docker compose --env-file /tmp/qms-experience.env \
  -f deploy/experience/compose.yaml exec -T app php tests/record_forms_schema_smoke.php
docker compose --env-file /tmp/qms-experience.env \
  -f deploy/experience/compose.yaml exec -T app php tests/record_forms_print_smoke.php
curl -fsS http://127.0.0.1:18010/login/index | rg '体验测试环境|禁止录入'
```

预期：三个 smoke 均输出 `passed`，登录页同时出现体验环境和禁止录入提示。

- [ ] **步骤 4：验证 PDF 与数据持久化**

先通过浏览器创建一条明确标记为“模拟”的记录并生成 PDF；随后执行：

```bash
docker compose --env-file /tmp/qms-experience.env \
  -f deploy/experience/compose.yaml restart
curl -fsSI http://127.0.0.1:18010/login/index | sed -n '1,8p'
```

预期：重启后登录页返回 `200`，模拟记录与生成的 PDF 仍能在页面中找到。

- [ ] **步骤 5：清理本地候选环境**

```bash
docker compose --env-file /tmp/qms-experience.env \
  -f deploy/experience/compose.yaml down -v
rm -f /tmp/qms-experience.env /tmp/qms-experience-compose.rendered.yaml
```

预期：只清理本地候选容器和卷，不影响现用 `8010` 环境。

- [ ] **步骤 6：生成可追溯发布包**

```bash
cd /Users/lc.leixyz/Documents/AI工作台/01-项目代码/LIMS-zhj
export RELEASE_ID="$(date +%Y%m%d%H%M%S)-$(git rev-parse --short HEAD)"
git archive --format=tar.gz \
  --output="/tmp/LIMS-zhj-experience-${RELEASE_ID}.tar.gz" HEAD
shasum -a 256 "/tmp/LIMS-zhj-experience-${RELEASE_ID}.tar.gz"
```

把 commit、文件名、SHA-256、验证命令与结果写入 `02-本地候选包验证记录.md`。

---

### 任务 6：经 G2 授权后部署到云服务器

**文件：**
- 创建：服务器 `/www/server/jewelry-qms-experience/releases/<release-id>/`
- 创建：服务器 `/www/server/jewelry-qms-experience/shared/.env`
- 修改：宝塔中新建的体验测试站点 Nginx 配置
- 产出：`.team/交接箱/2026-07-13-云服务器体验部署/03-云端部署执行记录.md`

- [ ] **步骤 1：再次确认 G2 授权**

向用户列明本次将发生的真实变更：上传发布包、创建目录、生成 `.env`、启动两个容器、创建测试子域名站点、申请 HTTPS 证书、修改该新站点的反向代理。没有明确批准不得继续。

- [ ] **步骤 2：保护现有网页基线**

部署前再次执行任务 1 的现有站点 `curl`，并在宝塔中备份现有站点 Nginx 配置。只新增测试站点，不编辑现有站点配置。

- [ ] **步骤 3：上传并解压发布包**

使用任务 5 记录的 `RELEASE_ID`、包名和服务器地址执行 `scp`；服务器端校验 SHA-256 一致后再解压。发布目录只读使用，运行数据全部进入 Docker volumes。

- [ ] **步骤 4：生成服务器专用密码文件**

在服务器执行：

```bash
install -d -m 700 /www/server/jewelry-qms-experience/shared
umask 077
openssl rand -base64 32
openssl rand -base64 32
```

两次输出分别写入服务器 `shared/.env` 的 `MYSQL_PASSWORD` 和 `MYSQL_ROOT_PASSWORD`；不要复制进聊天、交接文件或 Git。

- [ ] **步骤 5：启动体验环境**

```bash
cd /www/server/jewelry-qms-experience
ln -sfn "releases/${RELEASE_ID}" current
docker compose --env-file shared/.env \
  -f current/jewelry-qms/deploy/experience/compose.yaml up -d --build
docker compose --env-file shared/.env \
  -f current/jewelry-qms/deploy/experience/compose.yaml ps
```

预期：应用和数据库启动；数据库没有宿主机端口；应用只监听 `127.0.0.1:18010`。

- [ ] **步骤 6：创建宝塔测试站点**

在宝塔中新增已确认的测试子域名，配置 HTTPS 和任务 4 的反向代理片段。不得把 `18010` 加入阿里云安全组公网放行规则。

---

### 任务 7：创建体验账号和模拟数据

**文件：**
- 产出：`.team/交接箱/2026-07-13-云服务器体验部署/04-体验账号交付清单.md`

- [ ] **步骤 1：立即修改默认管理员密码**

首次登录使用初始化账号后立即修改密码；交接文件只写账号用途、角色和交付对象，不写明文密码。

- [ ] **步骤 2：按角色创建体验账号**

至少创建：质量负责人体验账号、一般人员体验账号。账号名称包含 `experience` 或中文“体验”标识，避免与未来正式账号混淆。

- [ ] **步骤 3：建立模拟数据命名规则**

所有体验数据标题统一以 `【模拟】` 开头；附件只上传专门制作的测试文件；不得使用真实客户、真实检测报告、身份证明或现行受控文件。

- [ ] **步骤 4：建立反馈入口**

在 `.team/不好用清单.md` 增加“云端体验测试”来源标记；人员反馈由质量负责人集中登记，不要求体验人员接触项目文件。

---

### 任务 8：执行 G4 验收并交付体验入口

**文件：**
- 产出：`.team/交接箱/2026-07-13-云服务器体验部署/05-体验环境验收记录-v0.1.md`

- [ ] **步骤 1：运行自动验证**

在服务器执行 `jewelry-qms/deploy/experience/verify.sh`，并运行任务 5 的三个核心 smoke。记录退出码和 `passed` 数量，不以“看起来正常”代替结果。

- [ ] **步骤 2：验证现有网页不受影响**

再次执行现有网页基线 `curl`。预期：与部署前保持相同的正常状态码或重定向链。

- [ ] **步骤 3：执行浏览器验收**

使用 Windows Edge/Chrome 和质量负责人远程设备分别检查：

- 登录页有“体验测试环境”和禁止录入提示。
- 质量负责人和一般人员账号均可登录。
- 仪表盘、记录模板、记录填写、本月待办可打开。
- 可保存一条 `【模拟】` 记录。
- 可上传测试附件。
- 可打开打印页并生成 PDF。
- 注销后不能访问需登录页面。

- [ ] **步骤 4：验证重启持久化**

重启 Compose 服务后，重新登录确认模拟记录、测试附件和 PDF 仍存在。只重启体验环境 Compose，不重启现有网页服务或整台服务器。

- [ ] **步骤 5：记录资源水位**

```bash
docker stats --no-stream
free -h
df -h /
docker system df
```

记录空闲内存、系统盘占用和容器资源；若 PDF 期间出现 OOM、持续交换或磁盘异常增长，不开放给其他人员，先回到调试阶段。

- [ ] **步骤 6：用户验收闸门**

把测试域名、账号角色、禁止事项、已通过项目和已知限制写入验收记录，交用户确认后才向其他人员发送体验入口。

---

### 任务 9：建立日常更新、快照和回退节奏

**文件：**
- 产出：`.team/交接箱/2026-07-13-云服务器体验部署/06-更新与回退演练记录.md`

- [ ] **步骤 1：更新前创建测试快照**

```bash
install -d -m 700 /www/server/jewelry-qms-experience/shared/snapshots
docker compose --env-file shared/.env \
  -f current/jewelry-qms/deploy/experience/compose.yaml exec -T db \
  sh -lc 'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"' \
  | gzip > "shared/snapshots/db-$(date +%Y%m%d%H%M%S).sql.gz"
```

预期：生成非空 `.sql.gz`；快照仅含模拟数据。

- [ ] **步骤 2：发布新版本**

本地完成验证后生成新的带 commit 标识发布包，上传到新的 `releases/<release-id>/`，构建完成后再切换 `current`，不得覆盖旧 release。

- [ ] **步骤 3：执行发布后 smoke**

切换后运行 `verify.sh`、登录页检查和三个核心 smoke；现有网页也要重新检查。

- [ ] **步骤 4：演练一次回退**

把 `current` 切回上一个 release，重新执行 Compose `up -d --build`，确认登录页和模拟数据可用；随后根据验收结果决定保持旧版还是重新切回新版。

- [ ] **步骤 5：确定体验期复盘点**

体验运行 1–2 周后整理：人员反馈、内存峰值、磁盘增长、PDF 成功率、使用频率。届时再决定继续使用云服务器、迁移 NAS，或升级为正式生产方案；本计划不自动升级为生产部署。

---

## 最终验收清单

- [ ] 现有网页部署前后状态一致。
- [ ] Jewelry QMS 使用独立测试子域名和 HTTPS。
- [ ] 应用只绑定 `127.0.0.1:18010`，MySQL 无公网端口。
- [ ] 页面明确显示“体验测试环境”和“禁止录入正式或敏感数据”。
- [ ] 默认管理员密码已修改，体验账号按角色区分。
- [ ] 登录、填报、附件、打印/PDF 和退出登录均已实测。
- [ ] 容器重启后模拟数据仍存在。
- [ ] 三个核心 smoke 有本次执行的 `passed` 证据。
- [ ] 更新前快照和 release 回退各演练一次。
- [ ] 云服务器资源水位和已知限制已记录。
- [ ] 用户确认后才向其他人员发送入口。

## 用户需要在执行前提供的最少信息

1. 现有网页访问地址。
2. 准备用于体验环境的子域名，或授权在现有域名下新增测试子域名。
3. 只读体检时采用的登录方式；优先临时 SSH 公钥，不在聊天中发送密码。
4. G1 体检报告完成后，对实际安装、上传、建站和启动容器的单独授权。

## 计划自检结论

- **范围：** 仅覆盖体验测试部署，不扩展到正式生产、真实数据迁移、NAS 选型或高可用。
- **隔离：** 现有网页、体验应用、体验数据库和运行数据边界已分别定义。
- **验证：** 包含本地候选验证、云端自动验证、浏览器验收、重启持久化及回退演练。
- **敏感信息：** 密码只在服务器本地生成和保存，不进入 Git、聊天或交接文档。
- **停点：** G1 之前只读；G2 之前不执行任何云服务器写入。
