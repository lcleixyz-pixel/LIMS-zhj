# 法规动态复核技能包验收记录

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.1 |
| 执行日期 | 2026-07-15 |
| 技能版本 | v0.1 |
| 真源目录 | `~/.agents/skills/qms-regulatory-change-monitor` |
| 依赖契约 | LIMS 导出 `schema_version` 1.0 |
| 验收状态 | 自动化验收通过；AionUi 人工导入待用户完成 |

## 1. 任务 4 端到端命令与结果

### 1.1 正例：一单一库

```sh
tmpdir="$(mktemp -d)"
cp "$HOME/.agents/skills/qms-regulatory-change-monitor/tests/fixtures/one-list-one-library.json" "$tmpdir/input.json"
python3 "$HOME/.agents/skills/qms-regulatory-change-monitor/scripts/validate_candidate.py" "$tmpdir/input.json"
python3 "$HOME/.agents/skills/qms-regulatory-change-monitor/scripts/build_review_packet.py" "$tmpdir/input.json" --output "$tmpdir/review.md"
rg -n "输入完整性|官方来源核验|六类影响解释|人工确认问题|禁止自动执行" "$tmpdir/review.md"
```

实际结果：

- 校验退出码 0，输出 `{"valid":true,"schema_version":"1.0","source_trust":"unverified"}`
- 构建退出码 0
- 五个固定章节均可定位：`输入完整性`、`官方来源核验`、`六类影响解释`、`人工确认问题`、`禁止自动执行事项`

### 1.2 全量 unittest

```sh
cd "$HOME/.agents/skills/qms-regulatory-change-monitor"
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

实际结果：`Ran 58 tests in ~11s`，`OK`。

### 1.3 敏感字段反例

```sh
python3 scripts/validate_candidate.py tests/fixtures/forbidden-secret.json
```

实际结果：

- 退出码 3
- stderr：`error_type=sensitive`，路径 `$.source.evidence.references[0].Pass-Word`
- 未打印敏感字段值（`NO_LEAK`）

### 1.4 第三方来源反例

```sh
python3 scripts/build_review_packet.py tests/fixtures/third-party-only.json --output /tmp/regulatory-third-party-review.md
rg -n "证据不足|未完成官方来源核验" /tmp/regulatory-third-party-review.md
```

实际结果：构建成功并明确降级，出现“未完成官方来源核验”“证据不足，需人工核实”及“官方补证清单”；不产生适用性肯定结论。

### 1.5 禁止数据库逻辑抽查

对 `SKILL.md` 与 `scripts/` 检索 `mysql` / 写库语句：未发现数据库连接或写入逻辑。

## 2. 人工抽查

| 抽查项 | 结果 |
|---|---|
| “一单一库”四个关键影响（CMA 范围标识、体系文件、人员授权、LIMS 规则） | 通过：报告与初步影响范围均出现，并引用输入中的官方链接与规则 ID |
| 第三方转载正确降级 | 通过 |
| 禁用字段拒绝且不泄露值 | 通过 |
| 始终保留人工确认闸门 | 通过：区分机器发现 / 规则初判 / 人工结论；禁止自动执行事项完整 |
| `no_match` 不表述为“不适用” | 通过：固定为“现有规则未命中” |

## 3. AionUi 同步

| 步骤 | 状态 |
|---|---|
| 在 AionUi 重新导入 `~/.agents/skills/qms-regulatory-change-monitor` | 待用户在 AionUi 完成人工导入 |
| 关闭并重新打开对应助手会话 | 待用户完成 |
| 用“请复核这份法规候选 JSON，并给出初步影响范围”触发 | 待用户完成 |
| 对照源目录 SHA-256 与 AionUi 可见文件确认版本一致 | 待用户完成 |

说明：AionUi 为复制导入机制，自动化测试不能替代人工重新导入。本项不得假报完成。

## 4. 结论与边界

技能包 v0.1 可供受控试用：可校验 schema 1.0 脱敏 JSON，并生成结构化复核建议。

本验收通过不等于：

- 云端 LIMS 已启用法规监测；
- 任一候选已获质量负责人批准；
- AionUi 副本已与真源同步；
- 已完成 CMA/CNAS 合规判定或受控文件修订。
