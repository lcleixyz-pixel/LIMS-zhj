# G4-R6 页面与交互审阅进度卡 v0.2

## 当前状态

已完成 v0.2 复审意见吸收，覆盖：

1. G4-R6 原始页面截图审阅结论。
2. 交叉复审意见中的分歧与新增发现。
3. 定向文审进度卡中 `GL011/GL012:2025` 换版事件的系统侧后置需求。
4. 本地代码/数据库的只读核验。
5. 本地残留验收草稿 `验证试填-20260709-182412` 的备份清理。

## 本次确认的关键调整

1. `较可能影响` 不应直接改成 `建议复核`；应保留“初步结论”，另加“建议动作”。
2. 菜单固定名保留“法规候选池”，待确认状态用数字角标。
3. 候选详情缺少处置按钮，已由代码核验确认，应列入 G4-R7 第一优先。
4. 仪表盘数据可信度问题升为 P0。
5. 记录模板重复编号和已发布模板“删除”按钮升为 P0。
6. 追溯矩阵“完整”应拆为“结构完整度 + 运行证据”。

## 本地清理补记

已备份并删除本地 1 条残留验收草稿：

- ID：`97208a65-e234-6912-a2cc-7d659a60a540`
- 标题：`验证试填-20260709-182412`
- 状态：`draft`
- 备份：`jewelry-qms/runtime/private-backups/g4r6-v02-review-20260715/record_form_instance_verify_trial_97208a65.sql`

清理后命中数：

- `record_form_instances` 中 `验证试填 / TEST-LINK-20260709`：0。

## 下一步建议

建议开启：

> G4-R7 试运行可信度与候选处置闭环专项

建议首批实施：

1. 仪表盘空数据与调试痕迹治理。
2. 法规候选池列表可读性与术语映射。
3. 候选详情处置按钮组、建议动作、已生效提示。
4. 正式变更事件空态说明与候选池跳转。
5. 记录模板重复编号警示和已发布模板“删除”改“作废/换版”。

## 当前边界

1. 不自动修改体系文件。
2. 不自动修改人员授权。
3. 不自动作废或换版模板。
4. 不自动生成培训完成记录。
5. 不把 `no_match` 解释为“不适用”。
