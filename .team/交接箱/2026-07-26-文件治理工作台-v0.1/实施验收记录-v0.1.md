# 文件治理工作台 v0.1 实施验收记录

日期：2026-07-26

状态：已合入 `main`，已在 8021 启用并复验通过

分支：`codex/file-governance-workbench-v01`

## 1. 任务卡

- 我要做什么：把文件来源、手册条款、程序落实、记录支撑、冲突审查和试运行签批状态集中到一个只读治理页面。
- 材料在哪里：结构化文件、块级追溯、记录模板字段覆盖、GOV-TRIAL/0.2 连续正文与冲突审查材料、现有受控文件和签批状态。
- 不能碰什么：不写 8010；不改变纸质体系唯一正式地位；不自动关闭冲突；不绕过现有修订、换版和签批入口。
- 做完怎么算成功：CX-03-02 能在同一页面看清证据链、真实缺口、记录字段支撑和下一步，并可跳转到现有受控页面办理。

## 2. 已实现

1. 新增 `/planning/structures/workbench?id=<结构化文件ID>`。
2. 在结构化文件列表和详情中增加“治理工作台”入口。
3. 聚合展示：
   - 外部依据；
   - 手册条款；
   - 程序内容块；
   - 记录表格和系统模块；
   - 记录字段覆盖状态；
   - 本文件冲突与体系级提醒；
   - 连续正文、修订对照、冲突审查材料；
   - 现有受控文件和签批状态。
4. 按“动作 + 结果 + 下一步”显示最多 3 个优先动作。
5. 页面只读，不新增表单，不提供绕过现有文控规则的原地修改入口。

## 3. 真实沙箱验证

验证对象：

- 结构化文件：`SIM-GOV02-XZTC/CX-03-02-2022`
- 版本：`GOV-TRIAL/0.2`
- 受控文件：`4171717e-3f22-4c52-8c85-646497a63e40`
- 记录表格：`SIM-XZTC/BG-35-03 [治理试运行] 标准物质报废申请表`

验证结果：

- 服务读取成功；
- 连续正文入口存在；
- 受控文件和签批状态回链存在；
- BG-35-03 的治理归属保持为标准物质报废申请表；
- 调用前后 `qms_structured_documents`、`qms_document_block_links`、`documents` 的有效记录数量完全一致；
- 临时 Web 预览返回 HTTP 200；
- 页面明确显示“纸质体系仍为唯一正式体系”；
- 临时预览容器验证后已关闭，没有新增常驻容器。

## 4. 如实保留的治理缺口

CX-03-02 当前聚合结果仍显示：

- 外部依据待补链；
- 手册条款待补链；
- BG-35-03 已关联，但表格 schema 文档仍需复核。

这些是现有数据的真实状态。v0.1 只负责发现、集中展示和引导，不把缺口自动判定为已关闭。

## 5. 验证证据

- PHP 语法检查：`591` 个 PHP 文件通过。
- 相关回归：`7` 个测试脚本通过。
- 真实 8021 数据只读验证：`qms_file_governance_workbench_runtime_smoke passed`。
- 页面运行验证：HTTP `200`，核心边界和证据链文字均已渲染。
- 主体合并后 8021 复验：登录成功，工作台 HTTP `200`；8010 未修改，临时预览容器不存在。

相关测试：

- `tests/qms_file_governance_workbench_service_smoke.php`
- `tests/qms_file_governance_workbench_ui_smoke.php`
- `tests/qms_file_governance_workbench_runtime_smoke.php`
- `tests/qms_governed_trial_resolved_manifest_smoke.php`
- `tests/qms_governed_trial_resolved_documents_smoke.php`
- `tests/qms_governance_trial_signing_ui_smoke.php`
- `tests/qms_flash_message_ui_smoke.php`
- `tests/qms_governed_change_ui_smoke.php`

## 6. 验收入口

当前 8021 已读取合并后的主体代码，可按以下步骤验收：

1. 登录 8021；
2. 进入“体系策划 → 结构化文件”；
3. 找到 `SIM-GOV02-XZTC/CX-03-02-2022`；
4. 点击“治理工作台”；
5. 核对证据链、记录字段支撑、冲突提醒和下一步动作。

本记录不构成正式体系发布或 CMA/CNAS 评审证据。
