# 法规动态监测与技能包收尾复验

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.1 |
| 复验日期 | 2026-07-15 |
| 实施分支 | `codex/regulatory-change-monitor-implementation` |
| 复验基线提交 | `d320313 docs(ai): 登记法规动态复核技能包` |
| 复验范围 | LIMS 法规动态监测实现、`qms-regulatory-change-monitor` 技能包、项目交接文档 |
| 复验结论 | 本地技术验收通过；AionUi 因后端未运行未同步；未部署、未启用真实月检、未导入真实数据 |

## 1. 技能包复验结果

真源目录：`~/.agents/skills/qms-regulatory-change-monitor`。

复跑命令：

```sh
cd "$HOME/.agents/skills/qms-regulatory-change-monitor"
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

实际结果：

```text
Ran 58 tests in 10.064s
OK
```

补充抽查：

- 端到端正例 `one-list-one-library.json`：校验退出码 0，复核包生成退出码 0，固定章节可定位。
- 敏感字段反例 `forbidden-secret.json`：退出码 3，定位到 `$.source.evidence.references[0].Pass-Word`，未打印敏感值。
- 第三方来源反例 `third-party-only.json`：输出“未完成官方来源核验”“证据不足，需人工核实”和“官方补证清单”。
- 13 个技能源文件 SHA-256 与《2026-07-14-法规动态复核技能包版本台账-v0.1.md》逐项一致。

## 2. LIMS 侧复验结果

复跑环境：`lzhj-regulatory-test` 隔离 Docker Compose 项目。

实际结果：

```text
regulatory_source_registry_smoke passed
REGULATORY_DB_SMOKES_PASSED=11
regulatory_monitor_compose_smoke passed
QMS_REGRESSIONS_AND_CSRF_PASSED=4
REGULATORY_SERVICE_LINT_PASSED=14
REGULATORY_TEST_LINT_PASSED=12
planning/regulatory-monitor/export   get
planning/regulatory-monitor/promote  post
git diff --check 退出码 0
git status --short 无输出
```

覆盖口径：

- 来源注册测试在 `--network none` 下执行。
- 数据库型测试只写隔离测试库。
- 回归覆盖外部变更事件、字段审计、提醒命令和 CSRF。
- PHP 语法检查覆盖法规监测控制器、服务、命令、14 个 `app/service/regulatory/*.php` 和 12 个 `regulatory_*_smoke.php`。

## 3. Git 与交接状态

- 分支：`codex/regulatory-change-monitor-implementation`。
- 相对 `origin/main`：本地领先 34 个提交，远端尚未发现同名分支。
- 当前工作区：干净。
- Cursor 接续提交 `d320313` 仅登记技能包版本台账和验收记录，未直接提交 `~/.agents/skills` 源目录内容，符合“项目仓库只登记交接文档”的边界。

## 4. 未完成边界

- AionUi 后端未运行，`aionui_api.py discover` 返回 `AionUi backend not found. Is the app running?`，因此未完成重新导入，不得宣称 AionUi 副本已同步。
- 未部署到云服务器。
- 未启用月度自动查新任务。
- 未使用真实官网当日页面、真实业务数据、云端数据或生产凭据。
- 未将候选提升为正式外部变更事件，未修订任何受控文件。

## 5. 收尾建议

1. 将当前分支推送到远端并创建 Draft PR，作为代码和交接文档的正式评审入口。
2. 用户打开 AionUi 后，重新导入 `~/.agents/skills/qms-regulatory-change-monitor`，再用示例候选 JSON 做一次可见性抽查。
3. 体验部署前继续保持 G 闸门：先做云端只读/配置体检，再部署测试环境；真实数据导入和真实月检启用另走审批。

## 6. 进度卡

当前版本：LIMS 法规动态监测验收记录 v0.4；法规动态复核技能包 v0.1；本收尾复验 v0.1。

本次完成：复核 Cursor 接续成果，复跑技能包 58 项测试、LIMS 强门禁、哈希台账、Git 状态和 AionUi 可用性检查。

下一步：推送分支并创建 Draft PR；AionUi 打开后执行技能重新导入；云端体验部署仍需独立闸门。

等你补齐：AionUi 应用运行状态；是否进入云端体验部署。

关键决定：本地技术验收可收口，但不扩大表述为“已部署/已启用/已同步 AionUi/已使用真实数据”。
