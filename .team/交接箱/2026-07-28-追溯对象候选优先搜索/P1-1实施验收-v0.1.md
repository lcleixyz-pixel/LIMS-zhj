# P1-1 追溯对象候选优先搜索实施验收

日期：2026-07-28

环境：8021 治理试运行沙箱

结论：通过

## 完成内容

- 外部条款、手册章节、程序文件、记录表格、岗位、运行模块和要素均使用零依赖的中文搜索。
- 可输入编号或名称即时过滤，无匹配项时显示中文恢复提示。
- 当前文件的外部依据、手册章节和运行记录候选显示“★ 本文件候选”，并排在同类对象前面。
- 切换关系用途时，隐藏对象的搜索框与选择框同步禁用，不会提交旧值。
- 无候选参数进入 `record_requirement` 内容块时，默认用途为“主链：运行记录”。
- P0-2 候选预填继续优先于内容块默认值，P0-3 保存端门禁保持不变。
- JavaScript 不可用时保留原生选择框和原表单提交路径。

## 8021 验收

以 CX-03-02“相关记录”内容块实测：

| 检查 | 结果 |
|---|---|
| 无参数进入记录要求块 | 默认“主链：运行记录” |
| 搜索 `BG-35-03` | 命中 1 项，且为本文件候选 |
| 搜索“标准物质报废” | 命中同一记录模板 |
| 搜索不存在对象 | 显示“未找到，请换用编号或名称；当前选择已保留” |
| 清空关键词 | 恢复 106 项，其中本文件候选 9 项 |
| 切换到外部依据 | 记录搜索框与选择框禁用；外部条款搜索启用 |
| 搜索 `2.11.3` | 命中市场监管总局公告条款候选 |
| 搜索 `6.4` | 命中手册候选并显示候选标识 |
| 从工作台带入记录候选 | 用途为运行记录、对象已选、状态为等待复核，并提示尚未保存 |

有效追溯关系验收前后均为 3182 条，没有提交复核表单。

## 自动化证据

4 个 PHP 文件语法检查通过，16 项专项及相关回归全部通过。

真实数据只读运行结果：

- CX-03-02 外部条款候选：16 项
- 手册章节候选：2 项
- 记录表格候选：9 项
- 37 份程序候选定向：939 项可定向、0 项阻断
- 语义治理状态保持：`missing_primary=19`、`suspected_mismatch=18`

## 版本记录

- `d39d74b3`：设计规格
- `0866ab15`：实施计划
- `967e2b4e`：候选标记与稳定排序
- `e5c33772`：中文搜索和内容块默认用途
- `b72cd6b3`：真实数据运行覆盖

## 保留问题

搜索暴露出少量同编号、同名称的重复选项，例如手册 `6.4` 和部分记录模板。系统本轮没有静默合并或删除；应在 P1-2 按版本、状态和来源规则治理后再去重。

## 边界

- 本功能只缩短人工查找路径，不代替复核结论。
- 没有自动确认、自动保存或批量改写追溯关系。
- 没有迁移真实运行记录，没有写入、部署或改动 8010。
- 纸质体系仍是唯一正式体系。
