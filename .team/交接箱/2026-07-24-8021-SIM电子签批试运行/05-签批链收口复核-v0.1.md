# 8021 SIM 电子签批试运行：签批链收口复核 v0.1

日期：2026-07-26

## 本次做什么

对 8021 隔离试运行环境的 SIM 文件电子签批链进行收口复核，并把“旧版保留/换版”治理思路同步到种子数据烟测。

## 本次确认的链条

- SIM 编制人、审核人、批准人账号与岗位任命可由试运行种子脚本建立。
- 文件提交审核后可建立分轮次审批记录。
- 审核、批准、驳回、修改后重提均保留轮次证据。
- DocuSeal 回调与系统内手工处理共用文件驳回收口逻辑。
- 在线签字完成后可归档签署文件资产，业务页面避免暴露过重的外部工具术语。

## 本次修正

烟测原先要求 `SIM-SIGN-20260724` 在数据库内只能存在 1 条非删除记录。后续治理加入“旧版保留/换版”后，同编号存在 `obsolete` 旧版是合理状态，因此本次改为：

- 种子脚本优先选择“当前非作废”的 SIM 演练文件。
- 种子烟测只要求“当前非作废”样本文档唯一。
- `obsolete` 旧版保留，不作为失败条件。

## 验证结果

在 8021 试运行容器 `lims-zhj-governance-trial-20260724-app-1` 内通过：

- PHP 语法检查：签批链控制器、服务、种子脚本、专用烟测均无语法错误。
- `qms_governance_trial_docuseal_compose_smoke.php`：通过。
- `qms_governance_trial_signing_seed_smoke.php`：通过。
- `qms_governance_trial_signing_ui_smoke.php`：通过。
- `qms_governance_trial_signing_lifecycle_smoke.php`：通过。
- `qms_wave1_d35_signing_loop_smoke.php`：通过。

## 未做什么

- 未向 8010 正式环境写入运行记录。
- 未合并到主分支。
- 未处理 G2 扩批、记录打印、职责页面等其他未提交改动。

## 下一步建议

下一步适合继续按“单组收口”的方式处理：优先把记录打印 / PDF 当前态链条单独复核并提交，再处理 G2 扩批包和职责页面，避免多条治理线混在同一个提交里。
