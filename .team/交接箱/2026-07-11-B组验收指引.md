# B 组验收指引（你回来照这个验）

> 写于 2026-07-11 深夜。B1/B2/B3 已实现 commit，本地待 push。这是你回来后的验收清单。
> 对应 commits：`d3cb6d3`(B1) / `8f7b9aa`(B1验证+时区) / `06bd94a`(B2) / `cddd2f0`(B3)

## 先做这一步：重建容器

B 组改了 Dockerfile、entrypoint、NotificationService、新增 Calendar 等，必须重建：
```bash
cd "/Users/lc.leixyz/Documents/AI工作台/01-项目代码/LIMS-zhj/jewelry-qms"
docker compose up --build
```
看到 `[jewelry-qms] crond started — daily check:reminders at 08:00` 就说明 B1 定时器装好了。

---

## B1 验收（你之前已验证过，确认还在就行）

- [ ] 启动日志有 `crond started` 行
- [ ] 手动跑一次不报错：`docker compose exec app php think check:reminders --type=all`
- [ ] 容器时区对：`docker compose exec app date` 应显示 CST / Asia/Shanghai

## B2 验收（扩了 3 类检查）

新 3 类 type：`equipment_verification`（期间核查）、`audit_schedule`（内审节点）、`source_freshness`（标准查新）。

- [ ] 单独跑每类不报错：
  ```bash
  docker compose exec app php think check:reminders --type=equipment_verification
  docker compose exec app php think check:reminders --type=audit_schedule
  docker compose exec app php think check:reminders --type=source_freshness
  ```
- [ ] 全量跑（type=all）覆盖 7 类不报错：`docker compose exec app php think check:reminders --type=all`

**想看真的生成通知**（可选，造一条临期数据）：
- 期间核查：在 `equipment_maintenances` 表插一条 `maintenance_type='verification'`、`next_due_date=明天` 的记录，跑 `--type=equipment_verification`，然后到系统的通知列表看有没有新提醒。
- 内审/查新同理（`audit_schedules.audit_date` / `qms_sources.next_freshness_due` 设明天）。

**字段核对（我做的时候已验证，列给你复查）**：
- 期间核查：表 `equipment_maintenances`，字段 `next_due_date`（筛 `maintenance_type='verification'`）
- 内审节点：表 `audit_schedules`，字段 `audit_date`（筛 `status='planned'`）
- 标准查新：表 `qms_sources`，字段 `next_freshness_due`（筛 `status='published'`）
- 三类临期阈值都是 30 天，通知接收人都是 quality_manager 角色，照现有 4 类模式

## B3 验收（本月体系待办页）

- [ ] 浏览器访问 `http://localhost:8010/calendar/index`（或点顶部菜单"本月待办"）
- [ ] 页面打开不报错，显示"X年X月"标题 + 待办列表（或"本月暂无到期待办"）
- [ ] 用非 admin 角色（如 staff_test）登录也能访问（全员权限）
- [ ] 如果 B2 造了临期数据并生成了通知（带 due_date 在本月），这里应该能看到

**预期**：列表/表格样式（Bootstrap 卡片+表格），列有标题/类型/到期日/剩余天数/状态徽章（过期红/即将黄/正常绿）/操作链接。**不是**精美日历组件——那是 UI 翻新小战役的事。

---

## 可能的小问题（我先说）

1. **B3 链接跳转**：通知的 `link_controller` 字段如果为空，操作链接显示"-"不跳转（不会报错）。如果点了一些提醒跳不对，告诉我具体哪个。
2. **B2 临期阈值**：三类都用 30 天（照现有 calibration/doc_review/competency）。如果想改（比如期间核查要 15 天），告诉我，一行代码的事。
3. **php -l 我跑不了**（沙盒没 PHP），代码是手动核对语法的。你重建容器后如果报语法错，把红字发我——但这种概率低（逻辑简单、照现有模式）。

## 验收完做什么

- 没问题：告诉我"B 组过"，我勾当前战役 + 更新验证方案（周期任务引擎相关）。
- 有问题：截图/报错发我，我修。
- 体验上想调（阈值、菜单位置、列表字段）：记入不好用清单，下一批改。

## push 提醒

本地又领先远端 4 个提交（B1验证、B2、B3、+之前的）。验收完找个时间 `git push origin codex/qms-governance-core`。
