# G3-R 问题台账 v0.1

- 日期：2026-07-15

| 编号 | 问题 | 等级 | 影响 | 建议处理 |
|---|---|---|---|---|
| G3R-01 | 首次快照路径误判，生成不完整快照 | 已处置 | 不完整快照不能作为 G4-R 回滚依据 | 以 `20260715-152215-g3r-before-upload-complete` 为准，其他 G3R 快照仅作排障痕迹 |
| G3R-02 | 第二次数据库备份尝试使用 root 空密码失败 | 已处置 | 未生成当次数据库备份 | 已改用容器内 `MYSQL_USER/MYSQL_PASSWORD/MYSQL_DATABASE` 生成完整数据库快照 |
| G3R-03 | 首次上传包包含 macOS `._` 扩展属性文件 | 已处置 | 会污染交接包目录 | 已用 `COPYFILE_DISABLE=1` 重新打包并覆盖上传，服务器检查 `dot_underscore_count=0` |
| G3R-04 | 本地 G2-R 源库为 79 表，云端当前库为 81 表 | 重要 | 本地 main 与云端法规候选模块版本存在差异 | G4-R 前确认代码/数据库基线；云端 3 张法规候选相关表已存在且为空 |
| G3R-05 | 本轮未上传代码 release | 需决策 | 若 G4-R 要上线最新治理代码，仍需重新打包 release 和镜像 | G4-R 前单独批准“代码 release 升级”或明确仅导入数据/候选材料 |
| G3R-06 | 脱敏包账号密码为不可登录占位值 | 预期边界 | 导入后员工不能直接登录 | G4-R 若导入，必须在服务器重新生成账号密码并形成交付清单 |

## 不构成阻断的事项

- 云端法规候选相关表已经存在：`qms_external_change_events`、`qms_external_change_candidates`、`qms_regulatory_monitor_runs`。
- 当前 incoming 只保存上传包和校验文件，不会影响运行中的 app/db 容器。
