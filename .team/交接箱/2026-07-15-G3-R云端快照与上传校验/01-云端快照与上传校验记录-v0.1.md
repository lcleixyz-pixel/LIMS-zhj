# G3-R 云端快照与上传校验记录 v0.1

- 日期：2026-07-15
- 服务器：`101.200.41.200`
- 测试域名：`qms.zhj-jc.com`
- 结论：G3-R 已完成；云端只做快照和上传校验，未导入、未切换、未开放员工登录。

## 1. 云端现状

| 项目 | 结果 |
|---|---|
| current 指向 | `/www/server/jewelry-qms-experience/releases/regmon-20260715-64fdb2a` |
| app 镜像 | `jewelry-qms-experience:amd64-64fdb2a` |
| db 镜像 | `jewelry-qms-mysql:8.4-amd64` |
| app 端口 | `127.0.0.1:18010->8000/tcp` |
| 数据库表数 | 81 |
| 法规候选相关表 | 3/3 存在：`qms_external_change_events`、`qms_external_change_candidates`、`qms_regulatory_monitor_runs` |
| G2-R 临时表 | 0 |

## 2. 快照

完整快照目录：

`/www/server/jewelry-qms-experience/shared/snapshots/20260715-152215-g3r-before-upload-complete`

快照内容：

| 文件 | 说明 |
|---|---|
| `current-target.txt` | current 指向 |
| `compose.yaml` | 实际运行体验环境 compose |
| `verify.sh` | 体验环境验证脚本 |
| `env.redacted` | 脱敏后的共享环境配置 |
| `container-state.txt` | app/db 容器状态和 compose 标签 |
| `docker-compose-ps.txt` | compose 状态 |
| `db-jewelry_qms.sql.gz` | 云端当前数据库逻辑快照 |
| `current-file-list.txt` | 当前 release 文件清单 |
| `uploads-not-found.txt` | 当前 app 挂载路径下未发现 `public/uploads` 目录 |
| `SHA256SUMS.txt` | 快照文件校验 |

说明：最初曾生成一个不完整快照 `20260715-152044-g3r-before-upload`，原因是误把 release 根目录当成 compose 根目录；随后经系统化调试确认实际 compose 位于 `current/jewelry-qms/deploy/experience/compose.yaml`，并生成了上述完整快照。另有一次中间失败目录 `20260715-152145-g3r-before-upload-complete`，因使用 root 空密码备份数据库被 MySQL 拒绝；最终完整快照为 `20260715-152215-g3r-before-upload-complete`。

## 3. 上传包

云端 incoming：

`/www/server/jewelry-qms-experience/shared/incoming/g3r-yidanyiku-20260715`

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `G3-R-upload-sanitized-yidanyiku-20260715.tar.gz` | 944558 bytes | `a969760b63f6d376f2348d6489845aa870fbc1a2547da46ae2e5e43cc14b5324` |

上传包内容：

- `artifacts/migration-database-g2r-sanitized.sql.gz`
- `artifacts/candidate-reg-2026-samr-14-yidanyiku-review-packet-v1.0.json`
- `reports/candidate-validation.json`
- `reports/一单一库候选复核包-v0.1.md`
- G2-R 本地迁移预演记录、敏感数据排除清单、一单一库本地验收记录
- `README-G3-R.txt`
- 包内 `SHA256SUMS.txt`

服务器侧校验：

- 本地 SHA 与服务器实际 SHA 一致。
- 重新打包后 `._` Apple 扩展属性文件数量为 0。
- 服务器侧写入 `SHA256SUMS.basename.txt` 供后续核对。

## 4. 未执行事项

本轮明确未执行：

1. 未解包导入数据库；
2. 未执行 `docker load`；
3. 未切换 `current`；
4. 未运行 `docker compose up`；
5. 未创建或开放员工账号；
6. 未修改 BasicAuth、Nginx、宝塔配置；
7. 未写入“一单一库”候选到云端数据库。

## 5. 结论

G3-R 达到验收要求：云端快照已补齐并可校验，G2-R 脱敏包与一单一库候选复核材料已上传并通过服务器侧 SHA 校验。当前状态可进入 G4-R 讨论，但 G4-R 前仍需确认是否同时升级代码 release，以及是否采用本轮脱敏包或重新生成脱敏包。
