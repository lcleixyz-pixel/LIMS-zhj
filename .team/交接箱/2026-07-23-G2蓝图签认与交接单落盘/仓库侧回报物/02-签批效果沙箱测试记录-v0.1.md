# 签批效果沙箱测试记录 v0.1

- 日期：2026-07-23
- 执行方：Codex（仓库侧）
- 目标：回答“哪个沙箱？能否顺便测试签批效果？”

## 沙箱说明

上一轮 Route A 样张生成使用的是本机 `jewelry-qms-app-1` 容器能力做代码级 smoke 与 HTML/PDF 渲染；未走 HTTP，未写 8010 正式路径数据。该表述称为“沙箱”不够严谨。

本次签批效果复测使用新建临时隔离栈：

- Compose project：`lims-zhj-signoff-smoke-20260723`
- App 容器：`lims-zhj-signoff-smoke-20260723-app-1`
- DB 容器：`lims-zhj-signoff-smoke-20260723-db-1`
- App 随机本地端口：本次为 `127.0.0.1:59969->8000`
- DB 端口：不暴露到宿主机
- 数据库：新卷新库，仅用于本次 smoke
- 8010：未用于本次签批写入测试

本次临时栈额外使用只读 `.env` 覆盖：

- `jewelry-qms/.team/tmp/signoff-smoke.env`
- `jewelry-qms/.team/tmp/compose.signoff-smoke-env.yaml`

原因：项目根 `.env` 仍写 `DB_HOST = 127.0.0.1`，在容器内会误连自身；临时覆盖将签批 smoke 栈修正为 `DB_HOST = db`，不修改正式 `.env`。

## 执行命令

```bash
docker compose \
  -f compose.wave1-smoke.yaml \
  -f compose.responsibility-test.yaml \
  -f .team/tmp/compose.signoff-smoke-env.yaml \
  -p lims-zhj-signoff-smoke-20260723 up -d

docker compose \
  -f compose.wave1-smoke.yaml \
  -f compose.responsibility-test.yaml \
  -f .team/tmp/compose.signoff-smoke-env.yaml \
  -p lims-zhj-signoff-smoke-20260723 exec -T app php tests/qms_responsibility_ui_smoke.php

docker compose \
  -f compose.wave1-smoke.yaml \
  -f compose.responsibility-test.yaml \
  -f .team/tmp/compose.signoff-smoke-env.yaml \
  -p lims-zhj-signoff-smoke-20260723 exec -T app php tests/qms_responsibility_end_to_end_smoke.php

docker compose \
  -f compose.wave1-smoke.yaml \
  -f compose.responsibility-test.yaml \
  -f .team/tmp/compose.signoff-smoke-env.yaml \
  -p lims-zhj-signoff-smoke-20260723 exec -T app php tests/qms_responsibility_approval_smoke.php

docker compose \
  -f compose.wave1-smoke.yaml \
  -f compose.responsibility-test.yaml \
  -f .team/tmp/compose.signoff-smoke-env.yaml \
  -p lims-zhj-signoff-smoke-20260723 exec -T app php tests/qms_wave1_d4_webhook_smoke.php
```

## 结果

通过：

- `qms_responsibility_ui_smoke passed`
- `qms responsibility end-to-end smoke: OK`
- `qms responsibility approval smoke: OK`
- `qms_wave1_d4_webhook_smoke passed`

端到端签批证据输出：

```text
structure_empty_staffing=warning
activation_without_governance=blocker
activation_with_governance=pass
effective_version=effective
active_chain_appointments=4
old_version_during_change=effective
rbac_roles_unchanged=yes
Y13-CX20=conflict
Y13-CX21=conflict
Y13-CX32=review_required
```

解释：

- 空人员配置不会误生效，只给结构预警。
- 治理身份未补齐时阻断签批激活。
- 公司总经理与实验室主任身份补齐后，责任链可提交。
- 总经理批次与实验室主任批次均签批后，版本进入 `effective`。
- 生成 4 条有效任命证据。
- 责任链任命不改写 RBAC 角色。
- CX20 / CX21 / CX32 文件一致性检查仍按既有基线输出冲突或需人工确认。
- DocuSeal webhook 验证覆盖错误签名、过期时间、篡改内容、nonce 重放、原生签名、内容哈希不匹配。

## 复测中发现并修正

1. `tests/qms_responsibility_ui_smoke.php` 有两处旧页面文案断言，已同步为当前页面的“文件一致性检查（辅助）/（辅助核验）”。
2. `tests/qms_responsibility_ui_smoke.php` 有一处旧提示断言，已同步为当前页面“不生成真实任命”的提示。
3. `PlanningResponsibility::flashThrowable()` 曾把非业务异常的 `$e->getMessage()` 拼进页面错误提示；已修回“操作失败，请联系管理员”，内部异常细节仅写日志。

## 注意

责任链相关 DB smoke 不应并行跑在同一个测试库中，否则可能互相踩事务或保存点；本次最终结论以串行复测结果为准。

交接纪律继续保留：

> 209字段候选包与蓝图有差异时,一律回报裁决,不得静默采纳
