# 项目分支运行状态与 UX 收口建议

> 日期：2026-07-22
> 执行人：Codex
> 性质：只读体检 + 本地小修补 + 收口建议
> 红线：未推送、未部署、未写生产数据库、未重建现用镜像、未切换云端 current

## 1. 当前结论

本机 `jewelry-qms` 正常运行，远端公网入口 BasicAuth 正常拦截。版本侧处于“本地领先 + 工作区待收口”的状态：本地 `main` 比 `origin/main` 领先 4 个提交，且存在一批 UX-1/UX-2/UX-3 中文化和反馈文案改动尚未提交。

本轮检查发现一个小缺口：记录详情页新增了“申请更正”表单和控制器方法，但路由未登记。已补充：

- `record_form_instance/requestCorrection` POST 路由

## 2. Git 状态摘要

- 当前主仓：`/Users/lc.leixyz/Documents/AI工作台/01-项目代码/LIMS-zhj`
- 体检时分支：`main`
- 收口分支：`codex/ux-feedback-closeout-20260722`
- 本地 HEAD：`b9d37b22`
- 远端 `origin/main`：`799365a6`
- ahead/behind：ahead 4 / behind 0
- 远端标签：`v1.0.0`、`v2.2.0`
- 打开的 Draft PR：
  - PR #31：法规动态监测与复核技能包
  - PR #32：G4-R12 云端体验部署准备

## 3. 本机运行状态

- `8010` 主服务：登录页 `200`
- `8010` 本月待办未登录访问：`302` 到登录页，符合预期
- `3100` DocuSeal：容器运行中
- 保留演练环境：
  - `8011`、`8012`、`8013`、`8014` 登录页均 `200`
  - `8020` wave1 smoke 环境登录页 `200`

## 4. 远端运行状态

- 域名：`qms.zhj-jc.com`
- HTTPS 公网访问：`401`
- BasicAuth realm：`Jewelry QMS Experience`
- 结论：公网 HTTPS + BasicAuth 外层门禁正常。

本轮未能确认服务器内部容器状态：无交互 SSH 登录失败，提示缺少可用登录凭据。因此不能把“公网门禁正常”扩展解释为“服务器内 app/db/verify.sh 全部正常”。

## 5. 本轮验证

已通过：

- `php -l route/app.php`
- `php -l app/controller/RecordFormInstance.php`
- `php -l app/controller/Document.php`
- `php -l app/controller/Calendar.php`
- `php -l config/qms.php`
- `qms_ui_navigation_template_smoke passed`
- `qms_wave1_s5_rbac_whitelist_smoke passed`
- `qms_wave1_d35_signing_loop_smoke passed`
- `qms_wave1_d4_webhook_smoke passed`
- `git diff --check`

HTTP 抽查：

- `POST /record_form_instance/requestCorrection?id=__probe__` 未登录时返回 `302` 到登录页，说明路由已接入认证保护链路。
- `/calendar/index?filter=all`、`/calendar/index?filter=mine` 未登录时均返回 `302`，符合预期。

## 6. 风险判断

这批改动整体可收口，但不建议直接推 `main`，原因：

1. 本地 `main` 已经领先远端 4 个提交；
2. 当前工作区包含多批 UX 改动和交接材料；
3. 云端内部状态尚未通过 SSH/服务器侧 `verify.sh` 确认；
4. 现用 8010 虽正常运行，但台账仍写明“现用镜像待重建部署后全量页面实测”。

## 7. 我建议的下一步

建议走“本地分支 + 本地提交 + Draft PR 闸门”的路线：

1. 已从当前状态创建收口分支：`codex/ux-feedback-closeout-20260722`。
2. 把 UX-1/UX-2/UX-3 改动、路由修补、交接材料和本报告提交为一个本地 commit。
3. 再做一次针对文件详情、记录详情、本月待办、登录/改密的页面抽查。
4. 通过后再推分支并开 Draft PR。
5. 仍不直接部署云端，不重建现用 8010 镜像，直到用户单独授权。

## 8. 当前建议口径

可以进入版本收口，但只到 Draft PR 闸门；不进入远端部署。
