# A2 备份恢复演练记录

> 对应 `.team/当前战役.md` 任务 A2；CL01 7.11.3 b。
> 操作依据：`.team/体系可信度演练操作单-A2A3.md`
> **铁律已遵守**：真实库 `jewelry_qms` 只读备份，破坏/恢复全程在 `jewelry_qms_drill`。

## 基本信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-07-11 |
| 操作人 | Cursor（代执行）/ 机构验收人：待签 |
| 环境 | Docker Compose：`jewelry-qms-app-1` + `jewelry-qms-db-1`（MySQL 8.4） |
| 备份文件 | `backup-20260711-a2-drill.sql`（本目录副本；源在 `jewelry-qms/`） |
| 备份大小 | 2.16 MB（2,269,514 字节） |

## 步骤与结果

| 步骤 | 动作 | 结果 |
|---|---|---|
| 1 | `mysqldump -uroot jewelry_qms` → 备份文件 | 通过，文件非空，含 SQL dump 头 |
| 2 | `CREATE DATABASE jewelry_qms_drill` | 通过 |
| 3 | 备份灌入临时库 | 通过 |
| 4 | 抽查条数对比 | 见下表，一致 |
| 5 | 临时库删 1 条实例 → 再灌备份 | 12→11→12，通过 |
| 6 | `DROP DATABASE jewelry_qms_drill` | 通过；真实库条数仍为 12 |

### 抽查数据

| 表 | 真实库 `jewelry_qms` | 临时库恢复后 | 模拟丢失后 | 再次恢复后 |
|---|---|---|---|---|
| `record_form_instances` | 12 | 12 | 11 | 12 |
| `employees` | 12 | 12 | — | — |

## 结论

- **结果：通过**
- 证明：备份可完整恢复；模拟丢失后可从备份救回。
- 真实库在演练前后 `record_form_instances=12`，未改动。

## 备注

- 本演练为本地 Docker 开发库，非正式生产机房备份制度；生产环境仍需按运维手册约定备份周期与异地存放。
- 备份文件含业务数据，勿外发；仅作验证证据留存。
