# LIMS-zhj 8012 QMS 盲评独立检出报告

> 版本：v0.1  
> 评审日期：2026-07-19  
> 评审对象：`8012 QMS 盲评环境`  
> 性质：独立 SIM 文档/记录质量盲评，不作为真实运行、批准、生效或正式审核证据

## 1. 盲评边界

- 唯一读取的环境说明为：`证据/密封/盲评环境公开说明-v0.1.md`。
- 未读取、搜索或列举同目录其他文件；未读取、搜索或列举任何文件名含“答案”的文件。
- 页面核对仅访问 `http://127.0.0.1:8012/` 及其正常跳转的登录页；未重置账号、修改口令或绕过认证。
- 数据库核对仅执行 `SHOW`、`SELECT` 及 `START TRANSACTION READ ONLY`；未执行任何写入语句，未修改 8012。
- 公开冻结基线仅使用说明中披露的版本 `v0.3` 与 SHA-256 `0de2bf31bb279cea6cc420c65e477910d3d3968b320ceb28a35292f496a95c2d`。因公开说明未提供可供重新计算的冻结包，本次仅把该指纹作为来源标识，不据此反推预设偏差数量。
- 当前浏览器没有既有 8012 登录会话，公开说明也未提供口令。因此无法以已登录 SIM 角色对详情页的字段展示、按钮权限和状态标签做二次确认；以下判断以公开登录页、只读数据库字段及记录间一致性为主。

## 2. 环境与方法确认

- 根路径返回 HTTP `302`，正常跳转至 `/login/index`；登录页返回 HTTP `200`。
- 登录页显示环境标识“8012 QMS 盲评环境”和 SIM 使用限制。
- Compose 项目中的应用容器运行中，数据库容器为 `healthy`。
- 核心方法：检查受控文件的状态、日期、人员、变更说明、引用依据、发布标志、批准记录、修订记录、历史记录、字段变更记录以及正文/结构化资产之间的一致性。
- 判断口径主要对应 ISO/IEC 17025:2017（CNAS-CL01 等同采用）7.2.1.2、7.2.1.3、7.5、7.11.3(d)、8.3.2、8.4.1。本文区分“已确认偏差”与“因证据不足无法确认”。

## 3. 独立检出

### QMS-BLIND-01：已发布文件缺少生效日期和批准人

**对象**

- 记录 ID：`sim-blind-doc-required-01`
- 文件编号：`SIM/QMS-CX-01-2026`
- 标题：`SIM受控文件发布检查程序`

**只读证据**

| 字段/关联记录 | 观测值 |
|---|---|
| `status` | `published` |
| `publish` / `record_status` | `1` / `1` |
| `effective_date` | `NULL` |
| `approved_by` | `NULL` |
| `prepared_by` / `reviewed_by` | `sim-gov-user-dc` / `sim-gov-user-qm` |
| 关联 `approvals` 记录 | `0` |
| 关联 `document_revisions` 记录 | `0` |
| 关联 `histories` / `field_change_logs` | `0` / `0` |

**判断**

已确认记录质量偏差。记录已经处于 `published`，但没有生效日期、批准人或独立批准记录，无法证明文件在发布前经过授权，也无法确定从何时开始适用。准备人与审核人字段不能替代批准证据。

**风险级别：高**

可能导致未经批准或适用日期不明的程序被当作现行文件使用，削弱文件控制、人员执行版本判断和后续责任追溯。对应 8.3.2(a)、8.3.2(d)、8.4.1，并关联 7.11.3(d) 的数据完整性要求。

**处置建议（不在 8012 执行）**

在正式发布闸门中强制校验 `effective_date`、`approved_by` 或等效批准记录；发布动作应产生不可缺失的审批及状态变更审计记录。修复前应将该记录视为“不可证明已有效批准”。

---

### QMS-BLIND-02：文件标题引用版本与换版说明相互矛盾

**对象**

- 记录 ID：`sim-blind-doc-reference-02`
- 文件编号：`SIM/QMS-ZY-02-2026`
- 标题：`SIM珠宝玉石术语引用核对作业指导书（引用 GB/T 16552-2017）`

**只读证据**

| 字段/关联记录 | 观测值 |
|---|---|
| `version` / `revision` | `B/0` / `1` |
| `status` / `publish` | `approved` / `0` |
| `effective_date` | `2026-07-19` |
| `approved_by` | `sim-gov-user-dir` |
| `change_reason` | `B/0 换版说明：引用依据统一更新为 GB/T 16552-2024。` |
| `qms_sources` 中含 `16552` 的来源记录 | 未检出 |
| 关联修订、历史和字段变更记录 | 均为 `0` |

**判断**

已确认记录内部一致性偏差。文件标题明确保留 `GB/T 16552-2017`，换版说明却声称已经统一更新为 `GB/T 16552-2024`。在没有正文资产、来源登记或修订审计记录可进一步消解冲突的情况下，不能确定获批版本实际采用哪一版依据。

**风险级别：中**

当前 `publish=0`，风险尚有发布闸门可控制；但记录已经是 `approved` 且带生效日期，若后续直接发布，可能把过时或版本不明的外部依据带入作业指导。对应 7.2.1.2、7.2.1.3、8.3.2(c)、8.3.2(d)、8.3.2(f)。

**处置建议（不在 8012 执行）**

发布前统一标题、正文、换版说明和外部依据登记中的版本号；补充来源有效性核查证据及修订对照，重新复核受该引用影响的术语与判定内容。

---

### QMS-BLIND-03：审核中修订稿同时带生效日期和发布标志

**对象**

- 记录 ID：`sim-blind-doc-state-03`
- 文件编号：`SIM/QMS-CX-03-2026`
- 标题：`SIM内部审核闭环程序`

**只读证据**

| 字段/关联记录 | 观测值 |
|---|---|
| `version` / `revision` | `A/1` / `1` |
| `status` | `reviewing` |
| `change_reason` | `A/1 修订稿仍在审核中。` |
| `effective_date` | `2026-07-19` |
| `publish` / `record_status` | `1` / `1` |
| `approved_by` | `NULL` |
| 关联 `approvals` 记录 | `0` |
| 关联修订、历史和字段变更记录 | 均为 `0` |

**判断**

已确认状态一致性偏差。`status=reviewing` 和“仍在审核中”表明该修订稿尚未完成批准，但同一记录已有生效日期且 `publish=1`。即使 `publish` 在实现中仅表示记录可见性而非正式发布，该组合仍会造成“审核稿是否已生效”的歧义；由于无登录角色会话，无法进一步确认页面是否把该记录展示为现行文件。

**风险级别：高**

可能使未批准修订稿进入可使用范围，或使人员误把审核稿当成现行版本，直接影响文件唯一有效版本控制及内部审核程序的可靠执行。对应 8.3.2(a)、8.3.2(d)、8.3.2(f)、8.4.1，并关联 7.11.3(d)。

**处置建议（不在 8012 执行）**

建立互斥状态约束：`reviewing` 时不得填写正式生效日期、不得进入现行发布范围；只有批准完成并形成审计记录后，才能原子化切换为已发布/已生效状态。

## 4. 共同证据缺口（条件性观察）

上述三条记录均出现以下只读查询结果：

- `file_path`、`file_name` 为 `NULL`；
- 关联 `qms_document_assets`、`qms_structured_documents`、`qms_document_blocks` 均为 `0`；
- 关联 `document_revisions`、`histories`、`field_change_logs` 均为 `0`。

这意味着本次只能审查元数据，无法核对正文内容、引用出现位置、修订前后差异以及状态变更全过程。若这些记录被设计为正式受控文件，则属于 7.5、8.3.2(c)、8.4.1 所需证据不足；若它们仅是专用于盲评的元数据样本，则应在数据模型或页面上明确标识，避免被当作完整受控文件。本观察不单独推定为新的预设偏差。

## 5. 结论与边界声明

- 已形成可复核的独立检出证据，判断基于记录自身字段及关联记录的一致性，不依赖任何预设答案。
- 本报告不推测环境中预设偏差的总数，不计算检出率。
- 未登录 SIM 角色详情页是本次的主要证据边界；因此未对页面级权限、展示文案和操作按钮行为作结论。
- 所有建议均为后续治理意见，本次未对 8012 执行修改。

