# G4-R11-B2/B3 账号与演示数据收口记录 v0.1

日期：2026-07-16

## 本轮目标

执行用户批准的 G4-R11-B2/B3：

- B2：停用或改密测试弱口令账号，重置 `qm_test`。
- B3：归档孤儿期间核查提醒，处理明显演示培训记录。

## 执行边界

- 只处理本地数据库。
- 不部署云端。
- 不删除真实业务数据。
- 不删除历史 PDF 文件。
- 新口令不写入 Git、不写入聊天、不写入交接文档。

## 备份

全库备份：

- `jewelry-qms/runtime/private-backups/g4-r11-b2-b3-20260716-1115/local-before-g4-r11-b2-b3-full.sql`

SHA256：

```text
3260df666ac52f6e446207e2e3df8100bf1e02b0324cdb4af850d7296f24cb4f
```

清理前证据：

- `evidence/g4-r11-b2-b3-20260716-1115/before-target-rows.tsv`
- `evidence/g4-r11-b2-b3-20260716-1115/before-password-check.tsv`

## 执行动作

### 1. 账号收口

已处理账号：

| 账号 | 处理 |
|---|---|
| `admin` | 保持启用，重置为强随机口令 |
| `qm_test` | 保持启用，重置为强随机口令 |
| `auditor_test` | 停用，随机化口令 |
| `head_test` | 停用，随机化口令 |
| `staff_test` | 停用，随机化口令 |

同时处理：

- 结束 `auditor_test / head_test / staff_test` 的开放 `user_sessions`。
- 删除这 3 个测试账号对应的 PHP session 文件。
- 保留 `admin` 会话文件，不做强制登出。

新口令保存位置：

| 账号 | 本机钥匙串 service |
|---|---|
| `admin` | `jewelry-qms-local-8010-admin` |
| `qm_test` | `jewelry-qms-local-8010-qm_test` |

如需在本机取回，可由授权人员运行：

```bash
security find-generic-password -a admin -s jewelry-qms-local-8010-admin -w
security find-generic-password -a qm_test -s jewelry-qms-local-8010-qm_test -w
```

### 2. 孤儿期间核查提醒归档

归档对象：

```text
id = 779dcd3f-d9e8-4f1b-9d25-41df8ebb3cd0
title = 期间核查到期提醒
link_controller = equipment_maintenance
link_id = 00000000-0000-0000-0000-00000000b002
```

处理：

```text
publish = 0
soft_delete = 1
```

### 3. 演示培训记录作废

作废对象：

```text
id = 011c83e3-21c5-4093-b8c3-1e793c1f40b1
doc_number = XZTC/BG-01-02
record_title = 2026运行记录-XZTC/BG-01-02-人员培训记录表-001
status = generated
问题 = attendees 为空且已形成 PDF
```

处理：

```text
status = voided
record_title = 【演示数据-已作废】2026运行记录-XZTC/BG-01-02-人员培训记录表-001
```

说明：保留原 PDF 文件名，不删除历史证据。

## 执行中校验修正

执行过程中发现一次口令哈希生成命令的环境变量传递方式不正确，导致初次写入后即时校验发现哈希匹配空口令。已立即使用钥匙串中保存的新口令重新生成并重写正确哈希。

最终验证结果证明：

- 活动账号中 `password` 弱口令数量为 0。
- 活动账号中空口令数量为 0。
- `admin` 与 `qm_test` 的钥匙串口令均能匹配数据库哈希。

## 清理后验证

弱口令扫描：

```json
{
  "active_password_users": [],
  "active_empty_password_users": [],
  "active_weak_count": 0,
  "active_empty_count": 0
}
```

钥匙串匹配：

```text
admin_keychain_match=yes
qm_keychain_match=yes
```

账号状态：

```text
admin        publish=1 soft_delete=0
qm_test      publish=1 soft_delete=0
auditor_test publish=0 soft_delete=0
head_test    publish=0 soft_delete=0
staff_test   publish=0 soft_delete=0
```

测试账号会话：

```text
auditor_test/head_test/staff_test open_sessions = 0
PHP session 文件中未检出这 3 个测试账号
```

孤儿提醒：

```text
期间核查到期提醒 publish=0 soft_delete=1
```

演示记录：

```text
【演示数据-已作废】2026运行记录-XZTC/BG-01-02-人员培训记录表-001
status=voided
```

关键烟测：

```text
qms_g4r7_trial_readiness_smoke passed
qms_g4r9_stale_link_guard_smoke passed
```

登录页 debug 复验：

```text
HTML 5906 bytes
未检出 ShowPageTrace / SESSION_ID= / 运行时间 / /app/vendor
```

清理后证据：

- `evidence/g4-r11-b2-b3-20260716-1115/after-target-rows.tsv`
- `evidence/g4-r11-b2-b3-20260716-1115/after-weak-password-scan.json`
- `evidence/g4-r11-b2-b3-20260716-1115/after-keychain-match.txt`
- `evidence/g4-r11-b2-b3-20260716-1115/after-test-user-session-grep.txt`

## 结论

G4-R11-B2/B3 已完成。本地试运行环境不再存在活动弱口令账号；测试账号已停用且开放会话已结束；孤儿期间核查提醒已归档；明显演示培训记录已作废。

## 剩余硬伤

仍需后续闸门处理：

- G4-R11-C：`generated` 记录不能普通编辑。
- G4-R11-E：菜单按权限过滤。
