# G4-R11 独立验收记录 v0.1

- 日期：2026-07-16 19:00
- 验收人：Cursor（多角度审核 v0.1 的提出方，独立于实施方 Codex）
- 验收对象：G4-R11 五项放行前硬伤治理 + G4-R12 本地冻结（commit b27054a8，分支 codex/g4-r12-cloud-deploy-prep）
- 验收方式：运行态实测（本地 8010）+ 数据库只读核对 + 代码核对 + 重跑关键 smoke。除结束本次验收自产的 1 条 admin 会话审计记录外，未写业务库。

## 逐项验收结果

| 项 | 审核 v0.1 原始问题 | 实测结果 | 结论 |
|---|---|---|---|
| B1 debug | APP_DEBUG=true，trace 面板全角色可见 | 容器内 `APP_DEBUG=false`；`.env`/`compose.yaml` 均已改；登录页与登录态工作台均 0 命中 trace 特征 | **通过** |
| B2 账号 | 3 个弱口令 `password` 测试账号启用中、qm_test 口令漂移 | 5 账号扫描：weak=0、empty=0；`auditor_test/head_test/staff_test` publish=0 且开放会话=0；admin/qm_test 强口令入钥匙串（admin 口令实测可登录） | **通过** |
| B3 演示数据 | 孤儿"期间核查提醒"、参训人员空白的培训记录已出PDF | 提醒 publish=0/soft_delete=1；该记录 status=voided、标题冠"【演示数据-已作废】"、原 PDF 保留 | **通过** |
| C 记录编辑 | generated 记录可直接编辑且不留痕 | 实测 generated 记录（培训申请表-001）：详情页无可点击编辑链接、有"已形成 PDF 后不可直接编辑…"提示；直接访问 `/edit?id=` 被 302 回详情页 | **通过** |
| E 菜单过滤 | 主导航不按权限过滤，staff 31/41 入口点不进 | `main.html` 六大分组 + 菜单项两级 `qms_can` 守卫齐全（60+ 处）；`qms_ui_navigation_template_smoke` 通过 | **通过**（代码级+smoke；staff 运行态无法实测，因测试账号已按 B2 停用，属合理牺牲） |

## 回归验证

重跑 5 个关键 smoke 全部通过：`qms_ui_navigation_template_smoke`、`record_forms_instance_smoke`、`rbac_controller_normalization_smoke`、`qms_g4r7_trial_readiness_smoke`、`qms_g4r9_stale_link_guard_smoke`。

## 验收备注（不阻断，供后续闸门）

1. staff/auditor/head 有 `document` 读权限，故"体系文件"菜单内的**"新建文件"入口仍对其显示**（写操作会被中间件拦截）。属审核 v0.1"有点烦"第 8 条（动作按钮按权限渲染）范围，Codex 在 E 记录中已明确声明本轮不覆盖，口径一致，建议随试运行首月细化闸门处理。
2. staff 的"系统设置"分组因含"通知中心"仍会显示，但分组内只剩通知一项——功能正确，归类略怪，锦上添花级。
3. G4-R11 治理过程纪律良好：全库备份+SHA256、TDD 红绿记录、口令不落文档入钥匙串、验证会话自行收口，均有留痕。
4. G4-R12 冻结记录声明的边界（未上传云端、未切 current、未推送）与本地 git 状态相符。

## 验收结论

**G4-R11 五项放行前硬伤全部治理到位，验收通过。** 审核 v0.1 中"试运行放行前必须处理"清单清零，本地版本具备进入 G4-R12-C（云端部署前收口）的条件。
