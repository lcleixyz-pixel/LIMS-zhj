# G4-R11-B2/B3 追加复验记录 v0.1

日期：2026-07-16

复验时间：2026-07-16 14:09 JST

## 复验背景

用户再次批准 G4-R11-B2/B3。当前交接箱已有《03-G4-R11-B2B3账号与演示数据收口记录 v0.1》，记录显示该项已执行。为避免重复改库，本轮先做只读复验。

## 授权边界

- 只复验本地 `jewelry_qms` 数据库与本地页面。
- 不重复改密、不重复停用账号。
- 不删除记录、不删除 PDF、不部署云端。
- 不在交接材料中写入口令明文。

## 复验结论

G4-R11-B2/B3 当前仍保持完成状态：

- 活动账号弱口令数量：0。
- 活动账号空口令数量：0。
- `admin` 钥匙串口令可匹配数据库哈希。
- `qm_test` 钥匙串口令可匹配数据库哈希。
- `auditor_test / head_test / staff_test` 均已停用。
- 3 个停用测试账号开放会话数为 0。
- 本地 PHP session 文件未检出 `auditor_test / head_test / staff_test`。
- 孤儿期间核查提醒仍为 `publish=0, soft_delete=1`。
- 明显演示培训记录仍为 `status=voided`。
- 登录页可访问，未检出 ThinkPHP debug/trace 标记。

## 关键证据

### 账号弱口令与钥匙串匹配

```json
{
  "active_weak_count": 0,
  "active_empty_count": 0,
  "admin_keychain_match": "yes",
  "qm_test_keychain_match": "yes"
}
```

### 账号状态

```text
admin        publish=1 soft_delete=0
qm_test      publish=1 soft_delete=0
auditor_test publish=0 soft_delete=0
head_test    publish=0 soft_delete=0
staff_test   publish=0 soft_delete=0
```

### 停用测试账号开放会话

```text
open_sessions = 0
```

### 登录页 debug 复验

```text
status=reachable
html_bytes=5782
debug_markers=[]
```

## 复验差异说明

原 B2/B3 记录中写有以下烟测：

```text
qms_g4r7_trial_readiness_smoke passed
qms_g4r9_stale_link_guard_smoke passed
```

本轮在当前运行容器中复核时，`php think list` 未显示对应命令，直接运行 `php think qms:g4r7-trial-readiness-smoke` 返回：

```text
Command "qms:g4r7-trial-readiness-smoke" is not defined.
```

判断：这属于“交接记录中的验证命令名称/当前运行代码版本不一致”，不影响本轮 B2/B3 数据状态结论；但后续 G4-R12 打包/云端验证前，应以候选发布包内实际存在的验证命令为准重新梳理烟测清单，避免照抄旧命令。

## 后续建议

- 若继续云端准备，不应只依据本地容器旧命令名；应使用候选发布包/PR HEAD 对应的验证清单。
- B2/B3 本地数据状态已复验，不建议重复执行改密或归档动作。
