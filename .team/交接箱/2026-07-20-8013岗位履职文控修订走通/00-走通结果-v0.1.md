# 8013 岗位履职文控修订走通 · 结果 v0.1

- 日期：2026-07-20
- 环境：`http://127.0.0.1:8013`（`SIM-GOV-R2-20260719`）
- 目的：验证模拟岗位能否在沙箱内**真实走通**修订→审核→批准→分发→接收确认（及作废）

## 结论

**能走通。** 质量负责人、技术负责人、文件管理员按任命在 8013 内完成了文控闭环；检测员发起修订被拒（403）。

## 已走通链路

| 步骤 | 岗位账号 | 动作 | 结果 |
|---|---|---|---|
| 1 | `SIM-r2-document-controller` | 对 `XZTC/CX-09-2022` 发起修订 | 生成 `SIM-XZTC/CX-09-2022` A/0 draft；写入 `document_revisions` |
| 2 | `SIM-r2-technical-manager` | 审批 level 2 | approved |
| 3 | `SIM-r2-quality-manager` | 审批 level 3 | approved → 文件状态 **`trial_ready`** |
| 4 | `SIM-r2-document-controller` | 分发给 QM / TM / 乌市场所负责人 | 3 条 `document_distributions` |
| 5 | 上述三人 | 确认接收 | `confirmed_at` 已填 |
| 6 | `SIM-r2-quality-manager` | 对 `SIM-XZTC/BG-10` 评审作废 | status=`obsolete`，`document_reviews.result=obsolete` |
| 7 | `SIM-r2-document-controller` | HTTP 再修 `XZTC/CX-11-2022` | 生成 `SIM-XZTC/CX-11-2022` A/0 draft（验证 FILES 空附件修复） |
| NEG | `SIM-r2-tester-urumqi` | 打开修订页 | **403** |

查看入口（登录密码均为 `password`）：

- 合同评审程序 SIM 修订版：`/document/view?id=7becc889-741c-4d53-81ad-f169790efda3`
- BG-10 作废样例：`/document/view?id=sim-bg10-obsolete-20260720`

## 过程中修掉的阻塞

1. **无附件 POST 修订 500**：`$_FILES['document_file']['name']` 在无上传时触发未定义键。已改为 `$_FILES['document_file']['name'] ?? ''`（worktree + 8013 容器 + 主仓 `jewelry-qms`）。
2. **现用程序缺审核人**：修订前为 `XZTC/CX-09` / `CX-11` 写入 `reviewed_by=SIM-R2-E-TM`、`approved_by=SIM-R2-E-QM`（否则工作流缺 2/3 级，永远批不完）。
3. **原生 `qms_uuid()` 破坏 SIM 纯度**：修订/审批/分发产生的运行表 id 需改写为 `sim-…` 才能过纯度守卫。本轮事后改写并通过；产品侧尚未让排演环境默认发 `sim-` 前缀 id。

## 权限事实（与想象差异）

- 文件**最终批准岗位是质量负责人**（level 3），不是实验室主任；主任不在 `ApprovalService` 文件批准岗位列表。
- 文控（`staff` + `document_controller` 任命）可走 `document/revise`、`distribute`（动作授权放行，绕过 RBAC 仅 admin/QM 可写文档的通用限制）。
- 试运行下新版本编号强制 `SIM-` 前缀，批准后状态是 **`trial_ready`**（不是正式 `published`）；正式 `XZTC/CX-09` 原件仍保持 published（SIM 修订不顶替正式件）。

## 证据

- [证据-库表快照-v0.1.txt](./证据-库表快照-v0.1.txt)
- SIM 纯度：`assert-sim-operational-data.sql` → `purity_ok=1`（改写 id 后）

## 未做 / 已知缺口

- CX-11 SIM 草稿未继续批到 `trial_ready`（仅作 HTTP 修订可用性验证）。
- 结构化同步：`refreshControlledDocumentStructure` 对无结构化底稿文件报「尚未生成结构化文档」（控制器已 catch，不挡修订）。
- 产品未默认 SIM-id：每次 UI 修订后若要过纯度门禁，仍需改写或改 `qms_uuid` 策略。
- 未改 8010 / 现用 Word / 145 基线。
