# 活动级责任链零污染与回滚证明

- 版本：v0.1
- 日期：2026-07-14
- 隔离 Compose 项目：`lzhj-responsibility-test`
- 被测提交：`cd3bac6e1eeedaad30faca9bc8283b3b53d43006`
- 最终复验项目：`lzhj-responsibility-finalverify3-0714`
- 最终复验提交：`ecb8544c66b7e3c3de19b2d1a44485eda5376f67`
- 结论：知识导出层零变化、生产数据库零接触、RBAC 零变化、事务故障无部分生效，隔离资源已回收。

## 一、隔离边界

- 测试先对固定隔离项目执行 `down -v --remove-orphans`，再从新建空卷 `lzhj-responsibility-test_db_data` 重建。
- `db` 服务没有宿主机端口映射，未连接生产数据库。
- 应用使用隔离项目命名的容器、网络、依赖卷和数据库卷。
- 官方参考材料仅以只读方式挂载；未写入参考目录。
- 未对正式数据库执行迁移，也未执行真实公司总经理登记、真实实验室主任任命、真实人员签批、部署、发布或远端合并。
- 未修改质量手册、程序正文或 `knowledge/internal/`。

## 二、知识导出层哈希对比

运行前后均按文件路径字典序，对 `knowledge/internal/` 全量文件计算 SHA-256。

| 项目 | 运行前 | 运行后 |
|---|---:|---:|
| 文件数 | 46 | 46 |
| 清单 SHA-256 | `0f227b041d34195982de0977a9c761cc6448771520143e8b118374dcb916b18a` | `0f227b041d34195982de0977a9c761cc6448771520143e8b118374dcb916b18a` |
| `diff -u` 输出行数 | — | 0 |

比较命令：

```bash
find knowledge/internal -type f -print | LC_ALL=C sort | while IFS= read -r file; do
  shasum -a 256 "$file"
done
diff -u /tmp/lzhj-task8-knowledge-before.txt /tmp/lzhj-task8-knowledge-after.txt
```

哈希清单保存在本机临时目录，仅包含文件路径与摘要；交接包不保存源文件副本。

## 三、RBAC 零变化

端到端测试在提交责任链前读取隔离公司全部测试用户的 `user_id → role` 映射，责任链签批、任命生成和变更草案克隆后再次读取并排序比较，结果严格相等：`rbac_roles_unchanged=yes`。

责任链任命只产生业务证据，不自动授予或撤销软件角色。

## 四、事务回滚证明

批准测试预置一个可预测的任命唯一键冲突，使最终批准在已经进入任命生成阶段后失败。实际断言结果：

- 新版本维持 `pending_approval`；
- 新版本责任绑定维持待批准；
- 新版本生成任命数为 0；
- 旧版本维持 `effective`，旧任命维持 `active`；
- 最终批准批次维持 `pending`；
- 清除故障后原批次重试成功。

这证明版本切换、绑定激活、批准签名和任命生成处于同一事务边界，没有“签了一半”或责任空窗。

## 五、隔离资源回收

完成哈希比较和证据登记后执行：

```bash
docker compose -f compose.yaml -f compose.responsibility-test.yaml \
  -p lzhj-responsibility-test down -v --remove-orphans
```

清理后于 2026-07-14 03:15:37Z（本地 12:15:37+0900）按 Compose project 标签复查：

```bash
docker ps -a --filter label=com.docker.compose.project=lzhj-responsibility-test
docker network ls --filter label=com.docker.compose.project=lzhj-responsibility-test
docker volume ls --filter label=com.docker.compose.project=lzhj-responsibility-test
```

| 标签查询对象 | 剩余数量 |
|---|---:|
| 容器 | 0 |
| 网络 | 0 |
| 卷 | 0 |

固定隔离项目资源已清空；Compose 项目 `jewelry-qms` 未执行任何命令、未受影响。

## 六、最终 HEAD 零污染复核

最终 19 项复验于 2026-07-14 04:55:07Z（本地 13:55:07+0900）收口。复验对象为提交 `ecb8544c66b7e3c3de19b2d1a44485eda5376f67`，使用全新项目 `lzhj-responsibility-finalverify3-0714`，没有复用前述阶段验收数据库卷。

| 检查点 | 最终结果 |
|---|---|
| `knowledge/internal/` 清单 SHA-256 | `0f227b041d34195982de0977a9c761cc6448771520143e8b118374dcb916b18a`，与验收前一致 |
| 测试工作树 `git diff --check` | 退出 0 |
| 最终代码复验收口时工作树 | clean |
| 私有官方参考材料 | 仅只读挂载 |
| 生产数据库/正式 Compose 项目 | 未连接、未执行命令 |

最终复验项目执行 `down -v --remove-orphans` 后，按 `com.docker.compose.project=lzhj-responsibility-finalverify3-0714` 标签查询：

| 标签查询对象 | 剩余数量 |
|---|---:|
| 容器 | 0 |
| 网络 | 0 |
| 卷 | 0 |

因此，最终 HEAD 的全量复验同样没有遗留隔离资源，也没有改变知识导出层。
