# 现用镜像重建执行单（待授权）v0.1

**状态：未执行。** 需用户一句话授权后再跑。

## 前置

1. `feature/wave1-defect-governance` 已审查并合入拟发布提交
2. 备份 8010：`mysqldump` + 保留 `8010-checksums-phase0.txt` 对照
3. 确认 Poppler 相关 Dockerfile 改动在拟发布提交中

## 命令

```bash
cd jewelry-qms
docker compose build app
docker compose up -d app
docker compose exec app php tests/qms_wave1_s1_no_get_writes_smoke.php
# 按需复跑冻结清单子集与外部依据抽取
```

## 不做

- 不 `docker compose down -v`
- 不灌沙箱 dump 进 8010
