# G-R13-B3 执行记录

> 版本：v0.1  
> 日期：2026-07-17  
> 闸门：关键记录留痕失败测试与最小修复  
> 结论：通过，可结束 B3；未推送、未合并、未部署

## 1. 实施结果

- 把不符合、客户投诉、能力确认和法规候选人工复核纳入精确字段留痕；
- 保留文件、CAPA、设备、内审发现和正式变更事件既有留痕能力；
- 只记录真实变化，空值等价、布尔等价和同一日期的不同时间格式不产生假变更；
- 留痕写入失败时，关键业务写入与留痕整体回滚，并返回不暴露堆栈的中文错误；
- 字段历史显示中文字段名、中文业务状态、业务编号、操作人姓名和在任岗位，不向业务人员暴露裸 UUID；
- 既有权限拒绝决定写入独立安全审计，不混入字段变更历史；
- 未改变岗位授权策略，RC-03 仍留待 B4 单独治理。

## 2. 分支与提交

- 分支：`codex/g-r13-b3-field-audit`
- 基线：B2 提交 `14e5fadb`
- 红灯提交：`da902570`
- 后端最小修复提交：`00f94feb`
- 页面可读性提交：`388b4b72`
- 工作树：`.worktrees/g-r13-b3-field-audit`

## 3. 失败基线与绿灯结果

- 红灯阶段：T01～T10 全部按预期失败，确认缺少关键模型覆盖、错误被吞、日期假变化、页面裸技术值和权限拒绝未独立留痕等根因；
- 绿灯阶段：T01～T10 全部通过；
- B1 字段契约 C01～C12：全部通过；
- B2 编号与 CAPA L01～L10：全部通过；
- 原有字段留痕、通用表单校验、内审 CAPA 增强：全部通过；
- 所有本批新增或修改 PHP 文件语法检查通过；
- `git diff --check` 通过。

## 4. 页面级验收

在独立数据库和临时应用容器中，使用专用夹具检查以下页面：

| 页面 | HTTP | 业务可读性 | 技术信息边界 |
|---|---:|---|---|
| 能力确认详情 | 200 | 显示“评价结果：待评价 → 合格” | 不显示操作人 UUID |
| 不符合详情 | 200 | 显示“影响评估”及原值、新值 | 不显示操作人 UUID |
| 客户投诉详情 | 200 | 显示“状态：已受理 → 调查中” | 不显示操作人 UUID |
| 法规候选详情 | 200 | 显示“人工确认状态：待人工确认 → 确认适用” | 不显示操作人 UUID |

四页均显示“页面留痕操作人（质量负责人）”，未出现 PDO 异常、调试堆栈或系统错误页。

## 5. 失败关闭与权限边界

- 字段历史表不可用或写入失败时，关键业务修改不落库；
- 页面仅显示可供业务复核的留痕，不把技术异常详情呈现给普通岗位；
- 越权请求仍按原有 RBAC 规则拒绝，只额外记录“谁在何时尝试了什么动作及失败结果”；
- 本批不新增授权、不放宽授权，也不把系统管理员身份等同业务批准身份。

## 6. 隔离与清理

- 临时页面容器 `qms-r13b3-page` 已删除；
- 隔离数据库 `jewelry_qms_p0_r13b3` 已整库删除，复查剩余数为 0；
- 临时 Cookie 和页面响应文件已删除；
- 现用 `http://127.0.0.1:8010/login/index` 返回 HTTP 200；
- 现用 `jewelry-qms-db-1` 保持 healthy；
- 未写当前数据库，未推送、未创建 PR、未合并、未部署云端。

## 7. 下一闸门建议

进入 `G-R13-B4“岗位动作授权失败测试与最小修复”`，以已确认的岗位—动作矩阵治理 RC-03。B4 会改变权限判定，必须单独批准后再执行；不得借 B3 留痕通过而提前开放岗位动作。
