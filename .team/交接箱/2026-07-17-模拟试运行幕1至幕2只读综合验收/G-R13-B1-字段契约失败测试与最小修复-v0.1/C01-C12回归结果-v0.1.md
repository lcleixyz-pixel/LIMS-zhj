# C01–C12 字段契约回归结果

> 版本：v0.1  
> 日期：2026-07-17  
> 基线：`04c29249769c09ce2d4b9bad3e690592f25249c2`  
> 隔离分支：`codex/g-r13-b1-field-contracts`  
> 测试环境：独立 MySQL 8.4 数据库 `jewelry_qms_p0_r13b1`、独立页面实例 `127.0.0.1:18013`

## 1. 红灯基线

首次运行 `tests/qms_p0_field_contract_smoke.php`：

- PASS：C05、C09、C10；
- FAIL：C01、C02、C03、C04、C06、C07、C08、C11、C12；
- 失败均命中既有字段错接、白名单缺失、编辑校验或软删除读取问题，没有依赖、缺表或测试夹具型假失败。

失败基线提交：`350d46b3 test(p0): 固化字段契约失败基线`。

## 2. 修复后结果

| ID | 结果 | 实测证据摘要 |
|---|---|---|
| C01 | PASS | 能力确认四页面仅使用 8 个业务字段；通用 `name/code/department_id` 已移除 |
| C02 | PASS | 有效 POST 返回 302；能力确认记录 0→1 |
| C03 | PASS | 仅提交结果和有效期后，员工/项目保持不变；结果为 `qualified`，有效期为 `2028-07-17` |
| C04 | PASS | 缺员工/项目返回中文字段提示；记录数 0→0 |
| C05 | PASS | 不符合登记返回 302；编号、日期、来源、严重度和中文状态页面均可读 |
| C06 | PASS | 仅改影响评估后，原描述保持不变，状态仍为 `open` |
| C07 | PASS | 伪造 `status=draft` 和未知字段被忽略；数据库状态仍为 `open` |
| C08 | PASS | CAPA 编辑不提交编号仍返回 302；编号、来源保持 `P0R13B-CAPA-001/internal` |
| C09 | PASS | 编辑提交空描述返回中文提示；原描述、根因均不变 |
| C10 | PASS | 投诉登记返回 302；列表显示投诉编号、客户、期限和中文状态 |
| C11 | PASS | 仅改联系方式和期限后，状态仍为 `received`，调查/处理/反馈仍为空 |
| C12 | PASS | 投诉软删除标志为 1；旧详情和旧编辑 URL 均返回 HTTP 404 |

修复提交：`2baf23e6 fix(p0): 统一关键质量记录字段契约`。

## 3. 相关非回归

以下脚本在同一隔离 MySQL 8.4 测试库重新运行并通过：

- `qms_p0_field_contract_smoke.php`
- `qms_validation_smoke.php`
- `qms_field_audit_smoke.php`
- `qms_audit_capa_enhancement_smoke.php`
- `qms_business_module_mapping_smoke.php`
- `rbac_controller_normalization_smoke.php`
- `qms_ui_navigation_template_smoke.php`
- `qms_responsibility_validation_smoke.php`

四模块列表、新增、详情、编辑页面均完成真实登录态访问，返回 200 且未出现调试栈。正式逐按钮截图仍按 P0 计划留在 G-R13-B5 页面级验收，不在 B1 冒充完成。

