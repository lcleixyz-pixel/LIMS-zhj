# G-R13-B1 执行记录

> 版本：v0.1  
> 日期：2026-07-17  
> 闸门：字段契约失败测试与最小修复  
> 结论：通过，可结束 B1；未部署云端

## 1. 实施范围

- 为通用 CRUD 增加“目标模块启用、其他模块保持原行为”的写字段白名单；
- `view/edit/delete` 对软删除记录统一按不存在处理；
- 统一能力确认、不符合、CAPA、投诉的页面字段、服务端校验和编辑保存边界；
- 不符合、投诉的工作流状态不再由通用编辑覆盖；
- CAPA 编辑不再强制重复提交编号，编号和来源字段不可由编辑页改写；
- 列表和详情优先显示业务编号、人员姓名及中文状态。

## 2. 明确未实施

- 未修改编号生成和 CAPA 双向关联（RC-02）；
- 未修改字段审计策略（RC-04）；
- 未实施岗位动作授权（RC-03）；已确认矩阵仅作为后续 B4 输入；
- 未写入现用 8010 数据库，未切换 current，未构建或部署云端。

## 3. 隔离与清理

- 主工作区既有修改保持不动；
- 实施位于 `.worktrees/g-r13-b1-field-contracts`；
- 测试数据统一使用 `P0R13B-` 标识；
- 验证结束后已删除临时页面容器 `lzhj-gr13-b1-app`；
- 已整库删除 `jewelry_qms_p0_r13b1`，复查结果为不存在；
- 现用 `http://127.0.0.1:8010/login` 复查 HTTP 200；
- 现用 `jewelry-qms-app-1` 继续运行，`jewelry-qms-db-1` 保持 healthy。

## 4. 版本状态

- 分支：`codex/g-r13-b1-field-contracts`
- 红灯提交：`350d46b3`
- 最小修复提交：`2baf23e6`
- 当前仅为本地候选分支，未推送、未创建 PR、未部署。

## 5. 下一闸门建议

进入 `G-R13-B2“编号与 CAPA 双向关联失败测试和最小修复”`。开始前先在独立测试库执行 RC-02 只读预检和 L01～L10 失败基线；不与 B4 动作授权混做。

