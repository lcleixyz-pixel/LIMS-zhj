# G-R13-B 执行记录

> 版本：v0.1  
> 日期：2026-07-17  
> 范围：P0 修复规格与回归矩阵；只读分析，不实施修复。

## 任务卡

- 我要做什么：把 G-R13-A 的 4 个 P0 根因转成工程可执行规格和回归闸门。
- 材料在哪里：G-R13-A 归并表、当前 HEAD 的控制器/视图/模型/服务/测试，以及本机 MySQL 只读聚合结果。
- 不能碰什么：不修改 `jewelry-qms` 代码、不写数据库、不清理数据、不部署云端、不覆盖冻结证据。
- 做完怎么算成功：每个 P0 有明确文件边界、业务规则、数据处置和通过/失败标准；授权不靠姓名硬编码；计划可交给下一个执行会话。

## 本次只读核对

1. 当前基线：`04c29249769c09ce2d4b9bad3e690592f25249c2`。
2. 确认能力确认、不符合和投诉页面仍存在通用字段错接；CAPA 编辑仍受缺失编号校验阻断。
3. 确认编号函数存在完整编号末尾取数问题；WorkflowService 尚无创建/回写同事务与幂等保证。
4. 确认当前 RBAC 为控制器级粗筛，动作授权未读取岗位任命和场所。
5. 确认 FieldAuditService 尚未覆盖不符合、投诉、能力确认和法规候选，且审计失败静默放行。
6. 本机 MySQL 只读聚合结果：数字型 CAPA 来源/反向关联均为 0；投诉/CAPA 重复编号均为 0；字段审计现存模型只有 Document。该结果不代表云端当前状态。

## 形成的决定

- P0 分 5 批：字段契约→编号/关联→留痕→经批准的动作授权→页面验收。
- 动作授权复用现有岗位任命和场所范围，不新增粗角色、不写死人员姓名。
- 云端和本机分别执行只读预检；无人工映射不得自动改历史关联。
- 规格中的动作矩阵为候选，实施任务 5 前必须由质量负责人确认。

## 下一闸门

建议进入 `G-R13-B1“字段契约失败测试与最小修复”`，先处理 RC-01；该批不依赖岗位动作矩阵，可在独立 worktree 中开始。RC-03 实施保持后置停点。

## 版本记录

| 版本 | 日期 | 变更 | 状态 |
|---|---|---|---|
| v0.1 | 2026-07-17 | 完成当前代码核对、P0 规格、回归矩阵和实施分批 | 现行 |
