# G-R13-B2 执行记录

> 版本：v0.1  
> 日期：2026-07-17  
> 闸门：编号与 CAPA 双向关联失败测试和最小修复  
> 结论：通过，可结束 B2；未部署云端

## 1. 实施结果

- 修正年度编号算法：只解析“前缀 + 四位年份 + 3～6 位流水号”，异常旧编号不参与最大序号计算；
- 投诉、CAPA、不符合编号增加机构内唯一约束；
- CAPA 来源增加 `(company_id, source_type, source_record_id)` 唯一约束；
- CAPA 创建、来源回写和幂等查询进入同一数据库事务；
- 重复或并发创建同一来源 CAPA 时返回既有记录，不重复发送任务通知；
- 来源不存在、UUID 格式错误或来源已关联其他 CAPA 时明确拒绝且 0 写入；
- CAPA 详情把来源 UUID 转为中文来源类型和业务编号链接；
- 新增只读命令 `qms:p0-preflight`，只输出汇总数量和候选记录 ID；
- 新增受控迁移；重复数据存在时主动阻断，不自动修数。

## 2. 失败基线与提交

- 分支：`codex/g-r13-b2-workflow-links`
- 基线：B1 提交 `2baf23e6`
- 红灯提交：`af114b88`
- 最小修复提交：`14e5fadb`
- 红灯结果：L04、L06、L09 通过；L01、L02、L03、L05、L07、L08、L10 失败。
- 绿灯结果：L01～L10 全部通过。

## 3. 数据与迁移闸门

1. 隔离库 `jewelry_qms_p0_r13b2` 先执行只读预检；
2. 首次发现 1 条红灯测试残留孤儿 CAPA，经记录标识核实后仅在隔离库删除；
3. 再次预检 9 类计数全部为 0，才执行唯一约束迁移；
4. 迁移重复执行通过；
5. 另建阻断测试库并制造重复投诉编号，迁移以 SQLSTATE `45000` 主动中止；
6. 未在当前本地库执行迁移，未修改当前业务数据。

## 4. 页面与回归证据

- CAPA 详情：HTTP 200，显示“客户投诉 / P0R13B2-PAGE-CP”，业务编号可返回投诉详情；
- 投诉详情：HTTP 200，显示“P0R13B2-PAGE-CAPA”，业务编号可打开 CAPA 详情；
- L01～L10：全通过；
- B1 字段契约 C01～C12：全通过；
- `qms_validation_smoke.php`：通过；
- `qms_audit_capa_enhancement_smoke.php`：通过；
- 修改后的完整建库 SQL 已在新建临时库导入成功；
- 现用 `http://127.0.0.1:8010/login/index`：HTTP 200。

## 5. 岗位—动作矩阵边界

已确认的岗位—动作矩阵继续作为 RC-03 的正式输入。本批只治理 RC-02 数据一致性，不因 B2 通过而提前放宽任何岗位权限，也不把系统管理员身份等同业务批准身份。

## 6. 隔离与清理

- 临时页面容器 `qms-r13b2-page` 已删除；
- 隔离数据库 `jewelry_qms_p0_r13b2` 已整库删除，复查剩余数为 0；
- 阻断测试库与建库验证库均已删除；
- 现用 `jewelry-qms-app-1` 继续运行；
- 现用 `jewelry-qms-db-1` 保持 healthy；
- 未推送、未创建 PR、未合并、未部署云端。

## 7. 下一闸门建议

进入 `G-R13-B3“关键记录留痕失败测试与最小修复”`，处理 RC-04 的 T01～T10。RC-03 动作授权仍保持后置，不与留痕修复混做。
