# 条款库要求原子补录实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 以可预演、可回滚、幂等的方式向 jewelry-qms 补录 62 个要求原子，并登记 CNAS-CL01 等同关系。

**架构：** 新增一个纯解析/规划服务，从 T1 包内三份 Markdown 按白名单提取要求原文；新增一个 ThinkPHP 命令负责预演报告和事务写入。数据库写入只消费已经通过完整性校验的计划，不调用现有会混入目录节点的通用抽取器。

**技术栈：** PHP 8、ThinkPHP 8、MySQL、现有 smoke 测试框架。

---

## 文件结构

- 创建 `jewelry-qms/app/service/QmsClauseRemediationService.php`：白名单、Markdown 原文块提取、预演计划、事务写入和等同说明。
- 创建 `jewelry-qms/app/command/QmsClauseRemediate.php`：`--source-dir`、`--output-dir`、`--apply` 命令入口。
- 修改 `jewelry-qms/config/console.php`：注册补录命令。
- 创建 `jewelry-qms/tests/qms_clause_remediation_smoke.php`：要求原子数量、排除项、内嵌编号、幂等和事务边界测试。
- 创建 `.team/交接箱/2026-07-13-条款库要求原子补录/`：备份、预演、执行结果和核验报告。

### 任务 1：用测试锁定要求原子口径

- [x] 创建 smoke 测试，断言 GB/G001/A015 数量分别为 7/37/18，总数 62。
- [x] 断言排除 GB `3.1–3.9`、A015 `2.1–2.8` 和所有纯章节标题。
- [x] 断言能够从同一行独立抽取 G001 `6.6.1c)` 与 A015 `7.4.4`。
- [x] 运行 `php tests/qms_clause_remediation_smoke.php`，确认服务不存在时失败。

### 任务 2：实现解析、预演与写入

- [x] 创建 `QmsClauseRemediationService`，只接受三份固定来源代码和批准白名单。
- [x] 为每个条款生成 `source_code/clause_number/title/original_text/locator/text_hash/review_note`。
- [x] 查询现库并将计划分成 `insert/existing/conflict`；冲突非空时禁止 apply。
- [x] 使用数据库事务写入 `qms_clauses` 与 `qms_clause_texts`，并追加 CNAS-CL01 等同说明。
- [x] 实现报告输出并注册 `qms:remediate-clause-library`。
- [x] 再次运行 smoke 测试，确认通过。

### 任务 3：现库备份和只读预演

- [x] 使用现运行数据库容器导出三张相关表到交接箱 SQL 文件。
- [x] 运行命令但不加 `--apply`，生成逐行 JSON/CSV/Markdown。
- [x] 核对 `conflict=0`、计划新增数与现库状态，并确认排除项不在预演中。

### 任务 4：事务补录和复核

- [x] 在同一来源目录上加 `--apply` 执行补录。
- [x] 再次只读预演，确认 `insert=0`、`existing=62`、`conflict=0`。
- [x] 查询三份依据的数据库计数、重复编号、缺失文本、哈希不一致和 CL01 独立条款数。
- [x] 用 HTTP 登录前可访问性检查确认系统仍正常响应，并运行相关 smoke 测试。

### 任务 5：交接和版本补正

- [x] 将 v0.2 中 A015 漏掉 `7.4.4` 的事实写入 v0.3 补正说明。
- [x] 生成写库清单、回滚说明和最终核验报告。
- [x] 打包交接目录到桌面，更新计划复选框并提交本次代码与文档。
